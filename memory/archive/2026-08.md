# GenericAgent Memory

> 仅追加，不覆盖历史。写入前先读当前内容。

## [2026-08-06 | init-project 初始化]

- **Events**：建立项目协作文件体系（此前仓库无任何规则/协作文件）。
- **Changes**：生成 `AGENTS.md`（canonical 规则）、`tools.md`（协作知识）、`memory.md`（状态快照）、`memory/archive/2026-08.md`；`.gitignore` 为 `memory/archive/` 补白名单。`memory.md` 的初始内容参考了最近 8 条 git 提交，用于恢复近期项目状态。
- **Insights**：根 `memory/` 目录是应用运行时记忆（L0-L4 + SOP，逐文件 gitignore 白名单），与协作 `memory.md`/`memory/archive/` 是两套体系，后续维护勿混淆。从现在开始，重要根因分析、调试洞察、关键决策和踩坑按月追加到本文件。

## [2026-08-06 | 全项目审查 + 14 项修复]

- **Events**：对 main@335b3bf4 全项目只读健康/安全审查（5 条并行审查链 + 2 个反证 reviewer），确认 11 项分级 findings；随后用户决策"去掉工具策略分级"并授权修复全部已确认问题。实施 14 项修复，全程在隔离 PostgreSQL 库验证。
- **Changes**：
  - D1 去分级：`tool_policies` 表/`users.tool_policy_version` 列删除（迁移 0046，注意迁移 SQL 必须自带 marker 表创建）；router 固定 `foundation.session-files.v1`；policy CRUD/分配 API 与存储层删除；OpenAPI 同步；契约测试断言同步。
  - D3 用户生命周期：BlockUser 事务内撤销全部会话；Login/ValidateSession 拒绝 blocked（含单测 + store 测试）。
  - D4 运行上限原子化：`ClaimNextTask` 事务内 advisory lock + 计数（`SetRunningTaskLimit`），跨实例不超卖。
  - D2 成功路径销毁时序：`sessionDraining` 标记在终态提交到销毁完成间阻止同 session claim（跨实例天然安全：接管即 generation+1 新容器）。
  - F1 凭据失败路径：新增 `WorkerRuntime.ExpireRunnerLease`（归还容量保留容器引用），scheduler 初始化失败改用它。
  - F3 lease 时钟统一：expires_at 由 `timezone('utc', now()) + make_interval(secs=>$n)` 计算，过期判定/列表/计数全部 DB 时钟。
  - F2 delivery fencing：claim 生成 `attempt_token`（md5(random()||clock_timestamp())），Ack/Retry/DeadLetter 必须携带（迁移 0047）。
  - D5+F4 根 Python：LLM 传输/HTTP/解析故障重试耗尽 → `LLM_FAILED` 结构化终态（worker 映射 TASK_FAILED）；`llmclient=None` 时显式错误终态不再线程崩溃。
  - F5 Web：Dashboard 改用 AuthContext 登录响应状态（不再调 admin pending 接口）；AuthProvider 保留 userId/status。
  - F6：`.tasks/sandbox-runner-refactor/SUBTASKS.csv` 从 HEAD 恢复误删 5 行 + 25 行全量规范化（字段含逗号必须引号）。
  - F7：新增 `test_route_contract.py` 自动比对后端路由 vs OpenAPI（新路由未同步 spec 立即失败；存量缺口白名单收敛）。
- **Insights**：
  - 动态策略双真值的根因是 2026-07-25 一次提交（271e0f9f）捆绑引入的"半成品"：管理 API 可创建策略但执行链只认静态 manifest。去分级比打通更务实——容器隔离（runsc + workspace 子路径）本身就是安全边界。
  - 成功路径先提交终态再销毁 Worker 的窗口是"释放串行槽"与"容器回收"解耦问题；失败路径 destroy-before-finalize 是对的，成功路径不能简单前移（destroy 失败不能让成功变失败），draining 标记是正确解。
  - `EnsureToolPolicyExists` 在 main.go 从未被调用（死代码随 D1 删除）；`CreateUser` 空密码 hash 是 scanUser 扫 NULL 崩溃的真实 bug（仅 CreateUser 的 RETURNING 缺 COALESCE）。
  - CSV 未转义逗号会静默吞行（csv 解析把后续行并入前一字段），比"列数错位"更危险——恢复必须从 git 历史取原始 25 行。
  - `go test` 用 `-r` 参数会触发 rg 的 replace 模式把匹配文本替换显示，审查时容易误读输出。

## [2026-08-06 | 审查 I-4 鉴权统一: 根因与设计决策]
- **Events**：审查发现任务端点走共享 dev-token 无 owner 校验，真实用户无法查自己任务；集成测试改造后暴露 pending 用户任务必然失败的隐藏缺陷（capability 在线校验要求 approved）。
- **Changes**：身份模型统一为用户（Bearer）；AdminToken 仅管理面；任务端点 owner 校验（session 归属 + RequesterID，越权 404）；提交门禁加 approved 校验；OpenAPI 32 条存量 gap 补齐并清空白名单；非ce 持久化分段追加、IM 聚合后台重试、死 fallback 移除。
- **Insights**：1) dev-token 是"管理员凭证"的历史命名误导，改名要全链路（env/header/Go 标识符/OpenAPI/测试/部署文档）且注意 git checkout 会丢未提交修改；2) 任务归属校验放 application 层（store.GetTask 被 scheduler 内部大量使用，不能加参）；3) 集成测试直插 user_sessions 需要 sha256-base64url(token) 与 Go hashToken 一致；4) OpenTestPool 全局互斥锁同测试二次调用死锁。

## [2026-08-06 | Round15 P2 真值源收敛: 设计判断与洞察]
- **Events**：Round14 报告 P2 六项（workspace hash 收敛/ctrl: JTI 进 proto/per-requester 命名/决策编号/活跃 claim 谓词/tools_schema_cn 去留）全部实施完毕并全量验证。
- **Changes**：domain.WorkspaceDirHash 唯一实现（删 sandbox 重复版 + checkpoint/application 裸 sha256 + 隐藏第5处 scheduler configDirFor；SandboxRoot 接口改 (string, error) 强制校验）；worker.proto 写入 ctrl: 约定并重生成双端绑定；PER_REQUESTER_RUNNING_LIMIT 改名全链路；SQL 谓词收敛为 postgres 包两常量（18 处替换）；平台 overlay 删 tools_schema_cn 死资产。
- **Insights**：1) "4 套 hash"实际是 5 套（审查漏了 scheduler configDirFor，grep sha256.Sum256 全仓才暴露）；2) ExecuteTask 不查 ctrl: 前缀是**有意的不对称**（loopback 回退 firstJTI 无前缀），契约注释必须写明"不对称是有意的"防止后人"修复"它；3) 活跃 claim 谓词实际 18 处而非 5 处，且 runner_leases 表有 status 列——多表 SQL 去前缀会 ambiguous column，收敛常量必须支持别名；4) task_lifecycle 容量门禁的 status 谓词**故意不含 lease 条件**，盲目统一成"status+lease"会绕过容量上限；5) tools_schema_cn 与 tools_schema 内容已全量漂移（9/9 工具描述不同），GA 只加载后者，平台拷 cn 是纯死资产；6) proto 注释会进入生成代码，改契约注释必须重跑 generate_bindings.py，否则生成产物与 proto 漂移。

## [2026-08-06 | Round15 P3/P4: 文档校准 + 分层/边界修复]
- **Events**：E 组文档校准（§9 阶段4 与最终决策对齐、worker 过时"复用 Runner"注释、bot-poller-auth 行号+/config、.tasks 16/17 行 status 修正、managed_agent 模块清单、psutil 死依赖）+ F 组（api 层常量上移 domain、UTF-8 截断收敛、心跳基线）+ 残余风险确认。
- **Changes**：domain/limits.go 8 个上限常量（postgres/api 引用，api 层不再因常量 import postgres）；domain.TruncateUTF8 单一实现（boundMsg/truncateBytes 删除，+3 单测）；TaskRunState.last_progress_at 以 put_task 时刻为基线（task_runner 构造传 time.monotonic()，修复启动后首轮长思考心跳被抑制）；150/120/300 三方契约写进 task_drain/llm-proxy transport 常量注释 + IMPLEMENTATION_SUMMARY 部署契约节；LLM_PROVIDER_ARCHITECTURE 记录 routing 竞争窗口为有意设计。
- **Insights**：1) boundMsg 是"固定上限截断"，收敛为通用 TruncateUTF8 时**调用点必须补 limit 参数**（跨文件调用点易漏，go build 暴露）；2) 心跳基线 bug 的根因是 monotonic 时钟初值 0.0 与进程 uptime 混用——初值应为"任务开始时刻"而非"进程 epoch"；3) provider revision 校验的竞争窗口是**有意的 fail-closed 设计**（409→TASK_FAILED 可重试），文档显式记录防止被当 bug 修；4) E2 报告的行号已漂移（task_ops/session_lifecycle 的"seed 继承"实为 checkpoint 恢复机制，不是过时注释——以代码实际语义为准，避免误删）。

## [2026-08-06 | Round16: GA 根项目首轮审查 + 平台遗留语义修复]
- **Events**：Round14 将 GA 根项目列为黑盒未审查；本轮首次深审 ga.py/agentmain.py/llmcore.py/agent_loop.py + 平台遗留项复核，发现 2 个真实缺陷（G1/G2）+ 平台 API 语义问题（P1/P2），并清掉 8 项 Round14 遗留未修项。
- **Changes**：G1 `_retry_or_exit` 删除 fatal 分支——3 次空响应/流异常/max_tokens 统一 LLM_FAILED（原非 fatal 返回空 dict，被 agentmain 当普通完成提交空结果 TASK_SUCCEEDED）；G2 `total_cd_tokens` 只累计本条消息增量（原把累积拼接的整个 user 字符串重复相加，O(n²) 虚高越过 9000 阈值导致工具描述每轮重注入）；P1 domain 新增 `ErrSessionAccessDenied`/`ErrWorkspaceNotFound` 哨兵，SubmitTask 错误分级 429/403/404/500（原全 500）；P2 validateSessionAccess 改用 `domain.ValidateWorkspaceKey` 严格解析（原 Sscanf 吞 trailing garbage，`personal:123abc` 被误判 uid=123 通过归属校验）+ team 只收 uuid（原整数 id 在 store `$1::uuid` cast 处 SQL 报错 500）；C3 task_terminal cancel/timeout 终态收敛唯一实现；C6 sandbox-manager help 与实现对齐；F5 docstring 修正；F6 TaskRunState 补齐 3 个 unwrap 显式字段；F7 ensure_session_dirs 改名；B5 domain.PersonalSessionKey 唯一实现；B8 cancelCall 移入 scheduler_worker.go；C2 poller 删 window>0 死分支（测试改挂 InboundCoalescingBuffer）。
- **Insights**：1) GA 侧"黑盒"不等于"无缺陷"——平台正确性依赖 GA 消费侧判定，G1 与 Round14 C2 同根（故障被当成功），修复必须两侧对称；2) Sscanf 宽松解析与 ParseInt 严格校验的语义分裂是"宽松入口+严格内部"典型，实测 `fmt.Sscanf("123abc","%d")` err=nil 才暴露；3) poller 死代码由 7 个测试驱动是"测试守护不可达分支"反模式——删除后测试改挂生产实现，覆盖不减；4) B3/C5 评估为有意遗留：B3 是 API 契约字段改名需联动 openapi/web（等契约窗口），C5 纯结构债可无限期拖。

## [2026-08-06 | Sophub 集成全链路梳理: 心智模型与已知缺口]
- **Events**：用户指出 fudankw.cn 是"需要用的 SOP 技能平台"，要求先弄清用法再判断文档；逐层核实 sophub 平台 API 文档(sophub.md)、backend-go infrastructure/sophub client、worker_sophub_proxy、runtime config 注入、worker sop_tool.py、原生 GA 根代码。
- **Changes**：无代码改动（纯调研记录）。确认: 原生 GA 根项目零 sophub 引用(黑盒无此能力)；平台集成是单路径——管理员经 `/v1/admin/sophub/binding` 绑定 key(密文存储) → runtime config 注入 `_platform_sophub` capability → worker 经 `/v1/worker/sophub/search|install` proxy 搜索/安装到工作区 `memory/sops/<remote-id>.md`(用户修改覆盖保护 + 字节上限 + capability 保护)，Runner 全程不持有 Sophub API Key。
- **Insights**：
  - **fudankw.cn 有两个用途勿混淆**：`fudankw.cn/sophub/` = SOP 技能平台(GA 运行时搜索/安装 SOP，平台已集成)；`fudankw.cn:9000/files/ga_install.*` = 自托管一键安装脚本分发(commit 4fbac39f "self-hosted install scripts"，脚本内部下载源是 `47.101.182.29:9000`，与文档域名不一致——观察项，属用户自托管配置)。
  - **sophub.md 的 Quick Start 是原生 GA 直连模式**(keychain 存 key、`/api/agents/register` 注册)——**对平台 Worker 不适用也不可达**，平台是 proxy 模式；两套心智模型并存易混淆，看文档时按"平台=proxy/GA 原生=直连"区分。
  - **平台侧只支持 single-file markdown SOP**(ServeInstall 强制 `single_file`+`approved`+markdown，bundle 403 `SOPHUB_NOT_PUBLIC`)；sophub 平台 2026-05-07 已上线多文件 bundle SOP。**有意的安全收窄(方案 §5.2)，非 bug**——但 bundle SOP 装不了是功能缺口，若要用需平台侧加支持。
  - **README 徽章链接 `https://fudankw.cn/sophub`(无尾斜杠)实测超时**，有效地址是 `https://fudankw.cn/sophub/`(带尾斜杠)；GA 原生 README 黑盒，改动需谨慎或反馈平台。
  - 平台 worker 内无 keychain/register/直连 sophub 残留(已 grep 确认)，不存在绕过 proxy 的第二路径。

## [2026-08-06 | 全项目健康清理(Round17): 范围收敛与执行]
- **Events**：用户对 Round16 后健康诊断(~40 项)要求"记录列计划逐个优化"；并行 5 域诊断发现后，用户两次修正范围：fudankw.cn 是需用的技能平台、GA 根项目是第三方黑盒一律不动。清理收敛到 tenant_platform 自有代码，10 任务全部 DONE 并提交 4 个 commit(e6fce4ad/0bba034d/07fdfe89/82e70e80)。
- **Changes**：backend-go 删 token_revoker.go 整文件(生产走 CapabilityStore.RevokeCapability)+12 死 store 方法+3 IsValid*+SessionConfigJSON/TransportConfigJSON+DeliveryPending/DeadLetter 常量+migrations 0031-0034 marker 残留+update-imports.ps1；worker/web 删旧 worker-python/Dockerfile+grace_seconds 死参数+legacy_import 空块+sop_tool document gateway 注释+web 4 无引用资源；文档校准(IMPLEMENTATION_SUMMARY Bearer→X-Platform-Admin-Token、bot-poller-auth /config 仅 POST、sandbox 方案 §1/§9 改已实施口径、8 份 superpowers 历史文档加废弃标注)；.tasks 进度真值刷新(12/15 DONE)。
- **Insights**：1) **GetRunnerLease 是"仅测试引用"但非死代码**——round10/13 关键不变量测试(release 清空容器字段/generation 语义)靠它断言，删前必须查测试是否用它守护回归；2) 死 store 方法删除后 import 清理是常见连锁(secret/token.go 的 subtle/binary、user_store 的 errors)；3) CSV 列索引(0-based)易错——status 是第 6 列(index 5)，Python csv 处理时先打印字段数核对；4) 用户范围决策(GA 黑盒/fudankw 保留)直接决定任务边界，诊断报告里的"P1"在用户语境下可能是有意设计(Round15 tools_schema_cn 保留决策同样如此)。

## [2026-08-07 | 微信绑定 404 排障: ILINK_BASE_URL 缺失导致路由不注册]
- **Events**：生产 console 管理端"获取微信二维码"报 404(POST /v1/admin/me/wechat-qrcode)；web 前端路径/契约/后端代码均正确，排查到 main.go 条件注册逻辑。
- **Changes**：compose.yaml platform 服务透传 ILINK_BASE_URL/ILINK_APP_ID/ILINK_CLIENT_VERSION；.env 配 `ILINK_BASE_URL=https://ilinkai.weixin.qq.com`(官方 iLink 网关，与 GA Core wxbot_client 一致)；仅 recreate platform 容器(env 变化触发，无需重建镜像)，接口恢复 201 返回真实二维码。
- **Insights**：1) **Go 1.22 ServeMux 未注册路由返回 404，方法不匹配才 405**——"接口 404 先查路由是否条件注册"，本类路由都挂在 `if s.xxxSvc != nil` 下，服务依赖 env 开关创建时极易整组静默消失；2) `--ilink-base-url requires BOT_TOKEN_KEY` 是显式安全护栏，BOT_TOKEN_KEY 已在 .env 满足；3) 微信绑定业务前提是**微信官方 iLink Bot 应用资格**(扫码登录的是 bot 微信号，非普通用户)，无应用时二维码请求会在 iLink 侧失败——这是业务准入，不是平台 bug。

## [2026-08-08 | MCP Gateway 架构重构: 从"stdio 支持"到统一架构收敛]
- **Events**：3b834bb 提交的 stdio transport 支持经审查发现提交级编译失败(缺 types.go/Server/CatalogSource/CatalogError/DefaultIdleTTL/DefaultCrashBackoff 定义)+三处生产链路缺陷(proxy 不转发 Mcp-Session-Id 致 gateway 第二跳 400；gateway 自造 JSON-RPC id 致第二个客户端错位；stdio URL 语义在快照层与 proxy resolve 层不一致)；用户要求"不要在意工程量，要最优架构"。在 docker 内实测编译 + 真实 DB + 真实子进程完成验证。
- **Changes**：新增 `internal/mcpgateway/types.go`(类型/常量)；重写 server.go/stdio.go——**gateway 会话无状态化**(删 sessions 表/校验/会话头，进程是唯一会话状态所有者，gateway 重启不破坏客户端)；JSON-RPC id 原样透传(initialize 缓存响应按客户端 id 改写)；**进程池** max_instances 生效(全忙且有余量才扩容)+指数退避+熔断(8 次/探活 30s)+revision 热更新；**子进程白名单环境**(PATH/HOME/TMPDIR，不继承 DATABASE_URL——原实现把 DB 凭据直接送给子进程)；readLoop 退出关 lines(进程死后请求立即失败而非等满超时)；proxy 转发白名单 +Mcp-Session-Id、双 http.Client(直连 30s/gateway 310s)；`domain.MCPServerGatewayURL` 单一合成(快照+resolve 共用，stdio url 强制空)；openapi 补 /v1/worker/mcp 路由缺口(既有)+MCP schema 新字段；web 表单 transport 切换；文档 §8 记录"http 迁 gateway 不做"(egress 约束矛盾)。测试: gateway 12 个(含多客户端 id 回归/进程池/熔断/热更新/环境隔离)、proxy 会话往返、domain 校验。
- **Insights**：
  1) **协议层代理必须透传客户端 id**——gateway 自造 id 只在"单客户端且计数恰好对齐"时碰巧能跑，第二个客户端必然错位；单测不断言响应 id 就发现不了。JSON-RPC id 是协议不变量，适配层只能透传。
  2) **"无会话"比"会话恢复"更优**——stdio 子进程内存里才有真正会话状态；gateway 模拟会话校验纯属多余状态，去掉后 gateway 重启/进程重建对客户端完全透明(真实 E2E 验证: 重启后同一客户端直接调用成功)。
  3) **锁序 g.mu→pool.mu 必须单向**——spawnLocked(持 pool.mu)内取内部 id 用 atomic 而非 g.mu，否则与 poolFor(g.mu→pool.mu) 死锁；race 测试抓住。
  4) **spawn 子进程必须显式白名单环境**——exec.Cmd 默认继承父进程环境，gateway 的 DATABASE_URL 会直接流进 stdio 工具进程，违背"子进程无凭据"基线；这是原提交的安全漏洞。
  5) **退避/熔断的计数语义要分开**：退避窗口内失败只推进计数不刷新窗口(否则请求风暴永久重置退避)；熔断期请求不得刷新探活时刻(否则永不探活)。
  6) **既有契约缺口**：/v1/worker/mcp/{server_id} 从未进 openapi 而 KNOWN_SPEC_GAPS 已清空——说明该测试在 Round14"补齐"后有回归盲区，新增内部路由同样必须进 spec。
  7) **mcp_servers.url 是 NOT NULL**——stdio 的"url 为空"存的是空字符串不是 NULL(domain 校验 TrimSpace)，migration 0049 未改列约束，INSERT NULL 会失败。
  8) **gofmt 是存量债**：cmd/platform/main.go 等大量文件本来就不 gofmt(CI 无门禁)，修文件时别顺手 gofmt -w 整文件(引入噪音 diff)。

## [2026-08-08 | 服务器部署: 重构后首次构建运行]
- **Events**：MCP Gateway 架构重构完成后按规范部署到本机服务器（4 vCPU/3.8G 内存/无 runsc）。
- **Changes**：`make build` 构建 7 镜像（含 mcp-gateway——重构前该镜像编译不过从未构建成功）；`.env` 基于 `.env.example.dev`（本机无 runsc，显式信任开关 ALLOW_RUNC=1 + 可变 tag）；`GA_RUNNER_MAX_ACTIVE=1`（内存 3.8G 偏小）；随机密钥全部生成；`docker compose up -d` 7 服务全 healthy；管理员 API 注册并启用 pandoc（stdio）server；gateway 白名单实时同步；容器内直连验证 initialize/tools/list/tools/call md→html 真实转换成功。
- **Insights**：1) mcp-gateway 无宿主端口映射是**架构正确形态**（仅 internal 网络 + platform proxy 转发），部署验证用 `docker exec` 容器内直连，不要为调试加端口映射；2) mcp-pandoc 工具参数名是 `contents`/`output_format`（schema 为准，勿凭印象传 format）；3) 本机内存 3.8G 跑 7 容器 + runner 偏紧，Runner 并发降到 1；4) Web 保持 127.0.0.1:8088，远程访问走用户自配隧道（cloudflared 等），勿暴露 8088/55432。

## [2026-08-08 | Sophub 绑定与搜索修复 + 双镜像 tag 部署坑]
- **Events**：用户提供 sophub Agent UID + API Key 要求配置；绑定成功后管理搜索返回空——实测 fudankw.cn/sophub 列表与详情 API 均不返回 is_public 字段，平台 filterInstallableSOPs 的 IsPublic=true 条件永假，搜索/安装链路全断。另发现服务器上存在旧 genericagent-*:local 镜像（用户此前构建），compose 一直引用它们，本次新构建的 platform:local 从未被使用。
- **Changes**：sophub 绑定 PUT /v1/admin/sophub/binding（agent_uid 远程校验回填，与用户提供一致）；domain.SophubRemoteSOP.IsPublic 改 *bool（区分"缺失"与"显式 false"）+ 新增 Source 字段；application 新增 isSophubSOPPublic（is_public 缺失时以 source=community 为公开信号，显式 false 仍拒绝）——搜索与 FetchRemoteSOP 准入共用；回归测试 TestSophubServiceSearchFiltersInstallableOnly；Makefile 镜像 tag 映射修复（platform→genericagent-platform:local，ga-runner 例外）——历史坑：按 Dockerfile 文件名打 tag 与 compose 引用名不一致导致构建产物不被使用；服务器镜像 retag + force-recreate 全部容器切新代码；清理 dangling 183MB。
- **Insights**：1) **sophub 远程 API 的"公开"信号是 source=community，不是 is_public**（字段从未存在）——平台此前准入条件永远不满足，绑定/搜索/安装全链路实际不可用，属既有契约 bug；2) **Makefile 打 tag 必须与 .env.example 的 GA_*_IMAGE 对齐**，否则 compose 静默用旧镜像——"构建成功 ≠ 部署成功"，部署后必须核对容器内二进制/行为；3) Go bool 无法区分 JSON 字段缺失与显式 false，需要三态语义时用 *bool；4) 本机无 make 命令（用户无免密 sudo），Makefile 语法用 golang 容器内 make -n 验证。

## [2026-08-08 | 微信绑定 404/504 回归修复: ILINK_BASE_URL 丢失 + iLink 状态长轮询]
- **Events**：生产 console 管理端"获取微信二维码"再报 404（POST /v1/admin/me/wechat-qrcode）；同日状态轮询报 504（35s 超时）。08-07 曾用 .env 配 ILINK_BASE_URL 修复过 404，08-08 重建时 .env 基于 .env.example.dev 重写把该变量丢了（两个模板当时都没有 ILINK 项）——部署回归。504 则是新发现的深层问题：iLink get_qrcode_status 是长轮询，QR 待扫时挂连接 ~30s 才回 wait；Go 客户端 20s 超时 × 3 次重试 ≈ 40s 超过 cloudflared 前置 ~35s 超时 → 504 → 前端 catch 后 stopPolling，绑定流程直接死。
- **Changes**：① 后端 admin.go 微信 QR 四路由改无条件注册，服务未配置（ILINK_BASE_URL 空）时返回 501 FEATURE_DISABLED（认证语义不变：外层 auth 先 401），不再裸 404；② ilink client 增加 `StatusRequestTimeout`（默认 8s）封顶每次 status 尝试，超时按"无变化"不重试（isRetryableStatusErr 对 DeadlineExceeded 返回 false）；③ wechat_binding_service.PollStatus 超时时返回 DB 最近状态 + nil error（200 wait），真实错误（4xx/ret!=0）仍 502；④ 前端 BindingPage 对 FEATURE_DISABLED 显示"微信绑定功能未启用"；⑤ 部署：live .env + .env.example.dev + .env.example 均补 ILINK_* 三项（模板缺口是回归根因），recreate platform 容器验证 201 建码 + 8.1s 内 200 wait；⑥ 回归测试：client 2 个（超时不重试/谓词）+ service 1 个（超时返回最近状态）。
- **Insights**：1) **"功能未启用"与"路径不存在"要区分**——条件注册的 404 无法表达前者，契约已声明的路由应无条件注册 + 结构化错误（501），前端才能给用户可理解的提示；2) **第三方长轮询接口必须按轮询频率设定尝试超时**——前端 3s 轮询根本不需要 30s 长轮询，超时即"无变化"，重试只会叠超时打爆前置代理；判断重试性要区分"瞬态网络错误"（重试）与"预期稳态超时"（不重试）；3) **部署模板缺项是隐藏回归源**——compose 透传了变量但两个 .env.example 都没列，任何"按模板重建 .env"都会静默丢功能，模板必须与 compose 引用项对齐；4) 验证长轮询假设的方法：直连 curl 计时（wait 态 31s vs expired 态 0.6s 即坐实）。

## [2026-08-08 | ga-runner 镜像命名漂移(WORKER_START_FAILED) + 架构收敛]
- **Events**：微信 bot 绑定成功后发"你好"，任务 WORKER_START_FAILED：ensure runner 409 RUNNER_ENSURE_FAILED，docker create 报 "Unable to find image 'ga-runner:local'"。根因：08-08 部署统一 genericagent-* 前缀只做了一半——镜像被 retag 成 genericagent-ga-runner:local，但 Makefile build 特例/runner-digest/compose 默认值/.env 的 GA_RUNNER_IMAGE 仍是旧名 ga-runner:local，sandbox-manager 启动时又不校验镜像存在，错误拖到任务运行时才暴露。用户明确要求区分架构问题与细节问题：这是配置真值源分散 + 缺启动自检的架构问题，按架构修复而非打补丁。
- **Changes**：① 命名统一 genericagent-* 前缀（Makefile build 删 ga-runner 特例、push/pull 补前缀、runner-digest 自动写 .env；compose.yaml 139/270 默认值 + main.go 默认值同步）；② compose.yaml ga-runner 服务 image 固定构建产物名（与运行时引用 GA_RUNNER_IMAGE 解耦——digest 模式 compose build 不再报 "build tag cannot contain a digest"，历史坑根治）；③ **sandbox-manager 启动 fail-fast 校验镜像**（ImageExists，缺失 exit 2 + 提示 make build && make runner-digest）；④ docker create 失败时诊断镜像（AvailableImages 列出本地可用镜像拼进错误，Platform 侧直接可操作）；⑤ .env 切 digest 引用 + .env.example* 同步；⑥ 端到端验证：模拟消息 → task succeeded → AI 回复真实生成并投递（messages 表 3 条闭环：inbound/回执/回复）。
- **Insights**：1) **"配置漂移"的根因是真值源分散**——同一实体的名字在构建脚本/compose 默认值/env/代码默认值四处各写一遍，任何一次"只改一处"的部署都会静默漂移；收敛方式：构建产物名固定为唯一真值（genericagent-*），运行时引用独立 env 控制且默认值与构建名一致；2) **fail-fast 自检放启动路径**：镜像/网络/密钥等外部依赖在进程启动时校验（exit 2 + 可操作提示），错误发生在"部署时"而非"任务运行时"，信噪比完全不同；3) **错误信息要可操作**：docker create 的原始错误没有配置上下文，附上"配置引用 + 本地可用列表 + 修复命令"才可指导排障；4) compose 的 image 字段同时承担"构建产物名"与"运行引用"两个职责，digest 模式下必然冲突——解耦后 compose build 恒产出固定 tag，运行时用 digest；5) 微信链路闭环验证方法：admin token 直接 POST /v1/router/messages（bot_uuid/ilink_user_id/message_id/text）模拟真实消息，查 tasks/messages 表即可端到端验证，不必等真实微信流量。

## [2026-08-08 | 微信链路三问题: 聚合乱序 bug / 回复 md 泄漏 / Pandoc 架构定位]
- **Events**：用户反馈: ① 发"文件+文字"回复两个(聚合配置 2.5s 看似没生效); ② 回复文本是 md 样式(星号/反引号); ③ 生成的 docx "没有真正排版", 问 Pandoc 是否没用、是否有配套 SOP。逐项排查: ① 聚合配置链路本身完整(写 DB + 热推 poller + 后台重试 + platform 启动下发 2500 均正确), 真 bug 在 `_can_coalesce` 要求 `current_at >= previous_at` 严格递增——微信"文件+文字"同时发送时 iLink 批次内顺序/时间戳可能乱序, 严格递增把同一发送动作拆成两个任务(复现测试 test_coalesces_reversed_batch_order FAIL→修后 PASS); ② worker 回复文本天然带 md 标记(GA 原生风格), 微信不渲染 → 平台 delivery 层新增 `cleanIMMarkdown` 全渠道降级(**加粗/反引号/删除线/链接/标题/表格分隔线→纯文本); ③ 会话记录显示 worker 调了 3 次 MCP pandoc 全失败(stdio server 在 gateway 容器, 与 runner 工作区文件系统隔离, contents 模式也无法落盘)——**架构定位: 文件型转换工具不该走网络 MCP, 应作为 runner 镜像本地能力**, ga-runner 新增 pandoc 2.17, 删除 mcp_servers 的 Pandoc 注册。
- **Changes**：poller `_can_coalesce` 改绝对间隔(`abs(current_at-previous_at) <= window_ms`) + 2 个乱序回归测试(22 tests 全过); delivery_service.go `cleanIMMarkdown` + 单测; ga-runner.Dockerfile apt 装 pandoc + 注释"勿恢复 MCP pandoc"; 删除 mcp_servers id=1(Pandoc stdio); 新增 `memory-template/wechat_delivery_sop.md`(微信交付排版 SOP: 纯文本铁律/pandoc 优先/python-docx 兜底/排版质量门槛/正反例) 并同步复制到现有工作区 memory/; 重建 ga-runner(新 digest b93bebf, make runner-digest 自动写 .env)+ platform + 重启, bot 恢复轮询。
- **Insights**：1) **"配置没生效"先验证配置链路本身**——热推/重试/启动下发都对了, 最后定位到执行层(合并谓词)的语义 bug: 窗口语义是"时间接近即合并", 实现却要求严格递增, 语义与实现漂移; 2) **文件型 MCP 工具的文件系统隔离是架构性不可用**——stdio server 容器与 runner 工作区隔离, 传路径不可达、contents 往返无法落盘; 文档转换应回归"镜像本地能力"(python-docx/pandoc 同层), 平台侧 MCP 只留给真正远程的服务(exa 等); 3) **IM 渠道的 md 降级应在平台 delivery 层做**——依赖 LLM 自觉(SOP)不可靠, 平台侧正则清洗是兜底(与 SOP 双保险); 4) "回复两个"还有一部分是**旧任务延迟完成**(LLM 生成 42KB docx 耗时 4 分钟)与新任务完成通知混叠——聚合修复后文件+文字合一, 混乱面缩小; 5) 排查聚合是否生效的实证路径: tasks.created_at ≈ 消息 create_time 说明消息被立即 flush(窗口=0/谓词不合并), 这是"未聚合"的可靠信号。

## [2026-08-08 | 审批后任务提交 500: 普通用户无 personal workspace + 测试清库事故]
- **Events**：用户注册 zhanglu 绑定微信 Bot，管理员审批后用户端仍显示"待批准"，再发消息无反应。DB 实查用户已 approved、bot active，poller 日志显示 `webhook post failed (500) submit task: workspace not found: "personal:1000"` 无限重试(60s 退避)——审批只更新 users 状态，从不创建 workspaces 行(bootstrap 用户由 EnsureAdminContext/EnsurePlatformAdminUser 建，普通用户注册→审批全链路无任何建 workspace 路径)，提交任务必 500。
- **Changes**：① `user_store.go` ApproveUser 事务内幂等创建 `personal:<uid>` workspace(ON CONFLICT (session_key) DO NOTHING)，volume_id=domain.WorkspaceDirHash(session_key)(满足 null_volume_requires_loopback 约束，与运行时共享卷哈希一致)；② 新增 migration 0050 为存量 approved 用户补数据(gen_random_uuid + sha256(session_key) hex，PG13+ 内置，无需 pgcrypto)；③ 端到端验证程序(注册→审批→workspace 行存在且哈希正确)；④ 前端"待批准"是 AuthContext 登录响应快照，重新登录即显示已批准(非 bug)。
- **Insights**：1) **"普通用户审批通过"是进入 approved 的唯一路径，生命周期不变量应挂在状态迁移事务里**——workspace 这类强依赖资源随状态迁移创建，而不是依赖调用方自觉；2) **迁移只做 DDL+marker 无业务行先例，但存量数据修复(migration 0050)模式可行**——INSERT...SELECT + marker，幂等；3) **前端状态快照(登录时签发)与 DB 实时状态会漂移**——审批类外部变更后需重新登录/刷新会话，文档化即可；4) **【事故】在共享开发库跑 postgres 包 go test 会清空全部业务数据**——测试 drop 全部 foundation 表重建(users/workspaces/tasks/bots 全清，marker 表保留故迁移不重跑)，测试残留 empty-pw-user；恢复=清理残留+重启 platform(EnsurePlatformAdminUser 自愈 admin+workspace)，但普通用户/bot token 密文(tokens NOT NULL)无法恢复，用户需重新注册+重新扫码绑定；教训: **后端测试必须连独立测试库(TEST_DATABASE_URL 指向独立实例)，或先备份再跑**；5) go test 包间 truncate 互踩是已知坑(AGENTS.md)，但"测试清空整个库"危害更大——postgres 包测试的 setup 是 drop 重建，不是 truncate 单表。

## [2026-08-08 | workspace 生命周期架构收敛: 注册即建 + 唯一实现 + 不变量测试]
- **Events**：用户反馈"若是架构设计问题不要打补丁"。审视上一条修复(ApproveUser 内手写 INSERT 建 workspace)确实是第三个散落的建行点(admin bootstrap / team×2 / approve), "approved 用户必有 workspace"是隐式约定, 且 invite 等未来"创建用户路径"仍会漏。收敛为架构规则: **users 行 ⇔ personal workspace 行**(与用户状态无关, 任务提交的 approved 门禁在 application 层)。
- **Changes**：① 新建 `postgres/workspace_store.go`——`insertPersonalWorkspaceTx` 成为 personal workspace 行的**唯一 SQL 实现**(含不变量契约注释), 另提供 `personalWorkspaceVolumeID` + `WorkspaceIDByKeyTx`; ② **注册即建**: CreateUser/CreateUserWithInvite 同事务建行(volume_id=WorkspaceDirHash), ApproveUser 回退纯状态迁移(移除上轮 INSERT); ③ admin bootstrap ensureContextUser 复用同一 helper; ④ 迁移 0050 语义改为**全量用户**补行(不只 approved——存量 pending 升级后审批才不缺行); ⑤ 新增 `user_workspace_lifecycle_test.go` 4 个不变量测试(注册建行/invite 建行/审批不重复建行且行不变/bootstrap 行语义); ⑥ 独立测试库 `genericagent_test`(教训落地: 测试不再指向业务库)。
- **Insights**：1) **资源行创建挂"用户创建"而非"状态迁移"**——任何创建用户的路径自动获得资源, 审批零副作用; 若挂在状态迁移, 每新增一条"变为 approved"的路径都要记得补, 必然漏; 2) **不变量与用户状态解耦**: workspace 行是账本(session_key 映射), 不是能力授予, 能力门禁在 application 层——这样不变量最简单(行存在即成立), 不必为 pending 用户特判; 3) **"唯一 SQL 实现 + 调用点注释引用"是收敛散落 SQL 的可行模式**: 4 处 INSERT 减为 1 处实现, 其余全走 helper; 4) 迁移语义"补存量"要与新不变量对齐: 只补 approved 会漏存量 pending(他们审批时新代码已不建行); 5) 验证节奏: 单测(隔离库) + 临时 main 程序(业务库, 随机前缀用户名, 测完删)双保险。

## [2026-08-08 | 文档工具链按官方能力重审: pandoc 3.8.3 + LibreOffice + 中文字体]
- **Events**：用户质疑"pandoc 官方能处理各种文档，为何你说不行，要看官方文档对比做决定"。查官方手册: pandoc 3.8 起官方新增 xlsx/pptx/asciidoc 输入(3.8.3 changelog 实证)，而镜像里 Debian apt 版 2.17 没有——原配 2.17 确实是旧版。PDF 输出官方明确需 pdf-engine(默认 pdflatex，可 weasyprint 等)。决定升级 pandoc 官方二进制 3.8.3 + 补 LibreOffice(老格式/高保真 PDF) + fonts-noto-cjk(中文渲染必需)。
- **Changes**：① ga-runner.Dockerfile: apt pandoc 2.17 换官方 3.8.3 tar.gz(GitHub releases, curl 下载解压 /usr/local)；apt 加 libreoffice-writer/calc + fonts-noto-cjk + curl；② 实测矩阵(全部容器内验证): pandoc 3.8.3 文本类互转正常；**xlsx 输入实测双 bug——openpyxl 生成的 xlsx(rels 绝对路径 Target=/xl/...)解析成 xl//xl/... 必失败，LibreOffice 生成的虽能读但中文乱码；官方最新 3.10.1 同样未修(下载实测)**；LibreOffice 7.4.7: docx/xlsx→pdf 中文完美、.doc/.xls 老格式读写转换正常、xlsx→csv 需 FilterOptions :44,34,76 指定 UTF-8(默认乱码)；**LibreOffice xlsx→docx 直接转换失败(0xc10, Calc→Writer 过滤不可靠, 业界共识)**；③ document_conversion_sop.md 重写: 工具矩阵按实测标注可靠/不可用场景 + 格式决策路线表 + LibreOffice 配方(UserInstallation 独立 profile 防锁冲突) + 9 条实测坑；④ 同步现有工作区 memory/ + 重建镜像(1.47GB)。
- **Insights**：1) **"官方宣传的能力"必须按实际部署版本验证**——apt 版 2.17 与官方 3.8 能力差一个 xlsx/pptx 输入，升级要查官方 changelog 而不是搜博客；2) **pandoc 3.8.3 与 3.10.1 的 xlsx reader 都是坏的**(openpyxl rels 绝对路径 bug 在 xlsx 解析生态是通病: QXlsx/CoreXLSX/PHPOffice 同款)——xlsx 处理一律 openpyxl/LibreOffice，SOP 明确"别浪费时间试 pandoc"；3) **PDF 生成的正确路线是 LibreOffice 渲染式**(docx→pdf 中文提取验证全对)而非 pandoc+LaTeX(重)或 weasyprint(未装)；4) **老格式 .doc/.xls 只有 LibreOffice 能读**——这是加它的核心理由，体积 600MB 换能力覆盖；5) 验证方法升级: 每个工具能力都"生成→转换→读回"闭环(openpyxl 生成 xlsx→pandoc 读→失败→LibreOffice 读→成功)，不轻信单点测试。

## [2026-08-10 | IM 多渠道架构调研: 群聊能力矩阵 + 对话单元分桶模型定案]
- **Events**：微信对接完成后评估接其他 IM 的架构准备度。梳理确认：GA 原生模型 = put_task(source) 多前端入口 + 单 agent 串行 + 全局 abort/history（"原生支持多前端提交，不支持多会话隔离"，会话秩序留给上层）；frontends 封装层存在两代风格债（qq/dingtalk/wecom 用 AgentChatMixin 继承式，wechatapp/tgapp 回调式/流式特化各自重写清理/命令/消费循环）。用户连续两轮澄清业务模型并纠正认知：① 隔离粒度是租户（workspace），不是 IM 也不是个人；团队=一个租户，多人共享；同租户跨渠道共享工作空间（"GA 原生单用户所有数据只有一份"）；② **微信 Bot 只能个人自用**（iLink 绑定用户自己微信号，仅私聊不能加群，不存在"好友A/B"多对话对象）——此前按"微信+好友"分桶是错误假设；③ 群聊渠道（飞书/QQ/企微/钉钉/TG）按群分会话，群内多人共享群会话；④ /new 清当前渠道（对话单元）桶。
- **Changes**：① 渠道能力矩阵代码实证（微信仅私聊单桶；QQ `group_openid`、钉钉 `conversation_type=="2"`→`group:{id}`、企微 `chatid`、TG、飞书可群聊）；② 业界对照调研（Coze 官方文档："多会话核心特性 = 文件共享，上下文隔离"；会话隔离层级 = 渠道+账号+Bot+session_name；群 = 独立对话单元；工作空间不分群/个人）——印证用户判断"群聊和个人工作空间在一起"；③ 数据模型定案：workspace 全共享 + 对话单元（channel:chat_id）分桶隔离 + /new 清当前桶；④ 落盘四处：新增 `tenant_platform/docs/IM_CHANNEL_ARCHITECTURE.zh-CN.md`（设计真值：模型/矩阵/落地路径/未决项）；`tools.md` 增"IM 渠道能力"章节（微信个人自用单桶等稳定事实）；`memory.md` 关键决策 + 最近活跃窗口；本月归档。
- **Insights**：1) **会话隔离粒度必须跟业务模型走**——"租户级共享、对话单元级隔离"是用户真实模型，业界（Coze）同款；通用做法是"文件共享+上下文隔离"，不按群/个人分工作空间；2) **微信 iLink Bot 是个人号自用模型**（1:1 绑定、无群、单对话单元），不能按客服机器人（多用户/多群）设计，桶 key 由 chat_id 决定即可天然单桶，无需特判；3) 平台侧架构已就位（workspace/Runner 容器级隔离 + ChannelBinding 渠道绑定通用模型），落地 = TaskEnvelope 加 channel/conversation_id + worker checkpoint 按桶存取 + router 透传，GA 核心零改动（memory.md Round17 黑盒约束兼容）；4) 封装层债务（IMAdapter 统一两代风格）与平台侧分桶模型是两条独立改进线，概念上 platform channel == 单机 source，勿混为一谈；5) 设计讨论中"表达抽象问题"（如"团队共享规则"）不如先拍板数据模型，模型定了问题自动消解。

## [2026-08-10 | IM 多渠道架构落地: 对话单元分桶 + /new 桶级化]
- **Events**：微信对接完成后按 IM_CHANNEL_ARCHITECTURE.zh-CN.md 定案落地分桶。三批提交: ① 契约 TaskEnvelope.conversation_id=14(复用 source 为渠道维度, worker 哑管道确认——恢复/提交选桶天然在 Go 控制面 CurrentRestorePoint/Prepare); ② migration 0051 + Go 全链路(恢复双路径: 空=存量 current_snapshot_id 指针, 非空=按桶取 MAX(generation) committed); ③ /new 桶级化 migration 0052(conversation_resets 表, workspaces.reset_at 退役)。期间用户纠正: frontends 单机封装层无使用价值(多租户项目), 任务 6 砍掉。
- **Changes**：① TaskEnvelope+conversation_id + 双端生成 + 契约测试 22 passed; ② migration 0051(ADD COLUMN IF NOT EXISTS + 自建 marker 幂等惯例——TestMCPHeaderCleanup 暴露并修复; migrationFiles() 补齐 0050/0051 既有列表债); ③ checkpoint_store Prepare 带桶键/CurrentWorkspaceSnapshot 双路径/CompleteSucceeded 仅默认桶推进指针(端到端测试抓出跨桶覆盖缺陷); ④ 0052 conversation_resets 按桶 upsert/取消/判定/消费(SubmitTask fresh 按桶、ResetWorkspaceForNewSession 按桶取消、WorkspaceIsFresh 按桶、CompleteSucceeded 按桶 DELETE); ⑤ 新测试: conversation_bucket_test.go(两桶隔离 6 不变量) + conversation_reset_test.go(桶级 /new 隔离/按桶取消/消费); ⑥ 全量 go test + race 5 关键包全绿(本机 ga-ci-pg 容器 5433/genericagent_test)。
- **Insights**：1) **"选桶"位置决定改动面**——先调研恢复/提交链路再设计: worker 是哑管道(给 ref 读 ref), 分桶全在控制面, worker 零改动; 2) **端到端测试抓真实缺陷**: CompleteSucceeded 无条件更新 workspace 指针在单桶时无害、多桶必串——按桶推进指针是分桶落地的最隐蔽坑; 3) **generation 是 workspace 级单调递增**, 桶内取 MAX 即最近恢复点, 不需要 per-bucket 指针表(fresh_session 已覆盖"清桶"语义); 4) **迁移必须幂等 + 自建 marker**(项目惯例), 否则 EnsureSchema 重放即炸; migrationFiles() 与 pendingMigrations 双列表都要同步, 0050 当年只加了一边是既有债; 5) 本机 Docker 有 ga-ci-pg(5433/genericagent_test) 可直接跑 DB 测试, 无需另起环境。

## [2026-08-10 | IM 渠道绑定（im-channel-binding epic）]

- **Events**：按 `tenant_platform/docs/IM_CHANNEL_BINDING.zh-CN.md`（用户已拍板、零未决）接入飞书/钉钉/QQ 渠道 + Web 微信绑定页改造为通用渠道绑定页；顺带清 memory.md B3 命名债（契约字段 ilink_user_id → channel_account_id）。6 个子任务全部 DONE。
- **Changes**：
  - migration 0053：`bots` RENAME `channel_configs`（FK 自动跟随）+ `channel_type` 列（存量微信默认 'wechat'）+ 唯一索引 (owner_id, channel_type) + `token_*`→`config_*`（凭据 JSON 密文）+ state 加 disabled；migrations.go 三个列表同步。
  - Go：`domain.Bot`→`ChannelConfig` 全库改名；新 API GET/PUT/DELETE `/v1/me/im-bindings` + admin 同款（OpenAPI 同步，test_route_contract 门禁）；Router 入站 Source=ChannelType、ConversationKey=ConversationID，微信身份校验仅限 wechat，新渠道属主直判；delivery 回复目标=conversation_id；/new /stop /status 按当前桶。
  - Poller：`poller_server.py` 重构为 BotAdapter 注册表（WeChatAdapter 迁移 + FeishuAdapter lark-oapi WS + DingTalkAdapter dingtalk-stream + QQAdapter botpy WS，SDK 惰性导入）；/start 契约 channel_type+config_json；配置热更新复用热推机制（PUT/DELETE → poller start/stop）。
  - Web：菜单"渠道绑定"；BindingPage 微信扫码卡片保留 + 飞书/钉钉/QQ 凭据表单卡片。
- **Insights**：
  - **0003 的 marker 就是 bots 表本身**：0053 RENAME 后 to_regclass('bots') 为空会让 EnsureSchema 把 0003 当作未应用而重放（重建空 bots 表）——0053 必须重建一个仅作 marker 的 stub `bots` 表（与 teams/platform_commands/users 早期 marker 表同惯例）。这是表改名类迁移最隐蔽的坑。
  - **migration 幂等模板**：RENAME 无 IF NOT EXISTS，用 DO 块条件执行（0052 先例）；列改名查 information_schema.columns；约束改名查 pg_constraint；marker 表 CREATE IF NOT EXISTS + INSERT ON CONFLICT。
  - **BotManager.start 持锁构造 adapter 会死锁**：WeChatAdapter 构造回调 coalesce_window_provider → 同一把非重入锁。构造必须移出锁区间（先检查注册表再构造再回写，并发重复 start 停掉后建者）。
  - **upsert 前捕获旧配置**：ON CONFLICT DO UPDATE 会覆盖 bot_uuid，事后无法再取旧 UUID 停止其 poller 会话（与微信 QR 重新绑定同模式）。
  - 新渠道身份模型：channel_configs 的 owner 即 canonical user（无需 ChannelBinding 二次绑定）；群消息触发是平台硬规则（钉钉/QQ 必须 @，飞书只申请 group_at_msg），adapter 只按平台规则订阅。
  - 验证发现 3 处与本次无关的存量失败：compose 六服务断言（mcp-gateway 服务加入后测试未同步）、integration 2 个（workspace 自动建行 vs 测试直插，c62db8ab 引入，base commit 可复现）、mcpgateway（Windows fakeserver）。

## [2026-08-10 | IM 流式输出（im-streaming-delivery epic）]

- **Events**：按 `tenant_platform/docs/IM_STREAMING_DELIVERY.zh-CN.md`（用户先调研 QQ 原生流式后定稿，要求实施前对照源码复核）实现 worker→IM 流式转发：按渠道分档（飞书=消息编辑打字机 / QQ 单聊=原生流式接口 / 钉钉微信=只发最终结果）+ 群聊收敛。7 个子任务全部 DONE；真实渠道冒烟待用户凭据。
- **Changes**：
  - migration 0054：`tasks.conversation_type('private'|'group')`（群聊收敛判定维度）+ `tasks.stream_final_at`（流式 commit 成功置位，delivery 据此跳过文本 part 文件照发）+ `platform_runtime_settings.text_value` 列 + `im_streaming_mode` 默认 streaming。
  - transport 新可选接口 `StreamingSender`/`StreamReply`（BeginReply/Append/Commit/Abort）：LoopbackTransport 实现（SentStreams 断言）；ILinkAdapter 生产实现（poller /send 扩展 stream_id+stream_action(open|append|commit|abort)，**不新增 HTTP 端点**）；非流渠道不实现（BeginReply 失败 → scheduler 回退终态 delivery）。
  - scheduler：`StreamForwarder`（stream_forwarder.go）= per-task 500ms 节流合并缓冲（streamBatcher，首条 chunk 为窗口起点）+ open/append/commit/abort 编排 + Enabled 判定矩阵（mode+source+conversation_type，fail-closed）；dispatch 非空 chunk→AppendText、心跳→FlushDue、SUCCEEDED→Commit+MarkTaskStreamFinal、defer Abort 兜底全部失败路径。
  - poller：FeishuAdapter 消息编辑（占位 CreateMessage→message_id→PUT UpdateMessage 全量替换，累积语义）+ _TokenBucket 5 QPS 节流；QQAdapter 原生流式（POST /v2/users/{openid}/messages + stream{state,id,index,reset} 帧序列 + msg_id 被动锚点 + _MAX_STREAM_APPENDS=2 被动回复 4 次/条保护）；BotAdapter 基类 send_stream_* 默认抛错（群聊/非流渠道拒绝）。
  - runtime settings：GET/PUT /v1/admin/settings/im-streaming（off|final_only|streaming）+ OpenAPI + Web SettingsPage 下拉。
- **Insights**：
  - **"流式"在 IM 语境 = 三种形态**（消息编辑/原生流式接口/限速分片），不是统一代码路径；飞书/QQ 编辑或流式接口都是**全量替换语义**（append 发送累积文本，非增量）——adapter 内部必须累积。
  - **QQ 原生流式参数**：官方文档无专门页面；实证来源 linux.do 2026-03 实测 + easybot SDK（stream.state 1=生成中/10=结束、id 首条 null 后续用返回 id、reset 终帧全量替换、被动回复 msg_id+msg_seq 递增去重）；index 递增语义有歧义需真实凭据实测。QQ 群聊官方明确无流式参数。
  - **QQ 官方契约修正（用户指出应以官方文档为准）**：最终以腾讯第一方实证为准——@tencent-connect/qqbot-nodejs 1.0.4 源码：`POST /v2/users/{openid}/stream_messages` 独立端点 + 扁平字段（input_mode/input_state/content_type/content_raw/event_id/msg_id/stream_msg_id/msg_seq/index），**同一流 msg_seq 共享仅 index 递增**，msg_id 必填（无锚点拒绝），限流 429/50002 指数退避；初版按 linux.do 的嵌套 stream{state,id,index,reset} + msg_seq 递增实现已重写对齐官方。教训：QQ 官方文档索引页不提流式细节，但腾讯官方开源 SDK（openclaw-qqbot/qqbot-nodejs）是权威实现，优先于第三方博客。
  - **成功路径"流式消息即最终交付"**与 outbox delivery 机制冲突的解法：stream_final_at 标记 + delivery 文本 part 跳过（文件照发），失败路径无标记照发兜底——不需要动 delivery 状态机。
  - **scheduler 事件循环无定时器**：500ms 节流 flush 由"下一 chunk 到达/心跳空 chunk/Terminal"驱动，低频 chunk 场景靠心跳 FlushDue 防滞留。
  - **节流窗口起点 bug**：lastFlush 零值时 now.Sub(zero) 恒 ≥ 窗口，第二条 chunk 就误触发 flush——首条 chunk 到达时刻作为窗口起点。
  - test 踩坑：botpy 无 `botpy.route` 模块（Route 在 `botpy.http`）；`run_coroutine_threadsafe` 需要 loop 在后台线程运行（fake 测试用 _LoopRunner）；`Route.path` 是未格式化模板。

## [2026-08-10 | IM 流式官方对齐复审（用户批评后两轮修正）]

- **Events**：用户指出实现依据应看官方文档——第一轮按 linux.do 博客（嵌套 stream{} 参数、/messages 端点、msg_seq 递增）实现被否；第二轮追到腾讯第一方 @tencent-connect/qqbot-nodejs 1.0.4 源码修正为官方契约（独立端点 /v2/users/{openid}/stream_messages + 扁平字段 + msg_seq 同流共享仅 index 递增 + msg_id 必填）；第三轮（本次）逐项对照官方 SDK 再审查，又发现 3 处偏差并修正。
- **Changes**：
  - 飞书 20 次编辑上限保护（官方文档明示）：_MAX_STREAM_EDITS=18，超限停止 PUT 累积到 commit 一次送达。
  - QQ 限流重试（官方 sendWithRetry 同构）：429/50002 指数退避 1s/2s 重试 2 次，重试帧 index 递增；append cap 从 2 放宽为 60（官方无帧数限制，业务上限防超长任务刷单关系 20 QPM）。
  - 全部单测更新 + 新增（编辑上限/限流重试/cap 模拟）。
- **Insights**：
  - **腾讯官方 SDK 是 QQ 能力最权威实现**：官方文档索引页不写流式细节（只有"发送单聊消息/流式消息"一句），但 openclaw-qqbot（@tencent-connect 官方）的 qqbot-nodejs 里有完整 StreamSession 实现——npm pack 下载源码逐行对照是正解，优先级高于第三方博客/逆向。
  - **飞书消息编辑有硬上限（20 次/条）**：打字机实现必须 adapter 侧计数截断，不能依赖 platform 节流——这是设计文档渠道矩阵没写明的官方限制。
  - **审查方法论**：实现后必须逐字段对照官方 SDK 源码（端点/payload 形状/msg_seq 语义/重试行为/边界限制），不能只看参数名是否相似；限流重试、编辑次数上限这类行为约束最容易漏。
  - 环境坑：Windows Git Bash heredoc 写含中文/转义 Python 脚本反复踩坑（
 字面 vs 真实换行、CRLF、三引号嵌套）——长脚本一律用 write 工具落盘再执行，不经 shell 传输。
## [2026-08-11 | 企微(wecom)渠道落地 + 审查 C1 教训]

- **Events**：用户确认"再加一个企业微信渠道"——渠道模块化假设成立（每渠道 = Go 枚举 + poller 适配器 + Web 卡片，DB 唯一键 (owner_id, channel_type)）。实现全链路后按用户要求走独立审查子代理，抓到 1 个 Critical（自己验证矩阵全绿仍漏）。
- **Changes**：
  - Go：ChannelWecom/SourceWecom 枚举+白名单；`channelTypeForTaskSource` 加 wecom 分支（C1 修复，RED→GREEN + delivery 路由测试）；StreamForwarder Enabled 矩阵 +wecom（单聊流式/群聊收敛）。
  - poller：WeComAdapter（wecom_aibot_sdk WebSocket，线程内自管 asyncio loop + `run_coroutine_threadsafe` 同步桥）；入站 frame→webhook body（chatid=对话单元，单聊 chatid==userid 判 private，空 sender 保守归群——M3）；出站 SEND_MSG markdown 承载纯文本（SDK SendMsgBody 无 text 类型，被动 reply_stream 依赖入站 req_id 不适合异步 delivery）；流式 SEND_MSG+stream 帧（首帧 '…' 占位 M4、open 失败清理流条目 M5）；BotManager 工厂接线。
  - Web：CREDENTIAL_CHANNELS +企微卡片（Bot ID/Bot Secret 标签、secretLabel 可选泛化、卡片 map 渲染消硬编码索引）；前端必填校验文案随渠道标签（M2）。
  - OpenAPI 枚举 6 处 + IM_CHANNEL_BINDING/IM_STREAMING_DELIVERY 文档同步（企微行设计矩阵早有，落实为已实现）；migration 无需新增（复用 app_id/app_secret 槽位）。
  - 测试：poller 10 个 wecom 用例（入站映射/出站帧/流式状态机/未知流幂等/工厂/空 sender 防御）+ Go TDD（API 保存/凭据校验/流式矩阵/delivery 路由）；全量 52 passed + contract 23 + web lint/build 绿。
- **Insights**：
  - **新渠道接入的完整同步清单**（缺一即错）：Go `IsValidChannelType`/`IsValidSource`/`channelTypeForTaskSource`（回复路由键，最易漏！）/`StreamForwarder.Enabled` 矩阵 + poller `VALID_CHANNEL_TYPES`/`_build_adapter` + OpenAPI 枚举 + Web `ChannelType`/卡片。**测试全绿≠覆盖全**：流式矩阵测试只测 Enabled()，不测 openReply/delivery 解析路径——fake bot resolver 按 channel_type 匹配真实 store 语义后 C1 才被测到（之前 fake 忽略渠道参数）。
  - **企微智能机器人凭据 = bot_id/secret**（非 app_id/app_secret）；SDK `send_message` 主动发送只支持 markdown/template_card/media（text 仅被动回复）；`reply_stream` 是被动能力（透传入站 req_id），异步 delivery 必须走 SEND_MSG。
  - **渠道接入形态先看项目内先例**：root `frontends/wecomapp.py` 已在用 wecom_aibot_sdk——新渠道优先复用既有 SDK/模式，不做第二套。
  - **审查子代理价值实证**：独立 reviewer 抓到自己全程 TDD + 验证全绿仍漏的路由分支（该分支无测试覆盖）；fake 增强（按 channel_type 匹配）是让测试能捕获此类缺陷的关键。
  - 残余风险（已文档化）：企微 SEND_MSG 主动流式帧未实测——若服务端静默丢弃用户只见首帧；上线前真实凭据冒烟，未通过前 im_streaming_mode 保持 off/final_only。

## [2026-08-12 | 推送审查：装配时序/死代码/配额语义修复]

- **Events**：用户推送 156 文件（MCP 治理 + IM 渠道/流式 + 分桶）后要求审查并发/逻辑/语义与新旧混乱；独立审查发现 2 个功能未接线 + 3 个语义缺陷；确认后全部修复并补回归测试。
- **Changes**：
  - B1 `cmd/platform/main.go`：transport 装配块（botTransport/botLifecycle/botPollerClient）整体前移至 `NewScheduler` 之前——Streaming 端口构造时立即断言 botTransport（值语义），先前声明后赋值导致断言恒 nil，IM 流式全链路静默失效；顺带消除 channelSvc.Start 闭包的 nil 窗口。
  - B2 `worker_credential.go`：`filterMCPServersByQuota` 接入 `issueInitialWorkerCredentials`（resolveMCPSnapshot 之后、签发之前）——此前仅有定义与单测，生产路径零调用，"调度过滤耗尽 server"设计未生效。
  - Y1 `worker_mcp_proxy.go`：配额扣减后移到 resolve 白名单校验之后——404/未启用 server 不再烧用户配额（原实现在 resolve 前扣减）。
  - Y2 `mcp_server_store.go`：`ConsumeMCPQuotas` 改单事务双周期（day→month 固定 FOR UPDATE 锁序）——原实现先扣 day 再查 month，被拒调用烧 day 计数。
  - Y3 `api/mcp_server.go`：`mergeMaskedHeaders` 掩码不匹配/新键提交掩码 → 400 拒绝——原实现把不匹配掩码字符串原样落库成为真实 header。
  - 回归测试 5 个：签发路径过滤（B2）、404 不烧配额（Y1）、被拒不烧 day + 双周期并发恰 limit（Y2）、陈旧掩码/新键掩码拒绝（Y3）。
- **Insights**：
  - **测试全绿 ≠ 功能生效**：B1/B2 都是"有实现、有单测、装配/调用链断掉"型缺陷，单测直接构造对象绕过了装配层——cmd 级装配时序需要断言（或至少注释硬约束）。
  - **Go 按值求参，非延迟引用**："先声明后赋值再注入"对立即求值的构造参数无效；闭包（channelSvc.Start 捕获变量）才是延迟引用。
  - **死代码检测**：写完带单测的函数后 `rg "函数名" --glob '!*_test.go'` 确认生产调用点存在。
  - 配额扣减要"先校验后扣费"：白名单校验（resolve）应先于计量（quota），404/502 类调用不应消耗周期性稀缺配额；多周期组合扣减必须单事务（部分扣减副作用会让被拒调用逐步烧尽另一周期）。
  - 掩码类"编辑保留"契约要处理陈旧回显：掩码不匹配 = 数据竞争/伪造信号，拒绝（400）而非落库。
  - gofmt 1.26 与仓库既有格式（旧版 gofmt）有对齐差异：只格式自己改的文件，且纯对齐噪音手动还原，保持 diff 聚焦（CI 不跑 gofmt）。

## [2026-08-12 | proxy 计量原则：JTI 预算只在调用发起时扣(Y5)]

- **Events**：上一轮审查后用户确认 JTI 预算先于 resolve 消费是设计问题（与 Y1 同类：拒绝路径烧稀缺计量），要求对称修复后再提交。
- **Changes**：
  - `worker_mcp_proxy.go` / `worker_sophub_proxy.go`：`authenticate`（校验+计量合一）拆为 `validateToken`（Bearer 解析+签名/在线校验+audience/operation）与 `consumeBudget`（ParseBudgetMaxTurns + JTI 原子消费）。
  - MCP `ServeProxy` 顺序：validate → serverID 400 → resolve 404/503 → 用户配额 429/503 → JTI 消费 403/429/503 → 转发。404 与配额 429 不再烧 JTI。
  - Sophub `ServeSearch`/`ServeInstall`：参数校验（q/id 为空 400）在 JTI 消费之前；fetch 后 403 判定视为已发起仍消费。
  - 回归测试 3 个：MCP 404 断言 quota+consume 均零调用；MCP 配额 429 断言 consume 零调用；Sophub 参数错误 400 断言 consume 零调用（经 handler 路由分发，覆盖 search/install 两路径）。
- **Insights**：
  - **计量语义统一原则**：预算只在"调用即将发起"时扣——客户端错误（401/400）、白名单拒绝（404）、配额拒绝（429）、系统故障（503）不计量；上游调用失败（502）与 fetch 后内容判定（403）视为已发起，计量。本次修复统一 mcp/sophub 两 proxy；llm-proxy 为既有计量模式（validate→consume→解析，model mismatch 409/body 413 也烧预算——无白名单/配额概念、拒绝路径罕见，未纳入本次范围）。
  - 拆分后 `writeMCPAuthFailure`/`writeAuthFailure` 复用（consumeBudget 返回状态码，错误响应函数不变），改动面小且对称。
  - 顺序约束：用户配额（周期性稀缺）先于 JTI（单任务短期）——两者都成功才转发，任一拒绝都不烧另一项。

## [2026-08-12 | 推送前整体设计审查(二轮)：两阶段配额 + 事务防御回滚]

- **Events**：Y5 提交后按用户要求做推送前整体设计审查，交叉验证计量顺序与事务语义，发现 2 处自我引入的缺陷并修复。
- **Changes**：
  - **proxy 配额两阶段化（Y5 二轮）**：Y5 新顺序（quota 扣减先于 JTI 消费）在 JTI 429/403/503 时产生配额白扣——正是 Y2 修复的同类缺陷在 proxy 层复发。`NewWorkerMCPProxy` 增 `quotaCheck`（MCPQuotaAvailable 只读预检，先于 JTI 消费）+ `quotaConsume`（ConsumeMCPQuotas 条件扣减，在 JTI 消费之后）：任一拒绝路径（404/配额预检 429/JTI 429/503）零扣减；唯一残余为极端竞态（预检通过→JTI 扣→配额扣减时被并发耗尽）下 JTI 白扣一次（短期预算可接受，用户配额从不错扣）。main.go 装配拆 quotaOwner/quotaCheck/quotaConsume 三闭包；测试 8 处调用点同步，新增"预检通过但 JTI 耗尽 → 配额扣减零调用"回归。
  - **ConsumeMCPQuotas 防御分支语义修正**：扣减循环内 ErrNoRows 防御分支原 `return nil`——withTx 语义下 nil=Commit，若触发则已扣周期生效（部分扣减）。改 `return error` 触发回滚（当前理论不可达，但防御代码语义必须正确，防未来 DELETE usage 路径埋雷）。
  - 归档修正：llm-proxy 为既有计量模式（非"三处一致"），未纳入本次范围。
- **Insights**：
  - **"先校验后扣费"必须两阶段**：条件扣减原语（ConsumeCapabilityCall/ConsumeMCPQuotas）互相之间仍有顺序问题——A 扣成功、B 拒绝 = A 白扣。正确模式：所有只读预检先行（不扣），条件扣减按序执行（各自"成功才扣"），唯一残余竞态窗口内的白扣选**影响小的一方**（短期 JTI vs 周期性用户配额→选配额不错扣）。
  - **防御分支语义必须与事务协议一致**：`withTx` 里 `return nil`=提交、`return error`=回滚——防御性 return 用错会把"整体拒绝"变成"部分提交"。
  - 自我引入缺陷的检测方法：对修复做"反向推理"——Y5 移动了扣减顺序后，问"现在哪条路径会扣了不成功/成功了被拒"。

## [2026-08-12 | 三轮审查：B2 配额属主分裂 + 防御哨兵错误码]

- **Events**：二轮提交后用户提示"引入了新问题"，重新追溯配额语义发现 B2 接入的调度过滤按 RequesterID、proxy 强制按 workspace owner——团队 workspace(team:<id>)下 requester ≠ owner，两处配额属主分裂。
- **Changes**：
  - **配额属主统一为 workspace owner**：`filterMCPServersByQuota` 签名改为 (sessionKey, snapshot)，内部 `GetWorkspaceOwner(sessionKey)` 解析 owner（与 proxy 强制路径完全同源）；`MCPServerSource` 接口补 `GetWorkspaceOwner`（postgres.Store 已有实现）；`issueProviderCapabilitiesWithRuntime` 删除从未使用的死参数 ownerKey。个人 workspace(requester==owner) 无影响；团队任务此前按 requester 过滤（B 耗尽则 server 不可见）却按 owner 扣减（烧 A 配额）——分裂消除。新增 `TestFilterMCPServersByQuotaUsesWorkspaceOwner`（team/personal 双路径回归）。
  - **防御分支哨兵错误码**：ConsumeMCPQuotas 扣减循环防御分支先 return nil（提交→部分扣减）、后 return error（回滚但漂移为 503）——现改 `errQuotaExhaustedInTxn` 哨兵：触发回滚（零副作用）+ 外层映射 (false, nil) = 429 配额耗尽语义。
  - 新增 `TestConsumeMCPQuotasHighContention`（100 并发 × 3 轮，无死锁、每轮恰 limit 成功）。
- **Insights**：
  - **"每用户配额"的属主必须先定义清楚**：capability JWT 只有 sessionKey（workspace 级），proxy 只能按 workspace owner 解析属主——调度过滤必须与强制路径同源解析，任何"按 requester"的捷径都会在团队场景分裂（requester 配额白设、owner 配额被队友消耗）。
  - **防御分支的错误码语义**：withTx 协议下"回滚 + 特定错误语义"需要哨兵 error 显式映射，不能靠返回普通 error（503 系统故障）或 nil（提交）——三种语义（提交/回滚+429/回滚+503）要显式区分。
  - 压力测试价值实证：100 并发验证事务锁序无死锁、配额精确性（恰 limit 成功）。

## [2026-08-12 | stdio MCP 恢复(Worker 进程宿主) + 并发限额调整]

- **Events**：用户反馈 web MCP 页示例与主流 mcp.json(Claude 风格, type/command/args + directTools)不一致, 且认为平台还支持 stdio——实际 stdio 曾于 D5(2026-08-11)被移除, 但 API/DB/openapi 残留 transport/command/args 字段造成"假支持"误导。决定恢复 stdio(主流格式兼容), 并同步把用户/全局并发限额调到 2/5。
- **Changes**：
  - **stdio 恢复, 架构改为 Worker 沙箱内进程宿主**: 后端 domain 校验放开(拒绝 shell 解释器 sh/bash/zsh 等, 命令模式 `^[A-Za-z0-9_./+\-]{1,256}$`, args ≤64×256B); snapshot 签发不再跳过 stdio 行, `_platform_mcp.servers` 携带 transport/command/args; worker 新增 `MCPStdioClient`(subprocess + 新行分隔 JSON-RPC, 2024-11-05 协议, 处理 server 通知/请求噪音、deadline 封口、stderr 排空诊断), `mcp_config.py`/`mcp_runtime.py` 按 transport 分发; web 编辑器加 transport/command/args + 两种 header 鉴权示例; openapi transport enum [http, stdio]。
  - **stdio 调用不经过 Platform proxy → 不参与按调用配额计量**(文档化); 配额仍按快照签发时 MCPQuotaAvailable 门控(用尽则 server 不进目录)。
  - **并发限额**: `.env`/`.env.example*`/`compose.yaml`: PER_REQUESTER_RUNNING_LIMIT=1→2, 新增 MAX_RUNNING_TASKS=5(此前 compose 未透传=关闭)。
- **Insights**：
  - **退役功能要在所有契约面同步清干净**, 否则残留字段(openapi/API 响应/DB 列)会让用户以为"还支持"——要么删净, 要么恢复, 不能留假皮。
  - 主流 mcp.json 是本地客户端格式; 多租户平台必须加 server_key(配额/路由引用键)、timeout、密钥托管——"mcp.json 风格"是子集+扩展, 文档别吹。
  - MCPStdioClient spawn 在构造期, 必须与 initialize 一样包成 MCPRuntimeError, 否则 session_lifecycle 的捕获列表接不住直接崩会话启动(测试补了 spawn 失败路径)。

## [2026-08-12 | stdio MCP 设计定案修订: 撤销 registry 白名单, runner 直接出网(主流模型)]

- **Events**：用户连续两轮质疑 stdio 恢复方案的过度设计——"你都用 API 了还用得着防吗？大家不都是本地有网环境跑 agent？"逐条对照后确认: D1 可信部署下, runner internal-only(审查 C2)与 registry 白名单代理(EPIC D10 原案)均偏保守; 且 C2 防的"任意目的地出网"在可信部署 + 主流模型下不被项目采纳。
- **Changes**：
  - **runner 出网**: compose `runner-control` 去掉 `internal: true`(该网络仅 platform/llm-proxy/runner, 后两者本就可出网, 实际影响面只有 runner); llm-egress 保持 llm-proxy 专用不动; profile.go 零改动(默认网络名不变)。
  - **共享缓存卷**(D10 保留要素): 新增 `GA_PKG_CACHE_VOLUME` → sandbox-manager `--pkg-cache-volume` → DockerCLI 创建 runner 时挂 `/var/cache/ga-pkg` + 注入 NPM_CONFIG_CACHE/UV_CACHE_DIR/PIP_CACHE_DIR, 全租户复用一份 npx/uv/pip 缓存。
  - **删过度防御**: domain 删除 shell 解释器黑名单(mcpShellInterpreters/isMCPShellInterpreter), 保留格式校验(非空/模式/长度, 防手误无副作用); 相关测试改断言"sh/bash 等合法可配"。
  - **注释同步**: worker_mcp_proxy/token/runtime_config/worker_credential/scheduler/mcp_config 中"Runner 仅 internal 网络, 无公网出口"全部改为"key 平台侧持有 + 配额计量"——HTTP MCP 走 proxy 的理由不再是无网, 而是 key 托管与配额(架构不变, 理由更新)。
  - **文档**: EPIC.md D10 修订为落地版(废弃 registry 白名单/受控代理, 注明原因)。
- **Insights**：
  - **先对方案后动代码**: 本轮教训——用户 8-11 已确认 D10("npx/uvx 共享缓存卷 + 版本固定 + registry 白名单 + reaper"), 我 8-12 直接实现了"容器内 spawn + 黑名单校验"却没先读 EPIC 记录, 造成返工。EPIC/SUBTASKS 是决策真值, 动手前必读。
  - **"防数据外泄"的语义**: 防的不是"出网"(LLM 调用本就出网), 是"任意目的地出网"(prompt injection 外发)。公开 SaaS 必防, 可信部署(D1)下用户有权选择主流模型——项目所有者拍板为准, 不要用大厂标准代答。
  - **网络是容器级的**: "只让 stdio 拉包、不让 agent 出网"需要进程级 proxy env 方案(复杂度高); 用户接受主流模型后, 容器级出网是最简解, 且 key 托管(LLM/HTTP MCP 仍走 proxy)不受影响——平台价值不在网络隔离, 在凭据与配额。

## [2026-08-12 | stdio 恢复真机验证 + 生产部署: xz-utils / UV_TOOL_DIR / 网络重建流程]

- **Events**：用户质疑"残余风险需真实 Linux 主机验证"——本机就是真实 Linux + 生产 compose，直接在真机验证。发现 2 个本地/容器环境才暴露的缺陷 + 1 个部署流程坑：①python:3.11-slim 无 xz，node 官方仅 .tar.xz，构建 tar: Cannot exec xz 失败；②runner 容器 read_only + 无 HOME，uvx 默认装工具到 ~/.local/share/uv 直接 Permission denied（npx 的 NPM_CONFIG_CACHE 与 uv 缓存都正常自动建目录，唯独 uvx 工具目录炸）；③compose.yaml runner-control 去 internal 后 `compose up -d` 尝试删旧网重建失败（活跃端点），platform 容器被拔（Exited 0）新容器未建。
- **Changes**：
  - ga-runner.Dockerfile: node 层先 `apt-get install xz-utils` 再解压（真实构建验证通过）。
  - docker_cli.go: 注入 `UV_TOOL_DIR=<卷>/uv-tools`（与 NPM_CONFIG_CACHE/UV_CACHE_DIR/PIP_CACHE_DIR 同批；工具安装全租户复用）。
  - 真机验证（genericagent-ga-runner:local 重建后 + 临时卷）：node 20.20.2/npm/npx/uv 0.12.3/uvx 全可用；命名卷首挂属主=镜像目录属主（ga-runner 10002）；npx -y 拉包 6MB 缓存 ✓、uvx 装工具+pycowsay 输出 ✓、pip 缓存 ✓。
  - 生产部署：make build 全量（备份 :local.bak-stdio）；先 stop llm-proxy/sandbox-manager → docker network rm genericagent_runner-control → compose up -d（新网络 internal=false）；滚动重启 5 服务全健康；预建生产卷 genericagent_ga_pkg_cache（属主 10002 验证）。
- **Insights**：
  - **"slim 基础镜像"的隐藏缺口是压缩工具**：node 官方只有 .tar.xz，slim 无 xz；固定版本 + sha256 校验的下载链在构建期才暴露——真机 docker build 是 CI 等价验证，模拟/单测覆盖不到。
  - **read_only + 无 HOME 的容器里，工具类 CLI 的"默认用户目录"全是坑**：uvx 工具目录(~/.local/share/uv)与缓存目录(UV_CACHE_DIR)是两个独立维度，只指缓存不指工具目录照样炸；凡是"装到用户目录"的 CLI 都要显式指到可写卷。
  - **compose 网络定义变化 ≠ 无缝重建**：internal 标志翻转会让 compose 尝试删旧网重建；有活跃端点时删除失败且已停的容器不会自动恢复（platform 被拔）。升级含网络变更的 compose 必须先停挂网服务再删网再 up，不能直接 up 期待 compose 自己处理。

## [2026-08-12 | stdio 恢复推送审查: proxy 只服务 http + store command 写回 + 3 处 CI 断言同步]

- **Events**：stdio 恢复变更推送前审查, 发现 3 个真问题 + 2 处小修: ①worker mcp_config 把 proxy(base_url/capability_token)无条件应用到所有 server, 而 MCPServerConfig.validate() 对 stdio 显式拒绝 proxy_base_url——compose 部署(MCPProxyBaseURL 非空)下配置任意 stdio server 即快照加载失败, StartSession 直接挂(混布与纯 stdio 都中招); ②store CreateMCPServer/UpdateMCPServer 的 command 参数仍是 stdio 移除时遗留的硬编码 nil, 真库 CHECK 约束 mcp_servers_transport_fields(stdio 必填 command)拒绝插入; ③security 测试两处断言未同步: runner-control internal is True(已去 internal)+ 卷集合缺 ga_pkg_cache。
- **Changes**：
  - mcp_config.py: proxy 字段仅对 transport=="http" 的 server 生效(use_proxy 分支), stdio 恒空; MCPServerConfig.validate() 加 url/command 类型守卫(None 不再 AttributeError 逃出 MCPConfigError 包装); 回归测试 2 个(混布+proxy、纯 stdio+proxy)。
  - mcp_server_store.go: command 参数 nil → nullString(input.Command)(domain 校验保证 http 恒空→NULL)。
  - cmd/platform main.go proxy resolve: stdio server 视为白名单外(404), 不产生空 URL 目标。
  - test_container_deployment_bundle.py: runner-control internal 断言翻转为 is not True(2026-08-12 修订语义)+ 卷集合加 ga_pkg_cache(命名卷断言分支泛化)。
  - ga-runner.Dockerfile: 预建 /var/cache/ga-pkg + chown 10002(新命名卷首挂以镜像目录含属主初始化, 否则卷根 root 写不进); mcp_runtime.py tab 缩进改空格。
- **Insights**：
  - **"恢复已退役功能"要 grep 全部契约面**: domain 校验放开 ≠ 功能恢复——store 写路径(nil 硬编码)、CHECK 约束、proxy 白名单、worker 快照消费(proxy 全局应用)、安全测试断言, 每一层都可能残留"退役时的手尾", 单靠 domain 测试抓不住。真库约束(23514)是最后一道防线。
  - **全局配置字段应用到子实体时必须有 transport 维度**: proxy 是快照级配置, 但只对 http 语义成立——"全局应用"在混布场景变成隐式污染, 校验器兜底拦住了但以整体失败为代价; 消费侧按子实体语义裁剪才是正解。

## [2026-08-12 | ga-runner 镜像补 stdio 运行时: node 20 LTS + uv]

- **Events**：用户确认 stdio 支持要覆盖 npx/uvx 按需拉包型——"支持 stdio 不都支持这些？" 代码层 command/args 直通本就全支持, 真正的 gap 是 ga-runner 镜像(python:3.11-slim)没有 node/npm 和 uv。
- **Changes**：ga-runner.Dockerfile 加 node v20.20.2(官方二进制, LTS)+ uv 0.12.3, 固定版本 + sha256 校验(与 pandoc 同款风格); 本地模拟验证 node/npm/npx/uv/uvx 全可用, 且 sandbox-manager 注入的 NPM_CONFIG_CACHE/UV_CACHE_DIR 被 npx/uv 正确识别(uv cache dir → /var/cache/ga-pkg/uv)。EPIC D10 措辞去掉"版本固定"硬要求(管理员 args 自控版本)。
- **Insights**：stdio 支持 = exec 直通, 预装/npx -y/uvx/绝对路径全部天然支持; 镜像提供运行时是唯一前提——加运行时是部署事实, 不用反复确认, 直接加。

## [2026-08-12 | 生产 IM 渠道首次真实连通: bot-poller 镜像缺全部渠道 SDK + 3 处 SDK API 兼容 bug + QQ IP 白名单]

- **Events**：用户反馈"配置了飞书，QQ 没反应"。查证链条：channel_configs 表 feishu/qq 均 active（配置本身没问题）→ poller 日志 `lark_oapi unavailable` / `botpy unavailable` → **bot-poller.Dockerfile 从建立起就只装 requests/pycryptodome/qrcode/pillow，从未装 qq-botpy/lark-oapi/wecom-aibot-sdk/dingtalk-stream**（pyproject all-frontends 有声明，但镜像构建绕过了它）→ 装好 SDK 后暴露 3 处代码 bug + 1 处平台侧配置问题。
- **Changes**：
  - bot-poller.Dockerfile: pip 追加 4 个渠道 SDK（qq-botpy>=1.0, lark-oapi>=1.0, wecom-aibot-sdk>=1.0, dingtalk-stream>=0.20）。
  - poller_server.py `QQAdapter._intents()`: `c2c_message`/`group_at_message` 不是 botpy 1.2.x 有效 flag（赋值即 AttributeError，被 `except: return None` 吞 → Client 构造 intents=None 崩溃）→ 改 `botpy.Intents(public_messages=True)`（1<<25 同时驱动 on_group_at_message_create + on_c2c_message_create，flags.py 源码实证）。
  - poller_server.py FeishuAdapter: `log_level=self._lark.LogLevel.WARN` → `WARNING`（lark_oapi 1.7.x LogLevel 枚举成员是 WARNING；WARN 抛 AttributeError('WARN')，日志表现为 `ws error: WARN`，极具迷惑性）。
  - poller_server.py QQAdapter._run: botpy 1.2.x Client.__init__ 直接调 `asyncio.get_event_loop()`，子线程无当前 loop 抛 RuntimeError → 线程内先 `set_event_loop(new_event_loop())`。
  - 部署: rebuild bot-poller + restart platform（RestoreActiveBots 只在 platform 启动时触发，poller 重启后不会自动重推——重启 platform 才有 restore 日志）。
  - 52 个 poller 单测全绿（本地需装 botpy/lark-oapi 等 SDK 才能跑流式测试）。
- **Insights**：
  - **"SDK 惰性导入缺失只影响该渠道"的容错设计掩盖了镜像缺依赖的事实**：poller 一直"正常运行"（listening + 微信渠道 OK），飞书/QQ/钉钉/企微全部静默不可用，从 8-10 部署至今。验证真实渠道连通性必须看 `ws started`/`ready` 日志或实测消息，不能只看容器健康。
  - **SDK 小版本 API 差异是真实杀手**：botpy 1.2.1 的 Intents flag 名（public_messages）、lark_oapi 1.7.x 的 LogLevel 枚举名（WARNING）都与代码假设不同；`except Exception: return None` 这类吞异常写法让首因更难定位——先打印再决定吞不吞。
  - **QQ 开放平台 IP 白名单（官方文档 §8.5）**：新增机器人默认启用 IP 白名单，正式环境（api.sgroup.qq.com）只有白名单 IP 才能连 WS 和调 OPENAPI；401 code 11298 err_code 40023002「接口访问源IP不在白名单」。配置入口 = q.qq.com 管理端 → 开发设置 → IP 白名单配置；服务器出口 IP = 23.94.23.150（ifconfig.me）。加白名单后需重启 poller（QQAdapter 无自动重连，_run 异常线程即死）。
  - 飞书此前同样从未真正连通（SDK 缺失），用户"配置了飞书"只是 Web 界面 active；修复后已 ws started，与 QQ 一起实测。

## [2026-08-12 | 生产 WORKER_START_FAILED: runner 挂载校验漏了 pkg cache 卷 + 全渠道连通]

- **Events**：飞书触发的任务报 `WORKER_START_FAILED: post-create inspect failed: runner mounts = 7, want exactly 6`——8-12 加 stdio 时 docker_cli.go 按 `GA_PKG_CACHE_VOLUME` 追加第 7 个挂载 /var/cache/ga-pkg（compose 生产已配置），但 inspect.go 的 post-create 校验表写死 6 个挂载，两者失配 → 生产每个 runner 创建都 409 RUNNER_ENSURE_FAILED。同日用户加 QQ IP 白名单后平台重启，`[Poller] qq ... ready`（QQ WS 首次真正连通）。
- **Changes**：
  - inspect.go `validateInspect` 增加 `pkgCacheVolume` 参数：非空时 expected 挂载表尾追加 {dst: PkgCacheMount, rw}（字典序 /var/cache/ga-pkg 恒在 /ga/... 之后），并对该挂载专门校验卷名归属（/var/lib/docker/volumes/<vol>/_data）+ 无 subpath + 必须 rw + 必须 volume 类型；未配置时仍严格 6 挂载。调用点（Inspect）传 d.cfg.PkgCacheVolume，测试 5 处调用同步 + 新增 TestValidateInspectPkgCacheMount（accept + 4 种漂移拒绝）。
  - 端到端验证：用平台容器内 GA_MANAGER_SECRET 构造 HMAC 签名请求，`POST /v1/workspaces/ensure` + `/v1/runners/ensure`（config_files 必须非空——writeConfigFiles 空 map 不建 config/g<gen> 目录，docker create 挂载 subpath 即失败；[]byte 在 JSON 里是 base64）→ HTTP 200 created，容器创建成功，测试后 DELETE 清理。
  - 全量 `go vet` + `go test ./...`（TEST_DATABASE_URL 指向本机 ga-test-pg 容器 172.17.0.2）零失败。
- **Insights**：
  - **"创建路径加了新挂载，校验路径必须同步"是第 N 次同类教训**（表改名 marker、契约字段改名同理）：docker_cli 与 inspect 是同一功能的两个面，单测 mock 的 inspect 形状与真实 docker 输出必须对齐，否则只测出"代码自洽"而非"与 daemon 一致"。这次是 409 在首次真实任务才暴露。
  - **模拟 manager 控制面请求可做生产端到端验证**：GA_MANAGER_SECRET 在 platform 容器 env，HMAC canonical = method\npath\ntimestamp\nnonce\nbody，X-GA-Timestamp/Nonce/Signature 头；workspace_key 用真实 "personal:1"（owner 1）。创建测试 runner 后必须 DELETE 清理（无 lease 的 runner 会驻留到 idle TTL）。
  - **QQ IP 白名单修复后无需改任何代码**：poller 重启 + platform restart（RestoreActiveBots）后 botpy WS 直接 ready——白名单是连接前置条件，代码无感知。

## [2026-08-12 | 旧实现漂移全量清理: runner 创建/校验双面同步 + SDK API 面逐项实证]

- **Events**：修完 WORKER_START_FAILED（挂载 7 vs 6）后按用户要求系统清理"旧实现漂移"——不逐处打地鼠，而是把创建路径与校验路径、SDK 调用面与已装版本逐项对齐。
- **Changes**：
  - Go 侧（创建/校验双面同步）：①`protectedRunnerEnvKeys` 补 `GA_OVERLAY_ROOT`（创建时恒设但未受保护——控制面透传可覆盖，安全相关）+ 4 个 pkg cache env（NPM_CONFIG_CACHE/UV_CACHE_DIR/UV_TOOL_DIR/PIP_CACHE_DIR）；②inspect `wantEnv` 补 `GA_OVERLAY_ROOT`（创建/校验失配）；③inspect 新增 pkg cache env 配套校验：启用时 4 个 env 必须精确指向 PkgCacheMount 子目录，未启用时不得存在（与挂载校验对称）；④过时注释清理（"固定六个挂载"/"five workspace subpaths"）。
  - Python 侧（SDK API 面实证）：容器内逐项验证 4 个 SDK 全部调用面——lark_oapi 1.7.2（EventDispatcherHandler/ws.Client/LogLevel.WARNING/Client.builder/im.v1.message.create+update 全 PASS）、botpy 1.2.1（Intents(public_messages=True)/BotAPI.post_group_message+post_c2c_message（含 markdown/msg_id/msg_seq 参数）/botpy.http.Route 全 PASS）、dingtalk_stream 0.24.3（DingTalkStreamClient/Credential/ChatbotMessage.TOPIC/register_callback_handler 全 PASS）、wecom_aibot_sdk 1.0.8（WSClient/on/connect/send_message 全 PASS，client.stop 不存在但代码未调用）。根项目 frontends/qqapp.py 防御式探测列表残留 4 个旧属性名（c2c_message/c2c_messages/group_at_message/group_at_messages——botpy 1.x 不存在），已清理并注释 pyproject 约束依据；fsapp.py 全部 API PASS（它用 LogLevel.INFO 无 WARN 问题）。
  - 矩阵核对：domain.IsValidChannelType 5 渠道 = OpenAPI enum = poller VALID_CHANNEL_TYPES = StreamForwarder 分档（feishu/qq/wecom 流式, wechat/dingtalk/web 终态）全部一致；文档 IM_CHANNEL_BINDING/IM_STREAMING_DELIVERY 与现状一致（QQ 流式参数仍诚实标注待实测）。
  - 验证：Go 全量 18 包 ok 零 FAIL + poller 52 passed；生产端到端 ensure 200（新镜像挂载+env 双校验通过），测试 runner 已销毁；make build 全量重建 + force-recreate platform/sandbox-manager，4 渠道 restored。
- **Insights**：
  - **"创建路径"与"校验路径"是同一功能的两个面，必须成对修改**：本次共发现 3 处同型漂移（挂载表、env 白名单、env 校验表），全部是"一侧加了东西另一侧没跟"。防漂移手段：校验表应从创建参数推导（如 pkg cache 分支共用同一 PkgCacheMount 常量与子目录列表），测试里 fixture（fixedInspectEnv）也要与真实创建参数同源。
  - **SDK 调用面用"容器内 introspection"实证比读文档可靠**：把代码里每个 API 调用点列成检查清单，在真实已装版本上逐个 hasattr/签名验证——一次清掉所有"可能是旧 API"的不确定性，包括根项目未启用的前端。
  - 防御式兼容代码（hasattr 逐个探测旧属性名）在约束已收紧（pyproject >=1.0）后是死代码加噪声，应随约束清理，避免未来维护者误以为旧名仍有效。

## [2026-08-12 | IM 流式两个真实渠道问题: 思考过程外泄 + QQ 40007 前缀不连续]

- **Events**：用户真实渠道实测(飞书/QQ 首次真正连通后)暴露两个问题: ①飞书收到 agent 过程转录(LLM Running/Turn 标记、<summary> 工作记忆、🛠️ 工具调用、!!!Error 片段)而非纯回复; ②QQ 流式只看到 '…' 占位+"生成中"状态, 最终结果以新消息补发——poller 日志 4 次 `stream_messages 500 {code:40007 已下发内容前缀不可修改}`。
- **Root causes**：
  - ①GA 核心 display 流(agent_loop yield_info)是过程转录, worker 的 `_create_agent` 注释声称"用户可见输出来自 cleaned reply text"但实现没做到: agent_loop 非 verbose 分支仍无条件 yield Turn 标记与 `🛠️ name(args)`, <summary> 是系统 prompt 要求模型输出的工作记忆块(在回复文本内)。worker chunk 与 final_body 原样透传 → 打字机/终态全脏。
  - ②QQ 官方文档(bot.qq.com 流式发送单聊消息)确认: replace 模式 `ContentRaw 须以上游已下发前缀 SentContent 开头`, 官方示例每帧都保留首帧"正在生成回答，请稍候"作前缀。我们的 open 首帧发 `text or '…'`(scheduler BeginReply 不带文本 → 发 '…')而累积基准 st['text']='' → 第一个 append 帧不以 '…' 开头 → 40007。用户看到的"状态再加载"就是 '…' 占位消息。
- **Changes**：
  - 新增 `ga_worker/reply_clean.py` `clean_reply_text`: 移除 `<summary>...</summary>` 块、`\n{Turn N|LLM Running (Turn N)} ...\n\n` 标记(含 ** 加粗变体)、`🛠️ name(args)` 工具行、`!!!Error: ...` 片段、折叠连续空行; 幂等。接入 task_drain `_handle_next_item`(chunk 文本+display_history)与 `_handle_done_item`(final_body); 纯工具轮清洗后空文本不产生 chunk。13 个单测。digest 基于清洗后 final_body(worker 内一致)。
  - Go 流式 open 携带首段文本: `StreamingSender.BeginReply` 接口加 `firstText` 参数; ilink.go open 请求带 Text; loopback 记录; stream_forwarder flush() open 时传 frag(首段不再 append 一次); 相关测试断言同步(open firstText、appends 数量变化)。poller QQ `send_stream_open` 累积基准 `st['text'] = text or '…'`(与首帧实际下发内容强一致, 前缀连续)。
  - 验证: Go 18 包零 FAIL、poller 52、worker 141(含新测试); 生产部署 make build + runner-digest(worker 代码在 ga-runner 镜像, .env digest 必须更新——否则 sandbox-manager 仍用旧镜像); 镜像内实证 clean_reply_text 清洗正确。
- **Insights**：
  - **"设计注释声称" ≠ 实现达成**: session_lifecycle 注释说 verbose=False 后输出即用户可见, 但 agent_loop 非 verbose 分支的 yield 面(无条件 Turn 标记/工具行)从未被核对——真实渠道首测才暴露。跨组件意图校验要落到"实际 yield 了什么"。
  - **QQ 流式 replace 的前缀契约是硬约束**: 首帧内容即 SentContent 基座, 后续每帧必须以其为前缀; 任何"占位符与实际累积不一致"都会 40007。open 必须携带真实首段(Go 侧), 累积基准必须=首帧内容(poller 侧), 两侧都修才闭环。
  - **worker 代码在 ga-runner 镜像且 .env 用 digest 引用**: 改 worker 后必须 make runner-digest 更新 .env, 否则新代码不生效(与 8-12 上午 bot-poller 镜像滞后同型教训)。

## [2026-08-12 | 思考外泄的架构正解: agent_loop 输出分层(verbose 语义落实), 非 worker 补丁]

- **Events**：用户质疑"思考内容为啥只有飞书有、QQ/微信没有"+"是架构问题还是打补丁"。实证: ①checkpoint result 文件(各渠道最终交付文本)全部以 "LLM Running (Turn 1) ..." 开头、含 <summary>/🛠️——**三个渠道的交付文本同源同脏**, 飞书因流式打字机逐帧显示最显眼, QQ/微信是一次性消息(思考在开头, 用户忽略); ②session_lifecycle `_create_agent` 注释声称 verbose=False 即用户可见输出, 但 agent_loop 非 verbose 分支仍无条件 yield Turn 标记/工具行——**注释与实现漂移, worker 正则清洗(reply_clean)是补丁**。
- **Changes(架构修复)**：agent_loop.py 输出分层由 verbose 开关落实——verbose=True 输出完整过程转录(TUI/CLI/根项目前端展示思考, 不变); verbose=False 只 yield 用户可见回复文本: 不 yield `\n{turnstr}\n\n` 轮次标记、不 yield `🛠️ ...` 工具行(删死代码 _compact_tool_args)、`_clean_content` 剥离 <summary> 工作记忆块。worker 侧 reply_clean 保留为纯防御层(幂等)。新增 tests/test_agent_loop_layering.py 回归(verbose 双分支断言)。agent_loop.py 在 platform + ga-runner 两个镜像, make build + runner-digest 双更新。
- **Insights**：
  - **"补丁 vs 架构"的判断标准: 问题的语义归谁**。思考内容混入用户交付 = GA 核心输出流语义混杂(过程转录与用户可见回复无分层)——归因在 GA 核心, worker 正则清洗是症状治疗(模型输出格式一变就漏); 输出分层由已有 verbose 开关表达才是正解(开关本就为此设计, 只是实现没落实)。
  - **渠道差异是感知差异不是路径差异**: 飞书(流式打字机逐帧)/QQ(流式失败只剩占位+新消息)/微信(final-only 一次性)三者最终文本同源; "只有飞书有思考"的观察来自打字机效果, 不是渠道实现差异——对比渠道行为前先确认交付文本同源。
  - **QQ 40007 属官方文档契约未在实现时验证**(replace 前缀语义), 修复=接口契约对齐(BeginReply 带 firstText), 非补丁; 与思考外泄(架构分层)是两类问题, 分开修。

## [2026-08-12 | 输出分层配套补强: display 流事件信号 + tgapp 事件化 + 心脏缺陷修复]

- **Events**：审查上一轮 IM 优化发现三个连带问题: ①分层后非 verbose 流**零事件**——工具执行期(单轮可数分钟)无任何 display 事件, worker 推进窗口(150s)到期心跳停发, 长工具轮会被 idle reaper(300s)误收割(旧代码靠每工具一行的 🛠️ 痕迹隐式刷新); ②tgapp 的轮次消息拆分靠解析文本标记("LLM Running (Turn N)"行), 分层后标记消失 → 整轮回复塌成单条持续编辑消息, 解析机制静默失效; ③根项目 qqapp 删 direct_message intent 后 on_direct_message_create 永不触发(死 handler), lark-oapi>=1.0 与 LogLevel.WARNING(1.7+)版本假设不匹配。
- **Changes**：
  - **display 流事件信号(架构)**: agent_loop 非 verbose + yield_info 时 yield `{'tool': name}`(工具活动事件, 非用户文本); agentmain 每轮边界推送 `{'turn': N}` 事件——先冲刷残留文本(保证 'next' 不跨轮次边界, 文本归属精确), 事件携带 outputs=turn_resps[-2:](wechatapp 按 outputs[-2] 落定上一轮文本的消费方式不变)。所有消费方只认 next/done 键, 未知键安全忽略(逐一核对: worker/wechatapp/tgapp/conductor/desktop_bridge/ga_httpapp/tui/stapp)。
  - **tgapp 事件化**: 轮次协调改事件驱动(on_turn: 上一轮有正文→定型发消息+开新草稿, 空轮保留草稿); 删 8 处文本标记解析死代码(_TURN_MARKER_RE/_TURN_SUMMARY_*/_turn_marker_number/_maybe_partial_turn_marker/_extract_turn_summary/_inject_turn_summary); 保留 code fence 追踪(流式 markdown 安全)。
  - **清理**: qqapp 删 on_direct_message_create(频道私信 intent 1<<12 未订阅, 注释说明); bot-poller.Dockerfile lark-oapi>=1.0→>=1.7(LogLevel.WARNING 版本绑定); Go stream_forwarder AppendText TrimSpace 防护(空/纯空白 chunk 不进缓冲, poller '…' 占位分支不可达, QQ 前缀契约下占位残留进终态); feishu/wecom 累积基线不对称加注释(飞书/企微=全量替换无前缀约束, QQ=前缀严格, 差异来自渠道契约); reply_clean/agent_loop 文档化语义边界(字面 <summary> 会被剥, 假设模型正文不出现该标签); uv.lock 孤儿文件 gitignore + 删除(CI 走 pip 无 uv 链路)。
  - **测试**: 新增 tests/test_agentmain_stream_events.py(turn 事件先冲刷/携带 outputs/tool 事件转发 3 断言); test_agent_loop_layering.py 增 tool 事件双分支断言。全量: 根 15 + worker 141 + poller 52 + Go 4 包全绿。
- **Insights**：
  - **分层不能只删文本, 必须补事件**: 用户可见层与内部信号层是正交维度——去掉过程文本后, "推进/保活/轮次边界"这些信号必须以事件形式保留, 否则下游的保活契约(心跳窗口)和轮次协调(前端)会静默退化。事件 dict 是 display 流已有的模式({'turn': N} 一直在 yield_info 里), 扩展它比发明新通道便宜。
  - **轮次边界必须由生产者精确给出**: 前端按 'next' 文本的 turn 字段切分不可靠(>30 字符冲刷可跨轮合并短轮文本); 生产者(turn 事件)先冲刷残留再发边界, 消费端切分才精确——信号精确性归生产者, 消费端只消费。
  - **心跳语义是隐式契约**: worker 的"卡死停发心跳"设计依赖 display 事件频率, 任何输出层改动都必须回查 last_progress_at 的刷新面——本次靠 review 发现, 建议未来把"输出流事件 → 心跳窗口"列为输出层改动的固定检查项。

## [2026-08-12 | tgapp 占位/终态语义定案: 查证 Telegram 官方 + 社区主流]

- **Events**：审查遗留问题"纯工具轮保留 thinking... 草稿直到下一轮正文, 要不要超时收尾"。查证官方文档与社区主流实现(Bot API 9.3 官方 AI 功能页、gramio/aiogram sendChatAction 文档、iii-hq harness worker、hermes-agent stream_consumer、ClaudeBot、yaebal、aws-bedrock-telegram-chatbot)。
- **官方立场**：①Bot API 9.3(2025-12-31)正式引入 `sendMessageDraft` 原生草稿流式(仅私聊): 客户端自动渲染 "Thinking…"/增量动画, 同一 random_id 更新; **收到正式消息客户端自动清除草稿**(message_typing_draft_ttl); 限流 20 次/5s + 40 次/30s(peer)。tgapp 的 `reply_text_draft(draft_id, ...)` 即此 API(random_id 语义)。②`sendChatAction('typing')` 是唯一"正在处理"原语, 5s 过期需循环重发, 官方建议仅用于"响应耗时明显"时; iii-hq 明确不用它(无 stop-typing API, 指示器在答复到达后仍残留数秒)。
- **社区主流**(多实现独立收敛): 私聊 = sendMessageDraft + editMessageText 兜底, **完成时以正式消息定型草稿**(drafts 无 message_id, 正式消息即终态); 群聊 = 节流 editMessageText(300ms-1.2s)+ 光标, ClaudeBot 甚至群聊只发最终消息防通知噪音; 工具边界 = 当前消息定型、新内容开新消息(hermes-agent on_delta(None) 语义, 与本项目 turn 事件机制同构); **占位/草稿绝不作为终态**, 空内容也要完成确认。
- **Changes**：结论=**不做超时收尾**(占位保持到内容到来是设计语义, 超时定型=消息碎片+通知噪音, 反模式); 只修终态残留——tgapp `_refresh` done 且 body 空时 `"..."` → `"✅ 已完成"`(纯工具轮任务结束不再留孤立 "..." 消息; 私聊 draft 模式由客户端自动清草稿+正式消息定型, 本就无残留)。
- **Insights**：
  - **判断"是否反模式"要看官方原语的语义**: 官方把 draft/typing 设计为"临时指示、正式消息到达即消失"——任何把指示器固化为终态的做法都违背官方语义; 反过来, 占位在生成期间保持不消失正是官方设计意图。
  - **社区收敛的验收标准**: 流式体验三条——①立即反馈(占位/typing); ②过程中原地更新(不产生消息碎片); ③终态必须是正式内容(占位零残留)。tgapp 三条件全部满足(私聊 draft 原生、群聊 edit、终态定型), 无需再加机制。

## [2026-08-12 | IM 优化一轮整体审查: 4 处修复 + 部署缺口确认]

- **Events**：按用户要求对当轮全部未提交改动做整体审查（输出分层/事件信号/reply_clean/QQ 40007 前缀修复/pkg cache 校验/SDK 兼容）。验证手段：通读全部 diff + 逐条核对 SDK 断言（qq-botpy 1.2.1 Intents(public_messages=True)=1<<25 源码实证、lark_oapi 1.7.2 LogLevel 成员=WARNING、wecom-aibot-sdk 1.0.8/dingtalk-stream 0.24.3 PyPI 存在）+ 官方文档实证（bot.q.qq.com stream_messages: replace 模式 ContentRaw 须以已下发前缀开头、40007=已下发前缀不可修改、msg_id 被动锚点、50 QPS）+ 全量测试（根 17/poller 52/worker 142/Go 含 race 全绿）+ 生产容器逐一 diff 部署镜像与工作树。
- **Changes（4 处修复）**：
  - **tgapp 纯工具收尾轮全量重复 bug（本轮引入）**：删轮次标记后 on_turn 把上一轮定型为独立消息，但 finalize 的「空会话 + done_text」分支把全量累计文本注入空会话 → 最后一条消息重复全部历史文本（旧代码靠标记文本使该分支不可达）。修：空会话以 "✅ 已完成" 收尾（与 _refresh 空终态语义一致），附件仍按 done_text 全量发送；新增 tests/test_tgapp_turn_finalize.py 两条回归。
  - **conductor 子代理卡片静默退化（本轮引入）**：卡片 running=extract_last_summary / stopped=extract_last_text_reply 依赖非 verbose 流中的 summary/轮次标记，分层后全部消失 → running 卡片空内容、stopped 显示全量文本。修：subagent 改 verbose=True（卡片是"展示思考过程"表面，与架构注释的 verbose 消费者一致，一行恢复旧语义）。
  - **ga_cli CLI 默认静默化（注释与默认值漂移）**：架构注释声明 CLI 属 verbose 转录表面，但 ga cli 不传 --verbose，分层后交互 CLI 只显示用户文本（分层前默认可见轮次标记+工具痕迹）。修：cli 命令补 --verbose。
  - **pyproject lark-oapi>=1.0 → 1.7**：poller Dockerfile 已收窄 >=1.7（LogLevel.WARNING 版本绑定），pyproject all-frontends 未同步——新装环境会装出 feishu WS 启动即崩的版本。
  - 部署缺口确认：**turn/tool 事件 + agentmain 冲刷轮次 = 未部署**（生产 platform/ga-runner 镜像只有分层无事件，长工具轮心跳风险未闭环）→ 本轮部署补齐。
- **Insights**：
  - **"所有消费方只认 next/done 键"的核对只验证了安全性，没验证显示语义**：conductor/stapp 等对非 verbose 流格式有隐式依赖（marker 切轮、summary 提取），分层后静默退化——输出层改动必须把"每个消费方从流里提取了什么信号"也列入检查项，不只是"是否忽略未知键"。
  - **接口改动要查"未改动文件"**：BeginReply 加 firstText 后所有实现/调用点都改了，但真正容易漏的是**没改的文件**（conductor/ga_cli/pyproject 这类不直接碰 IM 代码的消费方）——审查清单应含"反向引用面"。
  - **部署镜像与工作树的 diff 是最终验收**：md5 逐文件比对发现"分层已部署、事件轮未部署"的中间态——只信容器内实际文件，不信 memory.md 的"已部署"字样。

## [2026-08-13 | 时区统一北京时间: GA 感知时间差 8h 根因与修复]

- **Events**：用户在微信侧反馈两个问题——①配置了双渠道（new=默认/go=备用），但聊天显示一直用 go 不用 new；②服务器在美国，GA 返回时间与国内差 8 小时。排查：DB 查 llm_providers（new is_default=t, go f, 同 base_url 同模型）；llm-proxy 日志实证 00:45-00:46 双 provider 交替 502（new-api 停机）；timedatectl 实证服务器 Etc/UTC（非美东/美西，恰好与北京差 8h）。
- **Changes**：①ga-runner.Dockerfile ENV 加 TZ=Asia/Shanghai（zoneinfo 由 python base 自带，无需装 tzdata），重建镜像 + make runner-digest（新 sha256:3b2f35d6 已写入 .env）+ 重启 sandbox-manager/platform；②compose.yaml 四服务（platform/bot-poller/llm-proxy/sandbox-manager）加 TZ: Asia/Shanghai（web 浏览器本地化不需、postgres 故意不加）；③**llm-proxy/sandbox-manager 为 alpine/docker:27-cli 镜像实测无 /usr/share/zoneinfo，TZ 注入后仍 UTC（静默失效）** → 两个 Dockerfile 补 `apk add tzdata` 重建生效；④验证：镜像内 python time.tzname=CST、date=+8、平台日志时间戳 +08:00、healthz 全绿。
- **Insights**：
  - **TZ 环境变量生效前提 = 镜像内有 zoneinfo**：alpine/debian-slim 类最小镜像常缺 tzdata，注入 TZ 后 glibc/musl 回退 UTC 且无任何报错——改完必须 `docker exec <容器> date` 实证，不能只看 compose config / env 里有 TZ。
  - **渠道切换的\"默认失效\"错觉 = spring_back 防抖窗口**：MixinSession 切换成功后 300s 内每次调用直接走备用节点不回弹 primary（防反复横跳设计）；用户高频连续消息会一直续窗口，看起来\"从不用默认\"。排查此类问题先看 llm-proxy 日志的 502 时间窗（切换触发点），再看 DB is_default 与 llm_nos 顺序（is_default DESC, id ASC 强制默认第一）。
  - **时间语义分层**：DB 存储 UTC（Go .UTC() 明确）→ web 显示层 toLocaleString 浏览器本地化 → 只有 GA 运行环境（runner 容器）的 datetime.now()/strftime 跟随容器 TZ。修复只动 runner 镜像 + 辅助服务日志 TZ，不碰存储层，风险最小。

## [2026-08-13 | 图片任务 300s 超时根因: 模型生成 shell 风格 code_run 脚本]

- **Events**：用户换多模态模型(agnes-2.5-flash)后发图问"这是啥"，任务 307s TASK_INTERRUPTED 零回复。排查链: task_events 零 chunk → llm-proxy 0 WARN(16:25 重启后) → new-api 日志无 chat/completions → capability_usage 空表(终态撤销清理, 伪证) → 手动 curl 域名实例发现图片请求 500 "loading IMAGE data"(但那是我的测试, 真实任务 GA 不发图片给 LLM) → 复现(webhook HMAC 注入真实 bot + docker logs 抓 runner)抓到现场。
- **现场(runner 容器日志)**: 第一轮 agnes 正常响应([MixinSession] Using session provider-1), 模型在 code_run script 里生成 `python3 -c "..."`/`pip install Pillow pytesseract`/`cd &&` 等 shell 命令 → GA 写入 .ai.py 由 python3 执行 → SyntaxError → tool_result 报错 → agent 无限重试(context 30K→58K→...), 直到 300s 任务超时。**根因**: ①code_run 工具描述未声明"纯 Python 禁 shell", 模型按 shell 生成; ②GA 只把图片路径文本给模型, 多模态模型收不到图片内容, 只能靠 code_run 瞎折腾。
- **Changes**:
  - ga.py `_SHELL_STYLE_RE`(行首命令+空白/分号/管道; 含 shebang; Python 调用 echo(/ls( 不命中) → code_run python 模式命中自动降级 bash 并打印提示。
  - assets/tools_schema.json code_run 描述明确纯 Python/subprocess 处理 shell/pip 引导。
  - agent_loop.py `_inject_attachment_images`: 首轮 user content 把 prompt 引用的 attachments/*.jpg/png 等 base64 成 image_url 块注入(≤3MB/3张/去重; 路径优先 GA temp/ 回退 cwd——进程 cwd 是 /ga/legacy 不是 temp, 首版用 cwd 解析失败, 第二版修复)。NativeOAISession 通道原生支持 image_url(llmcore _msgs_claude2oai 保留)。
  - ga-runner.Dockerfile: 加 pillow/numpy/rapidocr-onnxruntime(GA memory/ocr_utils.py 依赖) + libgl1 libglib2.0-0(onnxruntime→opencv 依赖)。
  - 实证: 修复后同图片任务 187s succeeded, 正确识别"卡通小男孩角色立绘 30 动作, 水印'跑AI生'"(模型还自动 patch 了 memory/vision_api.py 的 /v1 双拼 bug 后调用成功)。
- **部署坑(重要)**:
  - **用户清理镜像删掉 ga-runner:local → 新任务 WORKER_START_FAILED "Unable to find image @sha256:..."**: 本机无 registry 时 `make runner-digest` 输出的 @sha256: 是镜像 ID 而非 RepoDigest, docker create 按 digest 引用会去 registry 拉取——镜像被删/无 RepoDigest 时彻底拉不回。**本机自部署 GA_RUNNER_IMAGE 已改回 :local tag**(GA_RUNNER_ALLOW_TAG=1 本就在), tag 引用重建镜像后无需重启 manager。
  - **runner 容器 idle 复用旧镜像**: 镜像重建后已存在的 runner 容器仍跑旧代码, 验证修复必须删旧容器(docker rm -f)否则"改了没效果"。
  - **capability_usage 表不能作"请求未到 llm-proxy"证据**: 终态撤销时计量行随撤销一并 DELETE(llm_capability_store.go), 空表=已清理非未发生。
- **Insights**:
  - **模型遵循度差异是真实故障源**: 同工具描述, deepseek 生成纯 Python, agnes 生成 shell——换模型后必须实测工具调用面, 不能假设"之前能跑就还能跑"。
  - **多模态模型接图要靠 GA 注入而非模型猜路径**: GA 架构里本地图片不会自动进 messages, 模型只能靠路径文本猜——image_url 注入是第一轮就生效的最小改动(NativeToolClient list content 透传, llmcore 已支持 image_url→上游)。
  - **复现 > 推理**: 在 new-api/llm-proxy/platform 三层日志都无结论时, 用 webhook HMAC 构造真实入站消息+实时 docker logs runner 是最短路径; GA 的 print(MixinSession/Attach/code run output)全在容器 stdout。

## [2026-08-13 | IM 多模态结构化链路: 补丁升级为契约级架构]

- **Events**：用户在架构审查中质疑补丁方案（agent_loop 正则扫 prompt 文本逆向图片路径）不可维护——正确。分析确认：①GA 原生协议层（llmcore）本就支持 image_url 块（Claude base64→OAI 转换齐全），**put_task(images=) 参数是早期设计的死代码**（从未被消费）——原生多模态是半成品；②平台化时媒体走"workspace 文件+prompt 文本路径"模式（文件模式），多模态模型收不到图片内容（消息模式缺失）；③主流共识（Claude Code/Codex/LangChain 实证）：媒体是用户消息的 content block，输入处理层完成文件→块转换（含降采样），工具视觉是 fallback。
- **Changes（契约先行，全链路结构化）**：
  - **proto**：TaskEnvelope 加 `repeated MediaItem media = 15`（alias/original_name/relative_path/size_bytes），generate_bindings 同步 Go gen + worker python。
  - **migration 0056**：tasks.media jsonb（DO 块幂等）；domain.TaskMedia + SubmitTaskCommand.Media + Task.Media；store submitTaskTx 写 media（validateTaskMedia: 干净相对路径/上限 16 条/非负大小）；scanTask 读回。
  - **Go**：router_commands 路由时 currentRefs(ImportInbound 附件清单) → toTaskMedia（仅 inbound）→ 随任务持久化；scheduler_dispatch workerTaskEnvelope 填 Media；SessionFileRef 补 SizeBytes（复制后 stat 落盘结果）。
  - **worker**：task_drain `put_task(prompt, images=[m.relative_path for m in task.media])`。
  - **GA**：put_task(images=) 死参数补完（run() 消费→initial_user_content 结构化）；agent_loop 重构 `media_content_blocks(user_text, image_paths=None)`——显式路径为主路径，文本正则降级兜底，PIL 降采样（最长边 1568px，对齐 Claude Code 成本控制），非图片扩展名跳过；_inject_attachment_images 保留兼容别名。
  - 实证：同图任务 306s 超时 → 补丁 187s → 架构化 **54s** 成功（直接看图秒回）；tasks.media 落库验证 [{"alias":"F011","size_bytes":149865,...}]。
- **Insights**：
  - **补丁与架构的判据**：补丁=在消费侧逆向推断生产者约定（正则解析 prompt 文本）；架构=生产者把结构化数据经契约显式传递（proto 字段）。跨语言系统里"文本约定"是最脆的接口，任何跨层数据都应走契约。
  - **GA 原生意图是正解**：put_task(images=) 死参数暴露了设计者的原始意图——实现它而不是另起炉灶，是成本最低的架构路径。
  - **文件模式与消息模式正交**：媒体既要给模型看（content block，模型可感知）也要给工具用（workspace 文件，可读/可存档/可导出）——并存而非互斥；主路径消息注入，工具（OCR/vision）是 fallback（与 Claude Code/Codex 一致）。
  - **成本控制随架构走**：base64 每轮重发膨胀 payload——降采样在注入层做（PIL 1568px），后续可升级 file_id 引用（Anthropic Files API 模式）。

## [2026-08-14 | Phase B 生图 image_gen v1 实施: 直连形态先行 + 双形态设计定稿]

- **Events**：方案两轮 fresh-context 审查通过（首轮 2 阻断 B-1/B-2 已定稿处理；二轮 0 阻断、4 重要 I-1~I-4 已回写）；用户拍板 D1：直连形态先行（v1 实施），托管（llm-proxy `llm.image`）为终态设计按需实施。实施完成：llmcore `BaseImageGenClient`/`OpenAIImageGenClient`/`resolve_image_gen` + ga.py `do_image_gen`（第 10 原子工具）+ tools_schema ×2 + mykey 模板 + 26 个单测全绿；本地 mock 闭环（同步/SSE 两种模式 + 未配置错误路径）验证通过。
- **Changes**：`tenant_platform/docs/IM_MEDIA_ARCHITECTURE.zh-CN.md` §6（B1-B5+D1 勾选 + §6.3 失败语义）/§9（生图 ✅）/§10/§11；`SUBTASKS.csv` T8 拆分 T8.1-T8.4；PROGRESS.md Phase B 段（D1 取代原"有意推迟 D6"表述）；tui_v3 `_XML_TOOL_RE` 补 image_gen。
- **Insights**：
  - **实现位置是硬约束**：生图客户端必须进 llmcore.py（沙箱 overlay 只物化 LEGACY_MODULES 固定清单）——"新增一个模块"这种直觉正确方案在平台沙箱里是全平台任务失败的炸弹（二轮审查 I-1）。
  - **错误前缀决定成败**：工具错误文本 `!!!Error:` 出现在模型最终回复尾部会触发 do_no_tool 致命判定/LLM_FAILED 链——工具层错误必须用 `[Error: xxx]` 前缀（I-4）；这是"失败诚实"在 GA 侧的具体形态。
  - **参数裁剪按 model**：gpt-image 恒返回 b64_json，发 response_format 可能 400——"协议兼容"不是把文档参数全发出去，而是按模型裁剪（I-3）；同理 dall-e 无 output_format。
  - **marker 回显依赖是交付链盲区**：工具返回 `[FILE:]` marker ≠ 交付发生，模型忘回显则静默丢失——v1 靠 schema 描述 + next_prompt 强调兜底；托管形态终态实施时须补 generated_output_files 登记（仿 do_export_docx，I-2）。
  - **落盘路径必须 self.cwd 相对**：硬编码 script_dir 在容器里（/ga/legacy vs 工作区 temp 符号链接）会写错位置，交付链断——根项目模式 outputs/ 无人预置，必须先 os.makedirs(exist_ok=True)。
  - **双形态 = 配置差异**：直连/托管切换不引入结构性问题（BaseSession 先例实证：chat 直连与平台托管共用一份代码）；平台侧零改动是 v1 范围红线。
  - **20MiB 交付上限要在工具侧前置检查**：Go 侧 delivery_capture 是 fail-closed 任务失败，等到提交时刻才爆等于让整个任务白跑——fail-closed 契约要前置到最近的生产者。

## [2026-08-14 | image_gen 真实 key 闭环实测: new-api 中转五模型兼容性图谱]

- **Events**：用户提供 newapi.myovo.cc.cd 双渠道 key（渠道1=6 图像模型，渠道2=96 模型含 agnes/sensenova）。一次性探测 10 请求（5 模型 × 最小/完整参数集）→ 定位 3 个兼容性差异 → 修客户端 → 完整 CLI 闭环（agnes-2.5-flash 对话 + gpt-image-2 生图）一次成功，agnes-image-2.1-flash url 直下兜底验证成功。探测成本可控：gpt-image-2 完整闭环 2.6MB PNG（quality=high，1254x1254）。
- **Changes**：客户端两处适配——①`_payload` size 缺省默认 1024x1024（new-api 计费硬要求）；②`_extract_images` b64_json 为空时 url 直下兜底（`_download` 流式读取 ≤20MiB 限流）；ga.py 的 20MiB 常量改为从 llmcore 导入（单一真值）；单测 27→30（+3 url 路径）。
- **Insights**：
  - **中转的"兼容"是带约束的**：OpenAI 兼容协议 ≠ 参数全部可发——new-api 要求 size 必传（计费），sensenova 的 size 是专属集合（1664x2496/2048x2048，无 1024x1024），错误信息里直接带合法列表（可引导模型自愈，符合失败诚实）。
  - **"恒 b64_json"假设在中转不成立**：sensenova/agnes 只回 url 直链（b64_json 空串）——客户端必须 url 直下兜底，否则 2/5 模型不可用；直链有有效期/CDN 风险，但比不可用强。
  - **默认参数是兼容性武器**：size 默认 1024x1024 = OpenAI 官方默认，对 gpt-image/gemini 系全兼容；个别模型不适配时靠错误文本自愈，不搞模型名黑名单。
  - **真实 key 闭环是唯一能发现"参数级"问题的验证**：mock 单测只验证形状，探测一次就发现 size 必传/url-only 两个设计盲区——"第二次就成功"的正确姿势是先探测后闭环，而非直接跑完整流程。
  - **对话模型与生图模型分离验证**：chat 走渠道2 agnes-2.5-flash（生产实证），生图走渠道1 gpt-image-2——两把 key 分渠道配置，各自独立 fail。

## [2026-08-14 | Phase B 托管形态实施: provider 能力维度 + llm.image 路由全链路]

- **Events**：v1 提交后直接实施 Step 3 托管形态（用户明确"这是多租户项目"）。全链路：migration 0058 capabilities 列 → store 读写 → llmproxy images/generations 路由（operation 参数化 llm.chat/llm.image，错配 401）→ worker_credential 按能力签发双 token → runtime_config image_gen 块下发（不进 chat mixin）→ policy 放行 image_gen → worker marker 兜底登记 → openapi/web 表单。验证：Go 全量 + race 3 关键包 + worker 146 + 契约 41 + web build 全绿。
- **Changes**：`LLMProvider.Capabilities`（省略=[chat] 存量兼容，EffectiveCapabilities/HasCapability）；`handleProviderPath(w,r,wantType,wantOp)` 参数化 + `OperationChat/OperationImage` 常量；`nativeEndpoint` 加 images/generations；生图响应 32MiB 上限（Content-Length 前置 fail-closed——MaxWorkerRequestBytes 只管请求体）；`issueProviderCapability` 抽公共签发逻辑；`RuntimeProviderBinding.Capability` 驱动 image_gen 块 vs session 变量分流；`install_image_gen_marker_registry`（generator 转发包装，成功才登记，错误零登记）；web capabilities 复选框（image 仅 native_oai）。
- **Insights**：
  - **"能力维度"是 provider 模型的正确扩展点**：单 model 字段约束下，image 能力 provider 必须是独立 provider（model=生图模型）；capabilities 列省略=[chat] 保证存量零迁移成本；native_claude 禁 image 是协议约束（生图走 OAI 端点）。
  - **operation 参数化是路由安全的骨架**：validateCapability 从硬编码 llm.chat 改为按路由维度校验——image token 打 chat 路由、chat token 打 image 路由全部 401 deny-by-default，双能力 provider 签双 token 各走各的路。
  - **image_gen 块不进 chat mixin 是设计红线**：生图是"角色分离"不是"同能力故障转移"；runtime_config 的 binding 能力标记驱动分流，多 image provider v1 fail-closed（GA 侧单块配置）。
  - **marker 兜底登记放 worker 层最稳**：GA 工具本体（ga.py）保持纯净（平台无关），worker 包装器做平台侧登记——与 export_docx 先例同构；generator 转发包装（yield 透传 + StopIteration.value 捕获）是 GA 工具包装的标准模式。
  - **响应上限是代理层安全审查的盲区**：MaxWorkerRequestBytes 只管请求体、DisableCompression 大 JSON 原样传输——生图响应可达 27MB+，按 Content-Length 前置拒绝比流式计数简单可靠。
  - **跨层联调要有"真实消费方探针"**：runtime_config 的 image_gen 块用真实 Python 子进程（llmcore.resolve_image_gen）验证 GA 能消费——同 runActualGAConfigProbe 先例，比 Go 侧断言字段更可信。

## [2026-08-14 | Phase B 生图实施完成态审查修复 + 生产部署]

- **Events**：独立 fresh-context 审查实施完成态（4 提交基线），发现 0 阻断 / 6 重要 / 10 建议。按"最佳状态"全量修复并部署。审查最大收获：**双能力 provider(chat+image) 的 runtime config 必然失败**——BuildRuntimeConfig 按 provider ID 去重，而双能力签双 token 双 binding（同 ID），原实现直接报 duplicate provider id；补测试才暴露（静态审查未抓到）。
- **Changes（3 提交 b58063a/4d08dad/faa63b6，CI 等效全绿后推送）**：
  - Go：reverse_proxy 32MiB 双闸（HasSuffix 路径判断修复自定义 base 前缀失效 + imageResponseGuard chunked 流式计数，超限中断连接）；runtime_config 按 (ID,能力) 去重 + 至少一个 chat provider 校验（image-only fail-fast）；migration 0059 capabilities DB CHECK（实测直插 ["video"] 被拒）；token.go 注释补 llm.image；新增测试：chunked/自定义前缀/image-only/双能力双 token。
  - 客户端：llmcore _delay 与 _stream_with_retry 完全对齐（retry-after=0 走指数退避）；ga.py output_format 枚举外回退 png；schema ×2 model 参数注明托管 409 语义；web 表单勾 Image 提示单 model 字段；mykey_template 双形态注释改已实施。
  - 文档：方案 §9.7 实施记录 + B1-B5 勾选 + 全部过时表述清除（9 文件 25+ 处）；IM_MEDIA §6.1/§11 + 托管语义边界段；README 9→10 工具 + 生图配置说明（中英）。
- **Insights**：
  - **BuildKit COPY 缓存命中存疑**：make build 后 sandbox-manager 镜像 CreatedSince 仍 22h（backend-go 已改），--no-cache 重建才出新 ID——改 Go 文件后 sandbox-manager 构建要留意缓存，必要时 --no-cache。
  - **生产迁移机制 = 全量幂等应用**：生产库 marker 表停在 0055，但 0056/0057/0058/0059 结构全部存在（SQL 自带 IF NOT EXISTS/DO 块）——验证迁移生效看结构/约束，不要看 marker 表。
  - **llm-proxy 无对外端口映射**（GA_HTTP_PORT 8088 是 web/nginx）——验证 llm-proxy 路由必须 docker exec 容器内 curl 127.0.0.1:8081。
  - **compose 端口映射 8088→8088 与容器内 LISTEN 8081 不一致是设计**（LLM_PROXY_LISTEN=0.0.0.0:8081 仅内网）。

## [2026-08-14 | spool 引用化交付文件 NULL content 事故: 微信生图成功事务回滚 CHECKPOINT_COMMIT_FAILED]

- **Events**：用户微信生图报"任务失败 CHECKPOINT_COMMIT_FAILED: insert delivery file outputs/image_*.png: null value in column content (SQLSTATE 23502)"。根因：迁移 0057 spool 引用化（审查 B4/T5）只新增可空 spool_path 列，未同步 INSERT——capture 侧 spool 模式域对象 Content=nil（delivery_capture_test 明确断言"Content must be empty for spool capture"），checkpoint_store 直插 $5 落 NULL 违反 content NOT NULL，整个成功终态事务回滚，任务判失败。
- **Changes**：`checkpoint_store.go` INSERT 改 `COALESCE($5, ''::bytea)`（落空 bytea，与 0057 注释设计"新写入行 spool_path 非空、content 为空"一致，列保持 NOT NULL 防御）；`store_test.go` TestCompleteSucceededPersistsDeliveryFiles 补 spool 行回归（Content=nil + SpoolPath 非空，断言落库后 Content 为空 bytea、SpoolPath 保留）；验证闭环：先临时还原 SQL 跑出与生产完全一致的 23502 错误，再验证修复通过；go vet/build + Go 全量 `-p 1`（含 DB）+ race（application/postgres）全绿。
- **Insights**：
  - **改 schema 列值语义必须 grep 全部读写点**：0057 把"content 存字节快照"改成"content 为空 + spool_path 引用"，却只加列没改 INSERT——域对象 nil 语义与 DB NOT NULL 约束失配；既有测试全走 loopback（content 模式）覆盖不到 spool 路径，生产第一次走 spool 就炸。
  - **迁移注释里写明的语义变更要当成契约**："content 为空" ≠ "为 NULL"——NOT NULL 列必须落空 bytea（`''::bytea`），COALESCE 在 SQL 层兜底比调用方逐处判 nil 更稳。
  - **回滚测试是修复闭环的最后一环**：先证明"无修复必失败（错误与生产逐字一致）"再证明"修复后通过"，回归测试才算真的覆盖了事故路径。

## [2026-08-14 | 微信生图交付死信全链路根因: spool relPath 缺失 → file_item 原图上传 → CDN 节流]

- **Events**：用户微信生图"生成了但收不到"。生产实证链路：①07:44 首次生图任务 CHECKPOINT_COMMIT_FAILED（spool NULL content，已修已部署）；②07:56 后任务 succeeded 但 3 条 task_complete 交付全部 dead_letter（SEND_FILE_FAILED: context deadline exceeded）——spool 里 10 张图全在，一条没发出去；③"其他渠道也会同样失败"（DB 层/交付层渠道无关）。
- **Changes**：5 个提交 433b7c7/260a59d/3a31dd4/cf081b0/1b93186——①poller `wxbot_client._upload` 超时拆分（connect=30s 覆盖连接+请求体发送，read≥50s）+ 2 次尝试 + 3s 退避（单次 /send ≤63s < Go 媒体预算）；②Go delivery 媒体发送预算 15s→90s（文本保持 15s）；③重试窗口 5min→30min（瞬时故障超 5 分钟不再必然死信）；④`send_image` 交付侧透明适配：超限静态图迭代转 JPEG ≤300KB（mkstemp 落可写 /tmp，只读 spool 卷下仍可用）；⑤**真正元凶**：buildPayload spool 分支漏设 `deliveryFile.relPath` → `mediaTypeForPath("")` 回退 "file" → 生成图全部走 file_item 原始 1.3-1.6MB 上传 → CDN 节流写超时 → 死信；补 `relPath: filepath.FromSlash(f.RelPath)` 后图片按 image 分类 → send_image 适配生效。验证闭环：真实 token + 真实 user id 端到端 3.9s 成功；重投 3 条死信（tasks.terminal_at 拨到 now 绕过过期窗口）→ 10 张图全部 acked 送达用户微信；26/26 交付 acked、0 死信、media_assets 10 条出站。
- **Insights**：
  - **"生成了但没发"要按交付链分层定位，不能只看任务状态**：worker 落盘成功 ≠ 捕获成功 ≠ 发送成功。本事故三层独立故障叠加（checkpoint NULL / 分类错 file_item / CDN 节流），任何一层都表现为"用户收不到"。
  - **spool 引用化（0057）后的交付路径从未端到端验证**：mediaTypeForPath 依赖 relPath，spool 分支漏设后全部走 file，而测试只覆盖了 content 快照分支——"改存储形态必须重跑全链路出站测试"。
  - **海外服务器 → 微信 C2C CDN 上传被节流**（实测 ~18KB/s 波动 + ~30s 断连，≤450KB 可活）：任何 >500KB 的微信媒体上传都会死；交付侧压缩适配（≤300KB JPEG）是唯一可靠解，GIF/视频仍受限（生成端 size 控制）。
  - **Go delivery 超时预算必须 ≥ poller 最坏路径**：15s 文本预算直接套媒体 = 必死信；且 poller 上传挂起会占线程（Go abort 不取消 poller 工作），要 poller 自限总时长。
  - **死信恢复操作**：手动重投 = tasks.terminal_at 拨 now + task_deliveries 重置 pending（无 admin 重投端点，操作有风险留痕即可；长期建议加 admin retry API）。
  - **部署校验**：`docker inspect` 的 StartedAt 可能误导，直接对比容器内二进制 md5 与镜像 md5 才是部署真值。

## [2026-08-14 | 交付链独立审查优化: 预算架空漏洞 + 死代码清理 + admin 重投端点]

- **Events**：对 08-14 事故修复 8 提交（1b93186→4272b04）做独立逐行审查。发现 A.5 预算漏洞：delivery 媒体 ctx 90s 被 `poller.Client` 单一 `http.Client.Timeout=15s` 架空——实际生效预算 15s，300KB 上传 14-17s 贴着边界，生产"26/26 acked"靠概率成功 + 幂等 client_id 兜底；另确认注释漂移（delivery_service.go 仍写"10s/33s"，实际 30s/63s）。
- **Changes**：
  - 预算链定稿：wxbot `_upload` 固定 (connect=30s 写, read=10s 读)×2+3s ≈ 最坏 85s < delivery ctx 90s < poller client 兜底 120s；client.go 拆 quick(15s)/media(120s) 双 http.Client，媒体预算唯一真值源=调用方 ctx。
  - 死代码清理：`_make_thumbnail`/`_STATIC_IMAGE_EXTS`（no_need_thumb 后无调用方）；WeChatAdapter 旧 send_file/send_image/send_video 双签名（无调用方）；process() 的 snapshotContent=false 分支 + root/relPath 字段 + deliverable_snapshot_{unix,windows}.go（161 行，spool 引用化后不可达）+ fifo helper 测试。
  - E2 admin 死信端点：`GET /v1/admin/deliveries` + `POST /v1/admin/deliveries/{id}/requeue`（AdminToken；仅 dead_letter 可重投；attempt_count 归零；error_code 置 REQUEUED 满足 CHECK 约束；attempt_token 重新生成满足 NOT NULL）——08-14 事故的手动 SQL 恢复有可审计替代。
  - C1 根前端：`build_done_text` 移除"生成文件: <服务器路径>"文本交付（对用户无用且泄漏）；AgentChatMixin.can_send_media=False 默认 + 诚实提示；钉钉根前端补图片直发（oapi/media/upload → sampleImageMsg，与平台同构），文件/视频 fail-closed；QQ 根前端明确声明文本通道（官方分片上传需公网 URL/凭据实测，不做半成品）。
  - C2 收敛：FILE: 提取/解析收敛到无副作用新模块 `frontends/im_markers.py`（extract_files/strip_files/resolve_file_markers），chatapp_common 再导出；fsapp/wechatapp/tgapp 切换，坏占位符过滤+去重+存在性统一。
  - A2 补齐：飞书 `_send_image` 加 ≤10MB fit（原 20MiB 全局闸 vs 官方 10MB 缺口）；video 渠道级适配缺口入文档 §11。
  - 文档：IM_MEDIA_ARCHITECTURE §3.2 限制矩阵/§5.1 决策 A5-A10/§11 残余风险/§10 微信端到端已实测；SOP 补 §3.5 图片交付约束；PHASE_B plan §9.8 交付链闭环记录。
- **Insights**：
  - **预算三层必须显式包含**：poller 最坏路径 < 调用方 ctx < client 兜底，任何一层缺了都会"看起来修好了"。单一 http.Client.Timeout 是隐形上限，审查必须核对调用链上每个 http client 的 Timeout。
  - **删死代码前 grep 测试引用**：snapshotDeliverable 的 FIFO 防护测试（审查 I7）是它存在的主要证据——防护语义已随 spool 引用化迁移到捕获侧（CopyFileFromBeneath O_NOFOLLOW）+ poller send_media os.path.isfile 守卫，测试一并删。
  - **写 store 方法时 DB 约束会教你设计**：attempt_token NOT NULL（requeue 要生成新 token）与 task_deliveries_error_payload CHECK（非 task_complete 行 error_code 非空 → REQUEUED 标记）都是测试先跑出来的，约束即文档。
  - **纯函数收敛要放无副作用模块**：chatapp_common 底部 import agentmain 有安装副作用，fsapp/wechatapp 不敢引——im_markers.py 零依赖, 收敛才成立。

## [2026-08-14 | 子代理复审(3 路并行): 逻辑语义/死代码/架构 — P1 重投窗口修复 + P2/P3 批量]

- **Events**：对优化后工作区开 3 路 fresh-context 只读 reviewer(语义/清理/架构)。语义与架构独立抓到同一 P1：admin 重投端点"假成功"——`RequeueDeadLetterDelivery` 置 pending 但窗口锚点仍是 `tasks.terminal_at+30min`，而 `DeadLetterExpiredDeliveries` 每 tick 先于 claim 执行；事故后数小时重投(端点设计主场景)的行 ~2s 内被原样打回死信，从未被 claim。
- **Changes**：
  - P1 修复：migration 0060 `task_deliveries.requeued_at`（DO 块幂等，重放测试先抓到 42701）；claim/死信/`retryDeadline` 窗口锚点统一 `GREATEST(terminal_at, requeued_at)`；domain.Delivery + admin 视图 + OpenAPI 同步；新集成测试 `TestDeliveryRequeueSurvivesExpiredRetryWindow`（terminal_at 拨回 2h 前 → requeue → 清扫不打回 → 可 claim）。
  - P2-1 预算算术：控制面 `_post`(getuploadurl/sendmessage) 显式 (10,10)——病理最坏 15+83+15=113s 超 ctx 90s 的口径修正为 ~103s 且诚实标注"Go 重试+幂等 client_id 兜底、线程占用有界"。
  - P2-2 clientFor 防呆：`TestMediaSendUsesMediaClient`（RoundTripper 替换断言媒体走 media client）。
  - P3：删基类 `BotAdapter.send_file` 死方法 + Feishu `send_stream_close_all`（均无调用方）；thumb 上传分支门控 `thumb_size and raw`（防服务端返回 thumb_upload_param 时主图重复上传超预算）；file_item len 复用已读 raw（删二次整读）；fit 注释 0.8^N→-0.15 档位修正；OpenAPI list 补 400；test fixture 恢复 sys.modules 原值；**`.pi/` 补 gitignore + `git rm --cached`（47faa02 曾误提交 10 个审查转录/mission 文件，可能含密钥）**。
  - 记录未修（P3 有意保留）：wecomapp 裸 extract_files 解析（fallback 语义差异）、dcapp 部分重复、无扩展名生成图→file 分类、maxDeliveryBackoff 5min 实际不可达（防御配置）。
- **Insights**：
  - **状态机改动必须测"与相邻机制的交互"而非单点**：requeue 单测全绿 ≠ 重投能生效——窗口清扫（tick 先行）与 claim 门禁才是行为真值；SQL 层测试补"窗口已过后重投→清扫不打回→可 claim"的集成场景。
  - **migration 加列必须 DO 块幂等**：重放幂等测试(TestMigration0053ReplayIdempotent 等)会抓 42701，先写 DO 块再跑测试。
  - **审查转录/子代理产物是密钥泄漏面**：.pi/subagents/artifacts/*.jsonl 含审查读取的完整文件内容，必须 gitignore（本次 47faa02 已误入库 10 个文件）。
  - **三路 reviewer 交叉验证价值**：语义与架构独立抓到同一 P1（重投窗口）、同一 P2（预算算术）——多角度一致 = 高置信；清理路抓到 .pi 泄漏是其他两路盲区。
