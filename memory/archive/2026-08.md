# GenericAgent Memory

> 仅追加，不覆盖历史。写入前先读当前内容。

## [2026-08-06 | init-project 初始化]

- **Events**：建立项目协作文件体系（此前仓库无任何规则/协作文件）。
- **Changes**：生成 `AGENTS.md`（canonical 规则）、`tools.md`（协作知识）、`memory.md`（状态快照）、`memory/archive/2026-08.md`；`.gitignore` 为 `memory/archive/` 补白名单。`memory.md` 的初始内容参考了最近 8 条 git 提交，用于恢复近期项目状态。
- **Insights**：根 `memory/` 目录是应用运行时记忆（L0-L4 + SOP，逐文件 gitignore 白名单），与协作 `memory.md`/`memory/archive/` 是两套体系，后续维护勿混淆。从现在开始，重要根因分析、调试洞察、关键决策和踩坑按月追加到本文件。

## [2026-08-06 | 全项目审查 + 14 项修复]

- **Events**：对 main@335b3bf4 全项目只读健康/安全审查（5 条并行审查链 + 2 个反证 reviewer），确认 11 项分级 findings；随后用户决策"去掉工具策略分级"并授权修复全部已确认问题。实施 14 项修复，全程在隔离 PostgreSQL 库验证。
- **Changes**：
  - D1 去分级：`tool_policies` 表/`users.tool_policy_version` 列删除（迁移 0046，注意迁移 SQL 必须自带 marker 表创建）；router 固定 `foundation.session-files.v1`；policy CRUD/分配 API 与存储层删除；OpenAPI 同步；契约测试断言同步。
  - D3 用户生命周期：BlockUser 事务内撤销全部会话；Login/ValidateSession 拒绝 blocked（含单测 + store 测试）。
  - D4 运行上限原子化：`ClaimNextTask` 事务内 advisory lock + 计数（`SetRunningTaskLimit`），跨实例不超卖。
  - D2 成功路径销毁时序：`sessionDraining` 标记在终态提交到销毁完成间阻止同 session claim（跨实例天然安全：接管即 generation+1 新容器）。
  - F1 凭据失败路径：新增 `WorkerRuntime.ExpireRunnerLease`（归还容量保留容器引用），scheduler 初始化失败改用它。
  - F3 lease 时钟统一：expires_at 由 `timezone('utc', now()) + make_interval(secs=>$n)` 计算，过期判定/列表/计数全部 DB 时钟。
  - F2 delivery fencing：claim 生成 `attempt_token`（md5(random()||clock_timestamp())），Ack/Retry/DeadLetter 必须携带（迁移 0047）。
  - D5+F4 根 Python：LLM 传输/HTTP/解析故障重试耗尽 → `LLM_FAILED` 结构化终态（worker 映射 TASK_FAILED）；`llmclient=None` 时显式错误终态不再线程崩溃。
  - F5 Web：Dashboard 改用 AuthContext 登录响应状态（不再调 admin pending 接口）；AuthProvider 保留 userId/status。
  - F6：`.tasks/sandbox-runner-refactor/SUBTASKS.csv` 从 HEAD 恢复误删 5 行 + 25 行全量规范化（字段含逗号必须引号）。
  - F7：新增 `test_route_contract.py` 自动比对后端路由 vs OpenAPI（新路由未同步 spec 立即失败；存量缺口白名单收敛）。
- **Insights**：
  - 动态策略双真值的根因是 2026-07-25 一次提交（271e0f9f）捆绑引入的"半成品"：管理 API 可创建策略但执行链只认静态 manifest。去分级比打通更务实——容器隔离（runsc + workspace 子路径）本身就是安全边界。
  - 成功路径先提交终态再销毁 Worker 的窗口是"释放串行槽"与"容器回收"解耦问题；失败路径 destroy-before-finalize 是对的，成功路径不能简单前移（destroy 失败不能让成功变失败），draining 标记是正确解。
  - `EnsureToolPolicyExists` 在 main.go 从未被调用（死代码随 D1 删除）；`CreateUser` 空密码 hash 是 scanUser 扫 NULL 崩溃的真实 bug（仅 CreateUser 的 RETURNING 缺 COALESCE）。
  - CSV 未转义逗号会静默吞行（csv 解析把后续行并入前一字段），比"列数错位"更危险——恢复必须从 git 历史取原始 25 行。
  - `go test` 用 `-r` 参数会触发 rg 的 replace 模式把匹配文本替换显示，审查时容易误读输出。

## [2026-08-06 | 审查 I-4 鉴权统一: 根因与设计决策]
- **Events**：审查发现任务端点走共享 dev-token 无 owner 校验，真实用户无法查自己任务；集成测试改造后暴露 pending 用户任务必然失败的隐藏缺陷（capability 在线校验要求 approved）。
- **Changes**：身份模型统一为用户（Bearer）；AdminToken 仅管理面；任务端点 owner 校验（session 归属 + RequesterID，越权 404）；提交门禁加 approved 校验；OpenAPI 32 条存量 gap 补齐并清空白名单；非ce 持久化分段追加、IM 聚合后台重试、死 fallback 移除。
- **Insights**：1) dev-token 是"管理员凭证"的历史命名误导，改名要全链路（env/header/Go 标识符/OpenAPI/测试/部署文档）且注意 git checkout 会丢未提交修改；2) 任务归属校验放 application 层（store.GetTask 被 scheduler 内部大量使用，不能加参）；3) 集成测试直插 user_sessions 需要 sha256-base64url(token) 与 Go hashToken 一致；4) OpenTestPool 全局互斥锁同测试二次调用死锁。

## [2026-08-06 | Round15 P2 真值源收敛: 设计判断与洞察]
- **Events**：Round14 报告 P2 六项（workspace hash 收敛/ctrl: JTI 进 proto/per-requester 命名/决策编号/活跃 claim 谓词/tools_schema_cn 去留）全部实施完毕并全量验证。
- **Changes**：domain.WorkspaceDirHash 唯一实现（删 sandbox 重复版 + checkpoint/application 裸 sha256 + 隐藏第5处 scheduler configDirFor；SandboxRoot 接口改 (string, error) 强制校验）；worker.proto 写入 ctrl: 约定并重生成双端绑定；PER_REQUESTER_RUNNING_LIMIT 改名全链路；SQL 谓词收敛为 postgres 包两常量（18 处替换）；平台 overlay 删 tools_schema_cn 死资产。
- **Insights**：1) "4 套 hash"实际是 5 套（审查漏了 scheduler configDirFor，grep sha256.Sum256 全仓才暴露）；2) ExecuteTask 不查 ctrl: 前缀是**有意的不对称**（loopback 回退 firstJTI 无前缀），契约注释必须写明"不对称是有意的"防止后人"修复"它；3) 活跃 claim 谓词实际 18 处而非 5 处，且 runner_leases 表有 status 列——多表 SQL 去前缀会 ambiguous column，收敛常量必须支持别名；4) task_lifecycle 容量门禁的 status 谓词**故意不含 lease 条件**，盲目统一成"status+lease"会绕过容量上限；5) tools_schema_cn 与 tools_schema 内容已全量漂移（9/9 工具描述不同），GA 只加载后者，平台拷 cn 是纯死资产；6) proto 注释会进入生成代码，改契约注释必须重跑 generate_bindings.py，否则生成产物与 proto 漂移。

## [2026-08-06 | Round15 P3/P4: 文档校准 + 分层/边界修复]
- **Events**：E 组文档校准（§9 阶段4 与最终决策对齐、worker 过时"复用 Runner"注释、bot-poller-auth 行号+/config、.tasks 16/17 行 status 修正、managed_agent 模块清单、psutil 死依赖）+ F 组（api 层常量上移 domain、UTF-8 截断收敛、心跳基线）+ 残余风险确认。
- **Changes**：domain/limits.go 8 个上限常量（postgres/api 引用，api 层不再因常量 import postgres）；domain.TruncateUTF8 单一实现（boundMsg/truncateBytes 删除，+3 单测）；TaskRunState.last_progress_at 以 put_task 时刻为基线（task_runner 构造传 time.monotonic()，修复启动后首轮长思考心跳被抑制）；150/120/300 三方契约写进 task_drain/llm-proxy transport 常量注释 + IMPLEMENTATION_SUMMARY 部署契约节；LLM_PROVIDER_ARCHITECTURE 记录 routing 竞争窗口为有意设计。
- **Insights**：1) boundMsg 是"固定上限截断"，收敛为通用 TruncateUTF8 时**调用点必须补 limit 参数**（跨文件调用点易漏，go build 暴露）；2) 心跳基线 bug 的根因是 monotonic 时钟初值 0.0 与进程 uptime 混用——初值应为"任务开始时刻"而非"进程 epoch"；3) provider revision 校验的竞争窗口是**有意的 fail-closed 设计**（409→TASK_FAILED 可重试），文档显式记录防止被当 bug 修；4) E2 报告的行号已漂移（task_ops/session_lifecycle 的"seed 继承"实为 checkpoint 恢复机制，不是过时注释——以代码实际语义为准，避免误删）。

## [2026-08-06 | Round16: GA 根项目首轮审查 + 平台遗留语义修复]
- **Events**：Round14 将 GA 根项目列为黑盒未审查；本轮首次深审 ga.py/agentmain.py/llmcore.py/agent_loop.py + 平台遗留项复核，发现 2 个真实缺陷（G1/G2）+ 平台 API 语义问题（P1/P2），并清掉 8 项 Round14 遗留未修项。
- **Changes**：G1 `_retry_or_exit` 删除 fatal 分支——3 次空响应/流异常/max_tokens 统一 LLM_FAILED（原非 fatal 返回空 dict，被 agentmain 当普通完成提交空结果 TASK_SUCCEEDED）；G2 `total_cd_tokens` 只累计本条消息增量（原把累积拼接的整个 user 字符串重复相加，O(n²) 虚高越过 9000 阈值导致工具描述每轮重注入）；P1 domain 新增 `ErrSessionAccessDenied`/`ErrWorkspaceNotFound` 哨兵，SubmitTask 错误分级 429/403/404/500（原全 500）；P2 validateSessionAccess 改用 `domain.ValidateWorkspaceKey` 严格解析（原 Sscanf 吞 trailing garbage，`personal:123abc` 被误判 uid=123 通过归属校验）+ team 只收 uuid（原整数 id 在 store `$1::uuid` cast 处 SQL 报错 500）；C3 task_terminal cancel/timeout 终态收敛唯一实现；C6 sandbox-manager help 与实现对齐；F5 docstring 修正；F6 TaskRunState 补齐 3 个 unwrap 显式字段；F7 ensure_session_dirs 改名；B5 domain.PersonalSessionKey 唯一实现；B8 cancelCall 移入 scheduler_worker.go；C2 poller 删 window>0 死分支（测试改挂 InboundCoalescingBuffer）。
- **Insights**：1) GA 侧"黑盒"不等于"无缺陷"——平台正确性依赖 GA 消费侧判定，G1 与 Round14 C2 同根（故障被当成功），修复必须两侧对称；2) Sscanf 宽松解析与 ParseInt 严格校验的语义分裂是"宽松入口+严格内部"典型，实测 `fmt.Sscanf("123abc","%d")` err=nil 才暴露；3) poller 死代码由 7 个测试驱动是"测试守护不可达分支"反模式——删除后测试改挂生产实现，覆盖不减；4) B3/C5 评估为有意遗留：B3 是 API 契约字段改名需联动 openapi/web（等契约窗口），C5 纯结构债可无限期拖。

## [2026-08-06 | Sophub 集成全链路梳理: 心智模型与已知缺口]
- **Events**：用户指出 fudankw.cn 是"需要用的 SOP 技能平台"，要求先弄清用法再判断文档；逐层核实 sophub 平台 API 文档(sophub.md)、backend-go infrastructure/sophub client、worker_sophub_proxy、runtime config 注入、worker sop_tool.py、原生 GA 根代码。
- **Changes**：无代码改动（纯调研记录）。确认: 原生 GA 根项目零 sophub 引用(黑盒无此能力)；平台集成是单路径——管理员经 `/v1/admin/sophub/binding` 绑定 key(密文存储) → runtime config 注入 `_platform_sophub` capability → worker 经 `/v1/worker/sophub/search|install` proxy 搜索/安装到工作区 `memory/sops/<remote-id>.md`(用户修改覆盖保护 + 字节上限 + capability 保护)，Runner 全程不持有 Sophub API Key。
- **Insights**：
  - **fudankw.cn 有两个用途勿混淆**：`fudankw.cn/sophub/` = SOP 技能平台(GA 运行时搜索/安装 SOP，平台已集成)；`fudankw.cn:9000/files/ga_install.*` = 自托管一键安装脚本分发(commit 4fbac39f "self-hosted install scripts"，脚本内部下载源是 `47.101.182.29:9000`，与文档域名不一致——观察项，属用户自托管配置)。
  - **sophub.md 的 Quick Start 是原生 GA 直连模式**(keychain 存 key、`/api/agents/register` 注册)——**对平台 Worker 不适用也不可达**，平台是 proxy 模式；两套心智模型并存易混淆，看文档时按"平台=proxy/GA 原生=直连"区分。
  - **平台侧只支持 single-file markdown SOP**(ServeInstall 强制 `single_file`+`approved`+markdown，bundle 403 `SOPHUB_NOT_PUBLIC`)；sophub 平台 2026-05-07 已上线多文件 bundle SOP。**有意的安全收窄(方案 §5.2)，非 bug**——但 bundle SOP 装不了是功能缺口，若要用需平台侧加支持。
  - **README 徽章链接 `https://fudankw.cn/sophub`(无尾斜杠)实测超时**，有效地址是 `https://fudankw.cn/sophub/`(带尾斜杠)；GA 原生 README 黑盒，改动需谨慎或反馈平台。
  - 平台 worker 内无 keychain/register/直连 sophub 残留(已 grep 确认)，不存在绕过 proxy 的第二路径。

## [2026-08-06 | 全项目健康清理(Round17): 范围收敛与执行]
- **Events**：用户对 Round16 后健康诊断(~40 项)要求"记录列计划逐个优化"；并行 5 域诊断发现后，用户两次修正范围：fudankw.cn 是需用的技能平台、GA 根项目是第三方黑盒一律不动。清理收敛到 tenant_platform 自有代码，10 任务全部 DONE 并提交 4 个 commit(e6fce4ad/0bba034d/07fdfe89/82e70e80)。
- **Changes**：backend-go 删 token_revoker.go 整文件(生产走 CapabilityStore.RevokeCapability)+12 死 store 方法+3 IsValid*+SessionConfigJSON/TransportConfigJSON+DeliveryPending/DeadLetter 常量+migrations 0031-0034 marker 残留+update-imports.ps1；worker/web 删旧 worker-python/Dockerfile+grace_seconds 死参数+legacy_import 空块+sop_tool document gateway 注释+web 4 无引用资源；文档校准(IMPLEMENTATION_SUMMARY Bearer→X-Platform-Admin-Token、bot-poller-auth /config 仅 POST、sandbox 方案 §1/§9 改已实施口径、8 份 superpowers 历史文档加废弃标注)；.tasks 进度真值刷新(12/15 DONE)。
- **Insights**：1) **GetRunnerLease 是"仅测试引用"但非死代码**——round10/13 关键不变量测试(release 清空容器字段/generation 语义)靠它断言，删前必须查测试是否用它守护回归；2) 死 store 方法删除后 import 清理是常见连锁(secret/token.go 的 subtle/binary、user_store 的 errors)；3) CSV 列索引(0-based)易错——status 是第 6 列(index 5)，Python csv 处理时先打印字段数核对；4) 用户范围决策(GA 黑盒/fudankw 保留)直接决定任务边界，诊断报告里的"P1"在用户语境下可能是有意设计(Round15 tools_schema_cn 保留决策同样如此)。

## [2026-08-07 | 微信绑定 404 排障: ILINK_BASE_URL 缺失导致路由不注册]
- **Events**：生产 console 管理端"获取微信二维码"报 404(POST /v1/admin/me/wechat-qrcode)；web 前端路径/契约/后端代码均正确，排查到 main.go 条件注册逻辑。
- **Changes**：compose.yaml platform 服务透传 ILINK_BASE_URL/ILINK_APP_ID/ILINK_CLIENT_VERSION；.env 配 `ILINK_BASE_URL=https://ilinkai.weixin.qq.com`(官方 iLink 网关，与 GA Core wxbot_client 一致)；仅 recreate platform 容器(env 变化触发，无需重建镜像)，接口恢复 201 返回真实二维码。
- **Insights**：1) **Go 1.22 ServeMux 未注册路由返回 404，方法不匹配才 405**——"接口 404 先查路由是否条件注册"，本类路由都挂在 `if s.xxxSvc != nil` 下，服务依赖 env 开关创建时极易整组静默消失；2) `--ilink-base-url requires BOT_TOKEN_KEY` 是显式安全护栏，BOT_TOKEN_KEY 已在 .env 满足；3) 微信绑定业务前提是**微信官方 iLink Bot 应用资格**(扫码登录的是 bot 微信号，非普通用户)，无应用时二维码请求会在 iLink 侧失败——这是业务准入，不是平台 bug。

## [2026-08-08 | MCP Gateway 架构重构: 从"stdio 支持"到统一架构收敛]
- **Events**：3b834bb 提交的 stdio transport 支持经审查发现提交级编译失败(缺 types.go/Server/CatalogSource/CatalogError/DefaultIdleTTL/DefaultCrashBackoff 定义)+三处生产链路缺陷(proxy 不转发 Mcp-Session-Id 致 gateway 第二跳 400；gateway 自造 JSON-RPC id 致第二个客户端错位；stdio URL 语义在快照层与 proxy resolve 层不一致)；用户要求"不要在意工程量，要最优架构"。在 docker 内实测编译 + 真实 DB + 真实子进程完成验证。
- **Changes**：新增 `internal/mcpgateway/types.go`(类型/常量)；重写 server.go/stdio.go——**gateway 会话无状态化**(删 sessions 表/校验/会话头，进程是唯一会话状态所有者，gateway 重启不破坏客户端)；JSON-RPC id 原样透传(initialize 缓存响应按客户端 id 改写)；**进程池** max_instances 生效(全忙且有余量才扩容)+指数退避+熔断(8 次/探活 30s)+revision 热更新；**子进程白名单环境**(PATH/HOME/TMPDIR，不继承 DATABASE_URL——原实现把 DB 凭据直接送给子进程)；readLoop 退出关 lines(进程死后请求立即失败而非等满超时)；proxy 转发白名单 +Mcp-Session-Id、双 http.Client(直连 30s/gateway 310s)；`domain.MCPServerGatewayURL` 单一合成(快照+resolve 共用，stdio url 强制空)；openapi 补 /v1/worker/mcp 路由缺口(既有)+MCP schema 新字段；web 表单 transport 切换；文档 §8 记录"http 迁 gateway 不做"(egress 约束矛盾)。测试: gateway 12 个(含多客户端 id 回归/进程池/熔断/热更新/环境隔离)、proxy 会话往返、domain 校验。
- **Insights**：
  1) **协议层代理必须透传客户端 id**——gateway 自造 id 只在"单客户端且计数恰好对齐"时碰巧能跑，第二个客户端必然错位；单测不断言响应 id 就发现不了。JSON-RPC id 是协议不变量，适配层只能透传。
  2) **"无会话"比"会话恢复"更优**——stdio 子进程内存里才有真正会话状态；gateway 模拟会话校验纯属多余状态，去掉后 gateway 重启/进程重建对客户端完全透明(真实 E2E 验证: 重启后同一客户端直接调用成功)。
  3) **锁序 g.mu→pool.mu 必须单向**——spawnLocked(持 pool.mu)内取内部 id 用 atomic 而非 g.mu，否则与 poolFor(g.mu→pool.mu) 死锁；race 测试抓住。
  4) **spawn 子进程必须显式白名单环境**——exec.Cmd 默认继承父进程环境，gateway 的 DATABASE_URL 会直接流进 stdio 工具进程，违背"子进程无凭据"基线；这是原提交的安全漏洞。
  5) **退避/熔断的计数语义要分开**：退避窗口内失败只推进计数不刷新窗口(否则请求风暴永久重置退避)；熔断期请求不得刷新探活时刻(否则永不探活)。
  6) **既有契约缺口**：/v1/worker/mcp/{server_id} 从未进 openapi 而 KNOWN_SPEC_GAPS 已清空——说明该测试在 Round14"补齐"后有回归盲区，新增内部路由同样必须进 spec。
  7) **mcp_servers.url 是 NOT NULL**——stdio 的"url 为空"存的是空字符串不是 NULL(domain 校验 TrimSpace)，migration 0049 未改列约束，INSERT NULL 会失败。
  8) **gofmt 是存量债**：cmd/platform/main.go 等大量文件本来就不 gofmt(CI 无门禁)，修文件时别顺手 gofmt -w 整文件(引入噪音 diff)。

## [2026-08-08 | 服务器部署: 重构后首次构建运行]
- **Events**：MCP Gateway 架构重构完成后按规范部署到本机服务器（4 vCPU/3.8G 内存/无 runsc）。
- **Changes**：`make build` 构建 7 镜像（含 mcp-gateway——重构前该镜像编译不过从未构建成功）；`.env` 基于 `.env.example.dev`（本机无 runsc，显式信任开关 ALLOW_RUNC=1 + 可变 tag）；`GA_RUNNER_MAX_ACTIVE=1`（内存 3.8G 偏小）；随机密钥全部生成；`docker compose up -d` 7 服务全 healthy；管理员 API 注册并启用 pandoc（stdio）server；gateway 白名单实时同步；容器内直连验证 initialize/tools/list/tools/call md→html 真实转换成功。
- **Insights**：1) mcp-gateway 无宿主端口映射是**架构正确形态**（仅 internal 网络 + platform proxy 转发），部署验证用 `docker exec` 容器内直连，不要为调试加端口映射；2) mcp-pandoc 工具参数名是 `contents`/`output_format`（schema 为准，勿凭印象传 format）；3) 本机内存 3.8G 跑 7 容器 + runner 偏紧，Runner 并发降到 1；4) Web 保持 127.0.0.1:8088，远程访问走用户自配隧道（cloudflared 等），勿暴露 8088/55432。

## [2026-08-08 | Sophub 绑定与搜索修复 + 双镜像 tag 部署坑]
- **Events**：用户提供 sophub Agent UID + API Key 要求配置；绑定成功后管理搜索返回空——实测 fudankw.cn/sophub 列表与详情 API 均不返回 is_public 字段，平台 filterInstallableSOPs 的 IsPublic=true 条件永假，搜索/安装链路全断。另发现服务器上存在旧 genericagent-*:local 镜像（用户此前构建），compose 一直引用它们，本次新构建的 platform:local 从未被使用。
- **Changes**：sophub 绑定 PUT /v1/admin/sophub/binding（agent_uid 远程校验回填，与用户提供一致）；domain.SophubRemoteSOP.IsPublic 改 *bool（区分"缺失"与"显式 false"）+ 新增 Source 字段；application 新增 isSophubSOPPublic（is_public 缺失时以 source=community 为公开信号，显式 false 仍拒绝）——搜索与 FetchRemoteSOP 准入共用；回归测试 TestSophubServiceSearchFiltersInstallableOnly；Makefile 镜像 tag 映射修复（platform→genericagent-platform:local，ga-runner 例外）——历史坑：按 Dockerfile 文件名打 tag 与 compose 引用名不一致导致构建产物不被使用；服务器镜像 retag + force-recreate 全部容器切新代码；清理 dangling 183MB。
- **Insights**：1) **sophub 远程 API 的"公开"信号是 source=community，不是 is_public**（字段从未存在）——平台此前准入条件永远不满足，绑定/搜索/安装全链路实际不可用，属既有契约 bug；2) **Makefile 打 tag 必须与 .env.example 的 GA_*_IMAGE 对齐**，否则 compose 静默用旧镜像——"构建成功 ≠ 部署成功"，部署后必须核对容器内二进制/行为；3) Go bool 无法区分 JSON 字段缺失与显式 false，需要三态语义时用 *bool；4) 本机无 make 命令（用户无免密 sudo），Makefile 语法用 golang 容器内 make -n 验证。

## [2026-08-08 | 微信绑定 404/504 回归修复: ILINK_BASE_URL 丢失 + iLink 状态长轮询]
- **Events**：生产 console 管理端"获取微信二维码"再报 404（POST /v1/admin/me/wechat-qrcode）；同日状态轮询报 504（35s 超时）。08-07 曾用 .env 配 ILINK_BASE_URL 修复过 404，08-08 重建时 .env 基于 .env.example.dev 重写把该变量丢了（两个模板当时都没有 ILINK 项）——部署回归。504 则是新发现的深层问题：iLink get_qrcode_status 是长轮询，QR 待扫时挂连接 ~30s 才回 wait；Go 客户端 20s 超时 × 3 次重试 ≈ 40s 超过 cloudflared 前置 ~35s 超时 → 504 → 前端 catch 后 stopPolling，绑定流程直接死。
- **Changes**：① 后端 admin.go 微信 QR 四路由改无条件注册，服务未配置（ILINK_BASE_URL 空）时返回 501 FEATURE_DISABLED（认证语义不变：外层 auth 先 401），不再裸 404；② ilink client 增加 `StatusRequestTimeout`（默认 8s）封顶每次 status 尝试，超时按"无变化"不重试（isRetryableStatusErr 对 DeadlineExceeded 返回 false）；③ wechat_binding_service.PollStatus 超时时返回 DB 最近状态 + nil error（200 wait），真实错误（4xx/ret!=0）仍 502；④ 前端 BindingPage 对 FEATURE_DISABLED 显示"微信绑定功能未启用"；⑤ 部署：live .env + .env.example.dev + .env.example 均补 ILINK_* 三项（模板缺口是回归根因），recreate platform 容器验证 201 建码 + 8.1s 内 200 wait；⑥ 回归测试：client 2 个（超时不重试/谓词）+ service 1 个（超时返回最近状态）。
- **Insights**：1) **"功能未启用"与"路径不存在"要区分**——条件注册的 404 无法表达前者，契约已声明的路由应无条件注册 + 结构化错误（501），前端才能给用户可理解的提示；2) **第三方长轮询接口必须按轮询频率设定尝试超时**——前端 3s 轮询根本不需要 30s 长轮询，超时即"无变化"，重试只会叠超时打爆前置代理；判断重试性要区分"瞬态网络错误"（重试）与"预期稳态超时"（不重试）；3) **部署模板缺项是隐藏回归源**——compose 透传了变量但两个 .env.example 都没列，任何"按模板重建 .env"都会静默丢功能，模板必须与 compose 引用项对齐；4) 验证长轮询假设的方法：直连 curl 计时（wait 态 31s vs expired 态 0.6s 即坐实）。

## [2026-08-08 | ga-runner 镜像命名漂移(WORKER_START_FAILED) + 架构收敛]
- **Events**：微信 bot 绑定成功后发"你好"，任务 WORKER_START_FAILED：ensure runner 409 RUNNER_ENSURE_FAILED，docker create 报 "Unable to find image 'ga-runner:local'"。根因：08-08 部署统一 genericagent-* 前缀只做了一半——镜像被 retag 成 genericagent-ga-runner:local，但 Makefile build 特例/runner-digest/compose 默认值/.env 的 GA_RUNNER_IMAGE 仍是旧名 ga-runner:local，sandbox-manager 启动时又不校验镜像存在，错误拖到任务运行时才暴露。用户明确要求区分架构问题与细节问题：这是配置真值源分散 + 缺启动自检的架构问题，按架构修复而非打补丁。
- **Changes**：① 命名统一 genericagent-* 前缀（Makefile build 删 ga-runner 特例、push/pull 补前缀、runner-digest 自动写 .env；compose.yaml 139/270 默认值 + main.go 默认值同步）；② compose.yaml ga-runner 服务 image 固定构建产物名（与运行时引用 GA_RUNNER_IMAGE 解耦——digest 模式 compose build 不再报 "build tag cannot contain a digest"，历史坑根治）；③ **sandbox-manager 启动 fail-fast 校验镜像**（ImageExists，缺失 exit 2 + 提示 make build && make runner-digest）；④ docker create 失败时诊断镜像（AvailableImages 列出本地可用镜像拼进错误，Platform 侧直接可操作）；⑤ .env 切 digest 引用 + .env.example* 同步；⑥ 端到端验证：模拟消息 → task succeeded → AI 回复真实生成并投递（messages 表 3 条闭环：inbound/回执/回复）。
- **Insights**：1) **"配置漂移"的根因是真值源分散**——同一实体的名字在构建脚本/compose 默认值/env/代码默认值四处各写一遍，任何一次"只改一处"的部署都会静默漂移；收敛方式：构建产物名固定为唯一真值（genericagent-*），运行时引用独立 env 控制且默认值与构建名一致；2) **fail-fast 自检放启动路径**：镜像/网络/密钥等外部依赖在进程启动时校验（exit 2 + 可操作提示），错误发生在"部署时"而非"任务运行时"，信噪比完全不同；3) **错误信息要可操作**：docker create 的原始错误没有配置上下文，附上"配置引用 + 本地可用列表 + 修复命令"才可指导排障；4) compose 的 image 字段同时承担"构建产物名"与"运行引用"两个职责，digest 模式下必然冲突——解耦后 compose build 恒产出固定 tag，运行时用 digest；5) 微信链路闭环验证方法：admin token 直接 POST /v1/router/messages（bot_uuid/ilink_user_id/message_id/text）模拟真实消息，查 tasks/messages 表即可端到端验证，不必等真实微信流量。

## [2026-08-08 | 微信链路三问题: 聚合乱序 bug / 回复 md 泄漏 / Pandoc 架构定位]
- **Events**：用户反馈: ① 发"文件+文字"回复两个(聚合配置 2.5s 看似没生效); ② 回复文本是 md 样式(星号/反引号); ③ 生成的 docx "没有真正排版", 问 Pandoc 是否没用、是否有配套 SOP。逐项排查: ① 聚合配置链路本身完整(写 DB + 热推 poller + 后台重试 + platform 启动下发 2500 均正确), 真 bug 在 `_can_coalesce` 要求 `current_at >= previous_at` 严格递增——微信"文件+文字"同时发送时 iLink 批次内顺序/时间戳可能乱序, 严格递增把同一发送动作拆成两个任务(复现测试 test_coalesces_reversed_batch_order FAIL→修后 PASS); ② worker 回复文本天然带 md 标记(GA 原生风格), 微信不渲染 → 平台 delivery 层新增 `cleanIMMarkdown` 全渠道降级(**加粗/反引号/删除线/链接/标题/表格分隔线→纯文本); ③ 会话记录显示 worker 调了 3 次 MCP pandoc 全失败(stdio server 在 gateway 容器, 与 runner 工作区文件系统隔离, contents 模式也无法落盘)——**架构定位: 文件型转换工具不该走网络 MCP, 应作为 runner 镜像本地能力**, ga-runner 新增 pandoc 2.17, 删除 mcp_servers 的 Pandoc 注册。
- **Changes**：poller `_can_coalesce` 改绝对间隔(`abs(current_at-previous_at) <= window_ms`) + 2 个乱序回归测试(22 tests 全过); delivery_service.go `cleanIMMarkdown` + 单测; ga-runner.Dockerfile apt 装 pandoc + 注释"勿恢复 MCP pandoc"; 删除 mcp_servers id=1(Pandoc stdio); 新增 `memory-template/wechat_delivery_sop.md`(微信交付排版 SOP: 纯文本铁律/pandoc 优先/python-docx 兜底/排版质量门槛/正反例) 并同步复制到现有工作区 memory/; 重建 ga-runner(新 digest b93bebf, make runner-digest 自动写 .env)+ platform + 重启, bot 恢复轮询。
- **Insights**：1) **"配置没生效"先验证配置链路本身**——热推/重试/启动下发都对了, 最后定位到执行层(合并谓词)的语义 bug: 窗口语义是"时间接近即合并", 实现却要求严格递增, 语义与实现漂移; 2) **文件型 MCP 工具的文件系统隔离是架构性不可用**——stdio server 容器与 runner 工作区隔离, 传路径不可达、contents 往返无法落盘; 文档转换应回归"镜像本地能力"(python-docx/pandoc 同层), 平台侧 MCP 只留给真正远程的服务(exa 等); 3) **IM 渠道的 md 降级应在平台 delivery 层做**——依赖 LLM 自觉(SOP)不可靠, 平台侧正则清洗是兜底(与 SOP 双保险); 4) "回复两个"还有一部分是**旧任务延迟完成**(LLM 生成 42KB docx 耗时 4 分钟)与新任务完成通知混叠——聚合修复后文件+文字合一, 混乱面缩小; 5) 排查聚合是否生效的实证路径: tasks.created_at ≈ 消息 create_time 说明消息被立即 flush(窗口=0/谓词不合并), 这是"未聚合"的可靠信号。

## [2026-08-08 | 审批后任务提交 500: 普通用户无 personal workspace + 测试清库事故]
- **Events**：用户注册 zhanglu 绑定微信 Bot，管理员审批后用户端仍显示"待批准"，再发消息无反应。DB 实查用户已 approved、bot active，poller 日志显示 `webhook post failed (500) submit task: workspace not found: "personal:1000"` 无限重试(60s 退避)——审批只更新 users 状态，从不创建 workspaces 行(bootstrap 用户由 EnsureAdminContext/EnsurePlatformAdminUser 建，普通用户注册→审批全链路无任何建 workspace 路径)，提交任务必 500。
- **Changes**：① `user_store.go` ApproveUser 事务内幂等创建 `personal:<uid>` workspace(ON CONFLICT (session_key) DO NOTHING)，volume_id=domain.WorkspaceDirHash(session_key)(满足 null_volume_requires_loopback 约束，与运行时共享卷哈希一致)；② 新增 migration 0050 为存量 approved 用户补数据(gen_random_uuid + sha256(session_key) hex，PG13+ 内置，无需 pgcrypto)；③ 端到端验证程序(注册→审批→workspace 行存在且哈希正确)；④ 前端"待批准"是 AuthContext 登录响应快照，重新登录即显示已批准(非 bug)。
- **Insights**：1) **"普通用户审批通过"是进入 approved 的唯一路径，生命周期不变量应挂在状态迁移事务里**——workspace 这类强依赖资源随状态迁移创建，而不是依赖调用方自觉；2) **迁移只做 DDL+marker 无业务行先例，但存量数据修复(migration 0050)模式可行**——INSERT...SELECT + marker，幂等；3) **前端状态快照(登录时签发)与 DB 实时状态会漂移**——审批类外部变更后需重新登录/刷新会话，文档化即可；4) **【事故】在共享开发库跑 postgres 包 go test 会清空全部业务数据**——测试 drop 全部 foundation 表重建(users/workspaces/tasks/bots 全清，marker 表保留故迁移不重跑)，测试残留 empty-pw-user；恢复=清理残留+重启 platform(EnsurePlatformAdminUser 自愈 admin+workspace)，但普通用户/bot token 密文(tokens NOT NULL)无法恢复，用户需重新注册+重新扫码绑定；教训: **后端测试必须连独立测试库(TEST_DATABASE_URL 指向独立实例)，或先备份再跑**；5) go test 包间 truncate 互踩是已知坑(AGENTS.md)，但"测试清空整个库"危害更大——postgres 包测试的 setup 是 drop 重建，不是 truncate 单表。

## [2026-08-08 | workspace 生命周期架构收敛: 注册即建 + 唯一实现 + 不变量测试]
- **Events**：用户反馈"若是架构设计问题不要打补丁"。审视上一条修复(ApproveUser 内手写 INSERT 建 workspace)确实是第三个散落的建行点(admin bootstrap / team×2 / approve), "approved 用户必有 workspace"是隐式约定, 且 invite 等未来"创建用户路径"仍会漏。收敛为架构规则: **users 行 ⇔ personal workspace 行**(与用户状态无关, 任务提交的 approved 门禁在 application 层)。
- **Changes**：① 新建 `postgres/workspace_store.go`——`insertPersonalWorkspaceTx` 成为 personal workspace 行的**唯一 SQL 实现**(含不变量契约注释), 另提供 `personalWorkspaceVolumeID` + `WorkspaceIDByKeyTx`; ② **注册即建**: CreateUser/CreateUserWithInvite 同事务建行(volume_id=WorkspaceDirHash), ApproveUser 回退纯状态迁移(移除上轮 INSERT); ③ admin bootstrap ensureContextUser 复用同一 helper; ④ 迁移 0050 语义改为**全量用户**补行(不只 approved——存量 pending 升级后审批才不缺行); ⑤ 新增 `user_workspace_lifecycle_test.go` 4 个不变量测试(注册建行/invite 建行/审批不重复建行且行不变/bootstrap 行语义); ⑥ 独立测试库 `genericagent_test`(教训落地: 测试不再指向业务库)。
- **Insights**：1) **资源行创建挂"用户创建"而非"状态迁移"**——任何创建用户的路径自动获得资源, 审批零副作用; 若挂在状态迁移, 每新增一条"变为 approved"的路径都要记得补, 必然漏; 2) **不变量与用户状态解耦**: workspace 行是账本(session_key 映射), 不是能力授予, 能力门禁在 application 层——这样不变量最简单(行存在即成立), 不必为 pending 用户特判; 3) **"唯一 SQL 实现 + 调用点注释引用"是收敛散落 SQL 的可行模式**: 4 处 INSERT 减为 1 处实现, 其余全走 helper; 4) 迁移语义"补存量"要与新不变量对齐: 只补 approved 会漏存量 pending(他们审批时新代码已不建行); 5) 验证节奏: 单测(隔离库) + 临时 main 程序(业务库, 随机前缀用户名, 测完删)双保险。

## [2026-08-08 | 文档工具链按官方能力重审: pandoc 3.8.3 + LibreOffice + 中文字体]
- **Events**：用户质疑"pandoc 官方能处理各种文档，为何你说不行，要看官方文档对比做决定"。查官方手册: pandoc 3.8 起官方新增 xlsx/pptx/asciidoc 输入(3.8.3 changelog 实证)，而镜像里 Debian apt 版 2.17 没有——原配 2.17 确实是旧版。PDF 输出官方明确需 pdf-engine(默认 pdflatex，可 weasyprint 等)。决定升级 pandoc 官方二进制 3.8.3 + 补 LibreOffice(老格式/高保真 PDF) + fonts-noto-cjk(中文渲染必需)。
- **Changes**：① ga-runner.Dockerfile: apt pandoc 2.17 换官方 3.8.3 tar.gz(GitHub releases, curl 下载解压 /usr/local)；apt 加 libreoffice-writer/calc + fonts-noto-cjk + curl；② 实测矩阵(全部容器内验证): pandoc 3.8.3 文本类互转正常；**xlsx 输入实测双 bug——openpyxl 生成的 xlsx(rels 绝对路径 Target=/xl/...)解析成 xl//xl/... 必失败，LibreOffice 生成的虽能读但中文乱码；官方最新 3.10.1 同样未修(下载实测)**；LibreOffice 7.4.7: docx/xlsx→pdf 中文完美、.doc/.xls 老格式读写转换正常、xlsx→csv 需 FilterOptions :44,34,76 指定 UTF-8(默认乱码)；**LibreOffice xlsx→docx 直接转换失败(0xc10, Calc→Writer 过滤不可靠, 业界共识)**；③ document_conversion_sop.md 重写: 工具矩阵按实测标注可靠/不可用场景 + 格式决策路线表 + LibreOffice 配方(UserInstallation 独立 profile 防锁冲突) + 9 条实测坑；④ 同步现有工作区 memory/ + 重建镜像(1.47GB)。
- **Insights**：1) **"官方宣传的能力"必须按实际部署版本验证**——apt 版 2.17 与官方 3.8 能力差一个 xlsx/pptx 输入，升级要查官方 changelog 而不是搜博客；2) **pandoc 3.8.3 与 3.10.1 的 xlsx reader 都是坏的**(openpyxl rels 绝对路径 bug 在 xlsx 解析生态是通病: QXlsx/CoreXLSX/PHPOffice 同款)——xlsx 处理一律 openpyxl/LibreOffice，SOP 明确"别浪费时间试 pandoc"；3) **PDF 生成的正确路线是 LibreOffice 渲染式**(docx→pdf 中文提取验证全对)而非 pandoc+LaTeX(重)或 weasyprint(未装)；4) **老格式 .doc/.xls 只有 LibreOffice 能读**——这是加它的核心理由，体积 600MB 换能力覆盖；5) 验证方法升级: 每个工具能力都"生成→转换→读回"闭环(openpyxl 生成 xlsx→pandoc 读→失败→LibreOffice 读→成功)，不轻信单点测试。

## [2026-08-10 | IM 多渠道架构调研: 群聊能力矩阵 + 对话单元分桶模型定案]
- **Events**：微信对接完成后评估接其他 IM 的架构准备度。梳理确认：GA 原生模型 = put_task(source) 多前端入口 + 单 agent 串行 + 全局 abort/history（"原生支持多前端提交，不支持多会话隔离"，会话秩序留给上层）；frontends 封装层存在两代风格债（qq/dingtalk/wecom 用 AgentChatMixin 继承式，wechatapp/tgapp 回调式/流式特化各自重写清理/命令/消费循环）。用户连续两轮澄清业务模型并纠正认知：① 隔离粒度是租户（workspace），不是 IM 也不是个人；团队=一个租户，多人共享；同租户跨渠道共享工作空间（"GA 原生单用户所有数据只有一份"）；② **微信 Bot 只能个人自用**（iLink 绑定用户自己微信号，仅私聊不能加群，不存在"好友A/B"多对话对象）——此前按"微信+好友"分桶是错误假设；③ 群聊渠道（飞书/QQ/企微/钉钉/TG）按群分会话，群内多人共享群会话；④ /new 清当前渠道（对话单元）桶。
- **Changes**：① 渠道能力矩阵代码实证（微信仅私聊单桶；QQ `group_openid`、钉钉 `conversation_type=="2"`→`group:{id}`、企微 `chatid`、TG、飞书可群聊）；② 业界对照调研（Coze 官方文档："多会话核心特性 = 文件共享，上下文隔离"；会话隔离层级 = 渠道+账号+Bot+session_name；群 = 独立对话单元；工作空间不分群/个人）——印证用户判断"群聊和个人工作空间在一起"；③ 数据模型定案：workspace 全共享 + 对话单元（channel:chat_id）分桶隔离 + /new 清当前桶；④ 落盘四处：新增 `tenant_platform/docs/IM_CHANNEL_ARCHITECTURE.zh-CN.md`（设计真值：模型/矩阵/落地路径/未决项）；`tools.md` 增"IM 渠道能力"章节（微信个人自用单桶等稳定事实）；`memory.md` 关键决策 + 最近活跃窗口；本月归档。
- **Insights**：1) **会话隔离粒度必须跟业务模型走**——"租户级共享、对话单元级隔离"是用户真实模型，业界（Coze）同款；通用做法是"文件共享+上下文隔离"，不按群/个人分工作空间；2) **微信 iLink Bot 是个人号自用模型**（1:1 绑定、无群、单对话单元），不能按客服机器人（多用户/多群）设计，桶 key 由 chat_id 决定即可天然单桶，无需特判；3) 平台侧架构已就位（workspace/Runner 容器级隔离 + ChannelBinding 渠道绑定通用模型），落地 = TaskEnvelope 加 channel/conversation_id + worker checkpoint 按桶存取 + router 透传，GA 核心零改动（memory.md Round17 黑盒约束兼容）；4) 封装层债务（IMAdapter 统一两代风格）与平台侧分桶模型是两条独立改进线，概念上 platform channel == 单机 source，勿混为一谈；5) 设计讨论中"表达抽象问题"（如"团队共享规则"）不如先拍板数据模型，模型定了问题自动消解。

## [2026-08-10 | IM 多渠道架构落地: 对话单元分桶 + /new 桶级化]
- **Events**：微信对接完成后按 IM_CHANNEL_ARCHITECTURE.zh-CN.md 定案落地分桶。三批提交: ① 契约 TaskEnvelope.conversation_id=14(复用 source 为渠道维度, worker 哑管道确认——恢复/提交选桶天然在 Go 控制面 CurrentRestorePoint/Prepare); ② migration 0051 + Go 全链路(恢复双路径: 空=存量 current_snapshot_id 指针, 非空=按桶取 MAX(generation) committed); ③ /new 桶级化 migration 0052(conversation_resets 表, workspaces.reset_at 退役)。期间用户纠正: frontends 单机封装层无使用价值(多租户项目), 任务 6 砍掉。
- **Changes**：① TaskEnvelope+conversation_id + 双端生成 + 契约测试 22 passed; ② migration 0051(ADD COLUMN IF NOT EXISTS + 自建 marker 幂等惯例——TestMCPHeaderCleanup 暴露并修复; migrationFiles() 补齐 0050/0051 既有列表债); ③ checkpoint_store Prepare 带桶键/CurrentWorkspaceSnapshot 双路径/CompleteSucceeded 仅默认桶推进指针(端到端测试抓出跨桶覆盖缺陷); ④ 0052 conversation_resets 按桶 upsert/取消/判定/消费(SubmitTask fresh 按桶、ResetWorkspaceForNewSession 按桶取消、WorkspaceIsFresh 按桶、CompleteSucceeded 按桶 DELETE); ⑤ 新测试: conversation_bucket_test.go(两桶隔离 6 不变量) + conversation_reset_test.go(桶级 /new 隔离/按桶取消/消费); ⑥ 全量 go test + race 5 关键包全绿(本机 ga-ci-pg 容器 5433/genericagent_test)。
- **Insights**：1) **"选桶"位置决定改动面**——先调研恢复/提交链路再设计: worker 是哑管道(给 ref 读 ref), 分桶全在控制面, worker 零改动; 2) **端到端测试抓真实缺陷**: CompleteSucceeded 无条件更新 workspace 指针在单桶时无害、多桶必串——按桶推进指针是分桶落地的最隐蔽坑; 3) **generation 是 workspace 级单调递增**, 桶内取 MAX 即最近恢复点, 不需要 per-bucket 指针表(fresh_session 已覆盖"清桶"语义); 4) **迁移必须幂等 + 自建 marker**(项目惯例), 否则 EnsureSchema 重放即炸; migrationFiles() 与 pendingMigrations 双列表都要同步, 0050 当年只加了一边是既有债; 5) 本机 Docker 有 ga-ci-pg(5433/genericagent_test) 可直接跑 DB 测试, 无需另起环境。

## [2026-08-10 | IM 渠道绑定（im-channel-binding epic）]

- **Events**：按 `tenant_platform/docs/IM_CHANNEL_BINDING.zh-CN.md`（用户已拍板、零未决）接入飞书/钉钉/QQ 渠道 + Web 微信绑定页改造为通用渠道绑定页；顺带清 memory.md B3 命名债（契约字段 ilink_user_id → channel_account_id）。6 个子任务全部 DONE。
- **Changes**：
  - migration 0053：`bots` RENAME `channel_configs`（FK 自动跟随）+ `channel_type` 列（存量微信默认 'wechat'）+ 唯一索引 (owner_id, channel_type) + `token_*`→`config_*`（凭据 JSON 密文）+ state 加 disabled；migrations.go 三个列表同步。
  - Go：`domain.Bot`→`ChannelConfig` 全库改名；新 API GET/PUT/DELETE `/v1/me/im-bindings` + admin 同款（OpenAPI 同步，test_route_contract 门禁）；Router 入站 Source=ChannelType、ConversationKey=ConversationID，微信身份校验仅限 wechat，新渠道属主直判；delivery 回复目标=conversation_id；/new /stop /status 按当前桶。
  - Poller：`poller_server.py` 重构为 BotAdapter 注册表（WeChatAdapter 迁移 + FeishuAdapter lark-oapi WS + DingTalkAdapter dingtalk-stream + QQAdapter botpy WS，SDK 惰性导入）；/start 契约 channel_type+config_json；配置热更新复用热推机制（PUT/DELETE → poller start/stop）。
  - Web：菜单"渠道绑定"；BindingPage 微信扫码卡片保留 + 飞书/钉钉/QQ 凭据表单卡片。
- **Insights**：
  - **0003 的 marker 就是 bots 表本身**：0053 RENAME 后 to_regclass('bots') 为空会让 EnsureSchema 把 0003 当作未应用而重放（重建空 bots 表）——0053 必须重建一个仅作 marker 的 stub `bots` 表（与 teams/platform_commands/users 早期 marker 表同惯例）。这是表改名类迁移最隐蔽的坑。
  - **migration 幂等模板**：RENAME 无 IF NOT EXISTS，用 DO 块条件执行（0052 先例）；列改名查 information_schema.columns；约束改名查 pg_constraint；marker 表 CREATE IF NOT EXISTS + INSERT ON CONFLICT。
  - **BotManager.start 持锁构造 adapter 会死锁**：WeChatAdapter 构造回调 coalesce_window_provider → 同一把非重入锁。构造必须移出锁区间（先检查注册表再构造再回写，并发重复 start 停掉后建者）。
  - **upsert 前捕获旧配置**：ON CONFLICT DO UPDATE 会覆盖 bot_uuid，事后无法再取旧 UUID 停止其 poller 会话（与微信 QR 重新绑定同模式）。
  - 新渠道身份模型：channel_configs 的 owner 即 canonical user（无需 ChannelBinding 二次绑定）；群消息触发是平台硬规则（钉钉/QQ 必须 @，飞书只申请 group_at_msg），adapter 只按平台规则订阅。
  - 验证发现 3 处与本次无关的存量失败：compose 六服务断言（mcp-gateway 服务加入后测试未同步）、integration 2 个（workspace 自动建行 vs 测试直插，c62db8ab 引入，base commit 可复现）、mcpgateway（Windows fakeserver）。

## [2026-08-10 | IM 流式输出（im-streaming-delivery epic）]

- **Events**：按 `tenant_platform/docs/IM_STREAMING_DELIVERY.zh-CN.md`（用户先调研 QQ 原生流式后定稿，要求实施前对照源码复核）实现 worker→IM 流式转发：按渠道分档（飞书=消息编辑打字机 / QQ 单聊=原生流式接口 / 钉钉微信=只发最终结果）+ 群聊收敛。7 个子任务全部 DONE；真实渠道冒烟待用户凭据。
- **Changes**：
  - migration 0054：`tasks.conversation_type('private'|'group')`（群聊收敛判定维度）+ `tasks.stream_final_at`（流式 commit 成功置位，delivery 据此跳过文本 part 文件照发）+ `platform_runtime_settings.text_value` 列 + `im_streaming_mode` 默认 streaming。
  - transport 新可选接口 `StreamingSender`/`StreamReply`（BeginReply/Append/Commit/Abort）：LoopbackTransport 实现（SentStreams 断言）；ILinkAdapter 生产实现（poller /send 扩展 stream_id+stream_action(open|append|commit|abort)，**不新增 HTTP 端点**）；非流渠道不实现（BeginReply 失败 → scheduler 回退终态 delivery）。
  - scheduler：`StreamForwarder`（stream_forwarder.go）= per-task 500ms 节流合并缓冲（streamBatcher，首条 chunk 为窗口起点）+ open/append/commit/abort 编排 + Enabled 判定矩阵（mode+source+conversation_type，fail-closed）；dispatch 非空 chunk→AppendText、心跳→FlushDue、SUCCEEDED→Commit+MarkTaskStreamFinal、defer Abort 兜底全部失败路径。
  - poller：FeishuAdapter 消息编辑（占位 CreateMessage→message_id→PUT UpdateMessage 全量替换，累积语义）+ _TokenBucket 5 QPS 节流；QQAdapter 原生流式（POST /v2/users/{openid}/messages + stream{state,id,index,reset} 帧序列 + msg_id 被动锚点 + _MAX_STREAM_APPENDS=2 被动回复 4 次/条保护）；BotAdapter 基类 send_stream_* 默认抛错（群聊/非流渠道拒绝）。
  - runtime settings：GET/PUT /v1/admin/settings/im-streaming（off|final_only|streaming）+ OpenAPI + Web SettingsPage 下拉。
- **Insights**：
  - **"流式"在 IM 语境 = 三种形态**（消息编辑/原生流式接口/限速分片），不是统一代码路径；飞书/QQ 编辑或流式接口都是**全量替换语义**（append 发送累积文本，非增量）——adapter 内部必须累积。
  - **QQ 原生流式参数**：官方文档无专门页面；实证来源 linux.do 2026-03 实测 + easybot SDK（stream.state 1=生成中/10=结束、id 首条 null 后续用返回 id、reset 终帧全量替换、被动回复 msg_id+msg_seq 递增去重）；index 递增语义有歧义需真实凭据实测。QQ 群聊官方明确无流式参数。
  - **QQ 官方契约修正（用户指出应以官方文档为准）**：最终以腾讯第一方实证为准——@tencent-connect/qqbot-nodejs 1.0.4 源码：`POST /v2/users/{openid}/stream_messages` 独立端点 + 扁平字段（input_mode/input_state/content_type/content_raw/event_id/msg_id/stream_msg_id/msg_seq/index），**同一流 msg_seq 共享仅 index 递增**，msg_id 必填（无锚点拒绝），限流 429/50002 指数退避；初版按 linux.do 的嵌套 stream{state,id,index,reset} + msg_seq 递增实现已重写对齐官方。教训：QQ 官方文档索引页不提流式细节，但腾讯官方开源 SDK（openclaw-qqbot/qqbot-nodejs）是权威实现，优先于第三方博客。
  - **成功路径"流式消息即最终交付"**与 outbox delivery 机制冲突的解法：stream_final_at 标记 + delivery 文本 part 跳过（文件照发），失败路径无标记照发兜底——不需要动 delivery 状态机。
  - **scheduler 事件循环无定时器**：500ms 节流 flush 由"下一 chunk 到达/心跳空 chunk/Terminal"驱动，低频 chunk 场景靠心跳 FlushDue 防滞留。
  - **节流窗口起点 bug**：lastFlush 零值时 now.Sub(zero) 恒 ≥ 窗口，第二条 chunk 就误触发 flush——首条 chunk 到达时刻作为窗口起点。
  - test 踩坑：botpy 无 `botpy.route` 模块（Route 在 `botpy.http`）；`run_coroutine_threadsafe` 需要 loop 在后台线程运行（fake 测试用 _LoopRunner）；`Route.path` 是未格式化模板。

## [2026-08-10 | IM 流式官方对齐复审（用户批评后两轮修正）]

- **Events**：用户指出实现依据应看官方文档——第一轮按 linux.do 博客（嵌套 stream{} 参数、/messages 端点、msg_seq 递增）实现被否；第二轮追到腾讯第一方 @tencent-connect/qqbot-nodejs 1.0.4 源码修正为官方契约（独立端点 /v2/users/{openid}/stream_messages + 扁平字段 + msg_seq 同流共享仅 index 递增 + msg_id 必填）；第三轮（本次）逐项对照官方 SDK 再审查，又发现 3 处偏差并修正。
- **Changes**：
  - 飞书 20 次编辑上限保护（官方文档明示）：_MAX_STREAM_EDITS=18，超限停止 PUT 累积到 commit 一次送达。
  - QQ 限流重试（官方 sendWithRetry 同构）：429/50002 指数退避 1s/2s 重试 2 次，重试帧 index 递增；append cap 从 2 放宽为 60（官方无帧数限制，业务上限防超长任务刷单关系 20 QPM）。
  - 全部单测更新 + 新增（编辑上限/限流重试/cap 模拟）。
- **Insights**：
  - **腾讯官方 SDK 是 QQ 能力最权威实现**：官方文档索引页不写流式细节（只有"发送单聊消息/流式消息"一句），但 openclaw-qqbot（@tencent-connect 官方）的 qqbot-nodejs 里有完整 StreamSession 实现——npm pack 下载源码逐行对照是正解，优先级高于第三方博客/逆向。
  - **飞书消息编辑有硬上限（20 次/条）**：打字机实现必须 adapter 侧计数截断，不能依赖 platform 节流——这是设计文档渠道矩阵没写明的官方限制。
  - **审查方法论**：实现后必须逐字段对照官方 SDK 源码（端点/payload 形状/msg_seq 语义/重试行为/边界限制），不能只看参数名是否相似；限流重试、编辑次数上限这类行为约束最容易漏。
  - 环境坑：Windows Git Bash heredoc 写含中文/转义 Python 脚本反复踩坑（
 字面 vs 真实换行、CRLF、三引号嵌套）——长脚本一律用 write 工具落盘再执行，不经 shell 传输。
## [2026-08-11 | 企微(wecom)渠道落地 + 审查 C1 教训]

- **Events**：用户确认"再加一个企业微信渠道"——渠道模块化假设成立（每渠道 = Go 枚举 + poller 适配器 + Web 卡片，DB 唯一键 (owner_id, channel_type)）。实现全链路后按用户要求走独立审查子代理，抓到 1 个 Critical（自己验证矩阵全绿仍漏）。
- **Changes**：
  - Go：ChannelWecom/SourceWecom 枚举+白名单；`channelTypeForTaskSource` 加 wecom 分支（C1 修复，RED→GREEN + delivery 路由测试）；StreamForwarder Enabled 矩阵 +wecom（单聊流式/群聊收敛）。
  - poller：WeComAdapter（wecom_aibot_sdk WebSocket，线程内自管 asyncio loop + `run_coroutine_threadsafe` 同步桥）；入站 frame→webhook body（chatid=对话单元，单聊 chatid==userid 判 private，空 sender 保守归群——M3）；出站 SEND_MSG markdown 承载纯文本（SDK SendMsgBody 无 text 类型，被动 reply_stream 依赖入站 req_id 不适合异步 delivery）；流式 SEND_MSG+stream 帧（首帧 '…' 占位 M4、open 失败清理流条目 M5）；BotManager 工厂接线。
  - Web：CREDENTIAL_CHANNELS +企微卡片（Bot ID/Bot Secret 标签、secretLabel 可选泛化、卡片 map 渲染消硬编码索引）；前端必填校验文案随渠道标签（M2）。
  - OpenAPI 枚举 6 处 + IM_CHANNEL_BINDING/IM_STREAMING_DELIVERY 文档同步（企微行设计矩阵早有，落实为已实现）；migration 无需新增（复用 app_id/app_secret 槽位）。
  - 测试：poller 10 个 wecom 用例（入站映射/出站帧/流式状态机/未知流幂等/工厂/空 sender 防御）+ Go TDD（API 保存/凭据校验/流式矩阵/delivery 路由）；全量 52 passed + contract 23 + web lint/build 绿。
- **Insights**：
  - **新渠道接入的完整同步清单**（缺一即错）：Go `IsValidChannelType`/`IsValidSource`/`channelTypeForTaskSource`（回复路由键，最易漏！）/`StreamForwarder.Enabled` 矩阵 + poller `VALID_CHANNEL_TYPES`/`_build_adapter` + OpenAPI 枚举 + Web `ChannelType`/卡片。**测试全绿≠覆盖全**：流式矩阵测试只测 Enabled()，不测 openReply/delivery 解析路径——fake bot resolver 按 channel_type 匹配真实 store 语义后 C1 才被测到（之前 fake 忽略渠道参数）。
  - **企微智能机器人凭据 = bot_id/secret**（非 app_id/app_secret）；SDK `send_message` 主动发送只支持 markdown/template_card/media（text 仅被动回复）；`reply_stream` 是被动能力（透传入站 req_id），异步 delivery 必须走 SEND_MSG。
  - **渠道接入形态先看项目内先例**：root `frontends/wecomapp.py` 已在用 wecom_aibot_sdk——新渠道优先复用既有 SDK/模式，不做第二套。
  - **审查子代理价值实证**：独立 reviewer 抓到自己全程 TDD + 验证全绿仍漏的路由分支（该分支无测试覆盖）；fake 增强（按 channel_type 匹配）是让测试能捕获此类缺陷的关键。
  - 残余风险（已文档化）：企微 SEND_MSG 主动流式帧未实测——若服务端静默丢弃用户只见首帧；上线前真实凭据冒烟，未通过前 im_streaming_mode 保持 off/final_only。

## [2026-08-12 | 推送审查：装配时序/死代码/配额语义修复]

- **Events**：用户推送 156 文件（MCP 治理 + IM 渠道/流式 + 分桶）后要求审查并发/逻辑/语义与新旧混乱；独立审查发现 2 个功能未接线 + 3 个语义缺陷；确认后全部修复并补回归测试。
- **Changes**：
  - B1 `cmd/platform/main.go`：transport 装配块（botTransport/botLifecycle/botPollerClient）整体前移至 `NewScheduler` 之前——Streaming 端口构造时立即断言 botTransport（值语义），先前声明后赋值导致断言恒 nil，IM 流式全链路静默失效；顺带消除 channelSvc.Start 闭包的 nil 窗口。
  - B2 `worker_credential.go`：`filterMCPServersByQuota` 接入 `issueInitialWorkerCredentials`（resolveMCPSnapshot 之后、签发之前）——此前仅有定义与单测，生产路径零调用，"调度过滤耗尽 server"设计未生效。
  - Y1 `worker_mcp_proxy.go`：配额扣减后移到 resolve 白名单校验之后——404/未启用 server 不再烧用户配额（原实现在 resolve 前扣减）。
  - Y2 `mcp_server_store.go`：`ConsumeMCPQuotas` 改单事务双周期（day→month 固定 FOR UPDATE 锁序）——原实现先扣 day 再查 month，被拒调用烧 day 计数。
  - Y3 `api/mcp_server.go`：`mergeMaskedHeaders` 掩码不匹配/新键提交掩码 → 400 拒绝——原实现把不匹配掩码字符串原样落库成为真实 header。
  - 回归测试 5 个：签发路径过滤（B2）、404 不烧配额（Y1）、被拒不烧 day + 双周期并发恰 limit（Y2）、陈旧掩码/新键掩码拒绝（Y3）。
- **Insights**：
  - **测试全绿 ≠ 功能生效**：B1/B2 都是"有实现、有单测、装配/调用链断掉"型缺陷，单测直接构造对象绕过了装配层——cmd 级装配时序需要断言（或至少注释硬约束）。
  - **Go 按值求参，非延迟引用**："先声明后赋值再注入"对立即求值的构造参数无效；闭包（channelSvc.Start 捕获变量）才是延迟引用。
  - **死代码检测**：写完带单测的函数后 `rg "函数名" --glob '!*_test.go'` 确认生产调用点存在。
  - 配额扣减要"先校验后扣费"：白名单校验（resolve）应先于计量（quota），404/502 类调用不应消耗周期性稀缺配额；多周期组合扣减必须单事务（部分扣减副作用会让被拒调用逐步烧尽另一周期）。
  - 掩码类"编辑保留"契约要处理陈旧回显：掩码不匹配 = 数据竞争/伪造信号，拒绝（400）而非落库。
  - gofmt 1.26 与仓库既有格式（旧版 gofmt）有对齐差异：只格式自己改的文件，且纯对齐噪音手动还原，保持 diff 聚焦（CI 不跑 gofmt）。

## [2026-08-12 | proxy 计量原则：JTI 预算只在调用发起时扣(Y5)]

- **Events**：上一轮审查后用户确认 JTI 预算先于 resolve 消费是设计问题（与 Y1 同类：拒绝路径烧稀缺计量），要求对称修复后再提交。
- **Changes**：
  - `worker_mcp_proxy.go` / `worker_sophub_proxy.go`：`authenticate`（校验+计量合一）拆为 `validateToken`（Bearer 解析+签名/在线校验+audience/operation）与 `consumeBudget`（ParseBudgetMaxTurns + JTI 原子消费）。
  - MCP `ServeProxy` 顺序：validate → serverID 400 → resolve 404/503 → 用户配额 429/503 → JTI 消费 403/429/503 → 转发。404 与配额 429 不再烧 JTI。
  - Sophub `ServeSearch`/`ServeInstall`：参数校验（q/id 为空 400）在 JTI 消费之前；fetch 后 403 判定视为已发起仍消费。
  - 回归测试 3 个：MCP 404 断言 quota+consume 均零调用；MCP 配额 429 断言 consume 零调用；Sophub 参数错误 400 断言 consume 零调用（经 handler 路由分发，覆盖 search/install 两路径）。
- **Insights**：
  - **计量语义统一原则**：预算只在"调用即将发起"时扣——客户端错误（401/400）、白名单拒绝（404）、配额拒绝（429）、系统故障（503）不计量；上游调用失败（502）与 fetch 后内容判定（403）视为已发起，计量。本次修复统一 mcp/sophub 两 proxy；llm-proxy 为既有计量模式（validate→consume→解析，model mismatch 409/body 413 也烧预算——无白名单/配额概念、拒绝路径罕见，未纳入本次范围）。
  - 拆分后 `writeMCPAuthFailure`/`writeAuthFailure` 复用（consumeBudget 返回状态码，错误响应函数不变），改动面小且对称。
  - 顺序约束：用户配额（周期性稀缺）先于 JTI（单任务短期）——两者都成功才转发，任一拒绝都不烧另一项。

## [2026-08-12 | 推送前整体设计审查(二轮)：两阶段配额 + 事务防御回滚]

- **Events**：Y5 提交后按用户要求做推送前整体设计审查，交叉验证计量顺序与事务语义，发现 2 处自我引入的缺陷并修复。
- **Changes**：
  - **proxy 配额两阶段化（Y5 二轮）**：Y5 新顺序（quota 扣减先于 JTI 消费）在 JTI 429/403/503 时产生配额白扣——正是 Y2 修复的同类缺陷在 proxy 层复发。`NewWorkerMCPProxy` 增 `quotaCheck`（MCPQuotaAvailable 只读预检，先于 JTI 消费）+ `quotaConsume`（ConsumeMCPQuotas 条件扣减，在 JTI 消费之后）：任一拒绝路径（404/配额预检 429/JTI 429/503）零扣减；唯一残余为极端竞态（预检通过→JTI 扣→配额扣减时被并发耗尽）下 JTI 白扣一次（短期预算可接受，用户配额从不错扣）。main.go 装配拆 quotaOwner/quotaCheck/quotaConsume 三闭包；测试 8 处调用点同步，新增"预检通过但 JTI 耗尽 → 配额扣减零调用"回归。
  - **ConsumeMCPQuotas 防御分支语义修正**：扣减循环内 ErrNoRows 防御分支原 `return nil`——withTx 语义下 nil=Commit，若触发则已扣周期生效（部分扣减）。改 `return error` 触发回滚（当前理论不可达，但防御代码语义必须正确，防未来 DELETE usage 路径埋雷）。
  - 归档修正：llm-proxy 为既有计量模式（非"三处一致"），未纳入本次范围。
- **Insights**：
  - **"先校验后扣费"必须两阶段**：条件扣减原语（ConsumeCapabilityCall/ConsumeMCPQuotas）互相之间仍有顺序问题——A 扣成功、B 拒绝 = A 白扣。正确模式：所有只读预检先行（不扣），条件扣减按序执行（各自"成功才扣"），唯一残余竞态窗口内的白扣选**影响小的一方**（短期 JTI vs 周期性用户配额→选配额不错扣）。
  - **防御分支语义必须与事务协议一致**：`withTx` 里 `return nil`=提交、`return error`=回滚——防御性 return 用错会把"整体拒绝"变成"部分提交"。
  - 自我引入缺陷的检测方法：对修复做"反向推理"——Y5 移动了扣减顺序后，问"现在哪条路径会扣了不成功/成功了被拒"。
