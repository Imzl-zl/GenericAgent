 总评                                                                                                           
                                                                                                                
 两份文档是成对的 PRD + 架构，不是互相打架的两套方案。产品形态被 iLink 1:1                                      
 约束推得很清楚，架构也做了源码核实并修正了「只存 self.history → 钝化后失忆」这类硬伤。
                                                                                                                
 结论：方向对，可落地性中上；但还不能直接开工。 先收文档内矛盾 + 拍板安全/批准策略，再写实现计划。
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────
                                                                                                                
 各自质量                                                                                                       
                                                                                                                
 ### PRD（...im-platform-design.md）— 产品侧扎实                                                                
                                                                                                                
 ┌────────────────┬───────────────────────────────────────────────┐
 │ 强项           │ 说明                                          │
 ├────────────────┼───────────────────────────────────────────────┤
 │ 协议驱动产品   │ 明确放弃「共享 bot 多好友」，避免做不可行形态 │
 ├────────────────┼───────────────────────────────────────────────┤
 │ 双模式一等公民 │ 个人 / 团队并存，不是假团队                   │
 ├────────────────┼───────────────────────────────────────────────┤                                             
 │ 中继 ≠ 组队    │ 边界写清楚，防产品混淆                        │
 ├────────────────┼───────────────────────────────────────────────┤                                             
 │ 分期合理       │ P0 个人闭环 → P1 团队/中继                    │
 ├────────────────┼───────────────────────────────────────────────┤                                             
 │ 验收可测       │ 隔离、/stop、踢人立即生效等可直接转测试       │
 └────────────────┴───────────────────────────────────────────────┘
                                                                                                                
 待拍板项（§11）仍开着，属正常；别带着「建议」直接写死实现。
                                                                                                                
 ### 架构（...architecture.md）— 工程侧明显迭代过
                                                                                                                
 ┌─────────────────┬────────────────────────────────────────────────────────────────────┐
 │ 强项            │ 说明                                                               │
 ├─────────────────┼────────────────────────────────────────────────────────────────────┤
 │ 源码对齐        │ backend.history vs self.history 分清；save_state/load_state 设计对 │
 ├─────────────────┼────────────────────────────────────────────────────────────────────┤
 │ 托管 API 契约   │ 不偷读私有字段，比「改 2 行」靠谱                                  │
 ├─────────────────┼────────────────────────────────────────────────────────────────────┤
 │ persona-in-task │ 团队排队 race 有真解，不是会话级 set_persona 糊弄                  │
 ├─────────────────┼────────────────────────────────────────────────────────────────────┤                       
 │ 安全前置        │ Key 本地代理 + 沙箱标成 MVP 必做，判断正确                         │
 ├─────────────────┼────────────────────────────────────────────────────────────────────┤                       
 │ SessionPool     │ 独立 GA + 钝化 + busy 跳过 LRU，小规模可用                         │
 └─────────────────┴────────────────────────────────────────────────────────────────────┘
                                                                                                                
 这是「读过 GA 再写」的架构，不是空中楼阁。
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────                               
                                                                                                                
 对齐得好的地方
                                                                                                                
 1. Bot 拓扑：每用户扫码绑自己 bot，平台持有全部 bot_token 做中继。                                             
 2. 两层批准：users.status + team_members.status。                                                              
 3. session_key：user_* / team_* / relay_*。
 4. 分期：P0 个人，P1 团队 + 中继。
 5. 安全条款交叉引用：S1/S3 ↔ 架构 7.5。
                                                                                                                
 核心模型一致，没有「PRD 要共享 bot、架构做 1:1」这类结构性冲突。
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────                               
                                                                                                                
 必须先修的问题
                                                                                                                
 ### 1. 架构文档自相矛盾（实现会抄错）
                                                                                                                
 ┌──────────────────────────┬───────────────────────────────────────┬──────────────────────────────────┐        
 │ 位置                     │ 写的是                                │ 决策 6 实际应是                  │        
 ├──────────────────────────┼───────────────────────────────────────┼──────────────────────────────────┤        
 │ 图 3、set_persona 调用链 │ put_task 前 set_persona               │ persona 进 task                  │        
 ├──────────────────────────┼───────────────────────────────────────┼──────────────────────────────────┤        
 │ §4.2                     │ 「任务未完成前不接受下一条 put_task」 │ 允许排队，persona 跟 task        │        
 ├──────────────────────────┼───────────────────────────────────────┼──────────────────────────────────┤        
 │ 决策 8 示例              │ @B 你好                               │ PRD / 状态表：整条仅为 @username │        
 └──────────────────────────┴───────────────────────────────────────┴──────────────────────────────────┘        
                                                                                                                
 Decision： 以决策 6 + PRD R2 为准；图 3、§4.2、决策 8 示例统一改掉。否则实现必分叉。
                                                                                                                
 ### 2. 工具沙箱被高估
                                                                                                                
 路径白名单 + 命令子串黑名单 挡不住认真攻击：
                                                                                                                
 - cd/os.chdir 绕路径                                                                                           
 - base64 / 拼接绕 curl/wget                                                                                    
 - 读 /proc、环境、其它进程
 - 同进程同 OS 用户，一租户崩/打满可拖死全站
                                                                                                                
 对 S1「用户数据默认互不可见」，MVP 至少要到：
                                                                                                                
 - 每 session 独立进程（最低），或                                                                              
 - 轻量容器 / user namespace（推荐）
                                                                                                                
 字符串过滤只能当附加层，不能当主隔离。文档应把「路径白名单」降为 defense-in-depth，主方案写进程/容器隔离。     
                                                                                                                
 ### 3. 「合规、无封号风险」过满                                                                                
                                                                                                                
 iLink 官方通道 优于灰色协议，不等于零风险。PRD S6 已声明风险自担，架构决策 1                                   
 的「无封号风险」应改成「相对合规、风险更低」。
                                                                                                                
 ### 4. 管理员批准：PRD 与架构建议打架
                                                                                                                
 - PRD 流程 / 验收：注册 → 管理员批准 → 绑定                                                                    
 - 架构 §11 / PRD §11：建议 MVP auto-approve
                                                                                                                
 未拍板前，MVP 范围和 admin 后台是否 P0 都定不下来。                                                            
                                                                                                                
 建议： MVP 代码路径保留 status 检查；默认 approved（或环境变量开关），admin 批准 UI 可                         
 P1。两边文档写同一句话。                                                                                       
                                                                                                                
 ### 5. 团队 key_info 共享未闭环
                                                                                                                
 架构 7.5.3 推荐 A：接受共享 + PRD 声明。合理，但 PRD T7 / G6 还没写清：
                                                                                                                
 ▏ 团队对话中抽取的关键信息对全体成员可见；不要在团队里塞私人敏感信息。
                                                                                                                
 不写进 PRD，实现/运营会按「隔离 bug」扯皮。
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────
                                                                                                                
 设计风险（方向对，但要控）
                                                                                                                
 ### 范围 vs MVP                                                                                                
                                                                                                                
 P0 实际包含：                                                                                                  
                                                                                                                
 账号 + Web + 多 bot 长轮询 + SessionPool + GA 托管 API + LLM 代理 + 沙箱 + 端到端
                                                                                                                
 对小团队偏满。可砍：
                                                                                                                
 - Web 先极简（绑定 + 状态 + 人设一页）
 - 管理员后台延后（auto-approve）
 - 表情 / 中继严格 P1
                                                                                                                
 ### GA 改动量可能低估
                                                                                                                
 work_dir 不止 __init__ + handler 一行；log_path / vfile / task_file / reflect 等多处 script_dir/temp。
 「约 45 行」应改成：以托管 API 为边界做一次全量 temp 路径审计，改完再锁行数。
                                                                                                                
 ### 单进程 50×GA                                                                                               
                                                                                                                
 - 内存估算 2.5–5GB 可接受
 - Python 线程 + 工具子进程 + 长轮询：故障域是整平台
 - 一个 session OOM/死循环工具 ≈ 全站抖动
                                                                                                                
 MVP 可单进程，但文档应写清 P1 进程边界（gateway / session worker / web 拆分），别等 200 用户再想。
                                                                                                                
 ### LLM 代理解决 S3，不解决计费公平
                                                                                                                
 代理让 GA 进程内无真实 Key —— 对 S3 正确。
 全站仍共用上游 Key 时：用量/限流/滥用要在 代理层按 session_key 做，文档里「P1 按租户路由 Key」应至少把         
 per-tenant 配额 标成 MVP 最小（即使 Key 仍是平台统一）。
                                                                                                                
 ### 中继触发语义                                                                                               
                                                                                                                
 PRD：整句仅为 @username
 架构示例：@B 你好
                                                                                                                
 产品上「整句 @」更清晰、防误触；架构示例按 PRD 改。若要 @B 你好 直开中继，PRD R2 要改，并定义首条正文怎么传。  
                                                                                                                
 ### /同意 /拒绝 复用
                                                                                                                
 团队邀请与中继共用命令，需 状态机优先级（进行中中继 vs 待处理团队邀请）。PRD/架构都未写冲突表，实现必踩。      
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────
                                                                                                                
 评分（主观）
                                                                                                                
 ┌──────────────────┬──────────────────────┬──────────────────────────────┐
 │ 维度             │ PRD                  │ 架构                         │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 问题定义 / 边界  │ 9/10                 │ 8/10                         │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 与 iLink 一致性  │ 9/10                 │ 9/10                         │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 与 GA 源码一致性 │ n/a                  │ 8.5/10（有核实、有旧图残留） │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 安全可上线       │ 需求写到了           │ 问题找对了，方案偏软（沙箱） │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 可实施性         │ 分期清楚             │ 模块全，MVP 偏重             │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 文档自洽         │ 好                   │ 中（图/表/决策未统一）       │
 ├──────────────────┼──────────────────────┼──────────────────────────────┤
 │ 整体             │ 可评审通过（补拍板） │ 方向通过，修矛盾后再计划     │
 └──────────────────┴──────────────────────┴──────────────────────────────┘
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────
                                                                                                                
 建议动作（按优先级）
                                                                                                                
 1. 修架构内矛盾：persona-in-task、会话排队策略、中继 @ 规则、图 3 与 §4.2。
 2. 拍板 4 项（阻塞实现计划）：
   - MVP 是否 auto-approve
   - 团队 key_info 选 A 并写进 PRD
   - 中继触发：整句 @ vs @ + 正文
   - 沙箱主方案：独立进程 or 容器（白名单降级）
 3. 收紧 MVP：个人闭环 + 隔离验证；admin/表情/中继不进 P0。
 4. 补：/同意 冲突表、崩溃恢复（半截 task、checkpoint）、bot token -14 运维流。                                 
 5. 再开 实现计划（接口签名 + 任务拆解），不要直接开写 platform/。
                                                                                                                
 ────────────────────────────────────────────────────────────────────────────────                               
                                                                                                                
 一句话
                                                                                                                
 ▏ 产品想清楚了，架构也贴着 GA 现实长出来了；最大问题不是方向错，而是文档未完全收敛 + 隔离方案偏乐观。          
 ▏ 修自洽、拍安全与批准，再动代码——这两份已经比多数「多租户 Agent 平台」草稿可用。
                                                                                                                
 需要的话我可以下一步直接：                                                                                     
 - 列一版「文档修订 diff 清单」，或
 - 按你拍板结果起草实现计划骨架。

 ※ recap: We reviewed the multi-tenant PRD and architecture specs: direction is solid, but doc contradictions   
 and sandbox/approval open items block coding. Next: you decide the four open product calls, or I draft a       
 revision checklist.  