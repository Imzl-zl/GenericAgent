# Progress

- Shape: epic
- FinalizationStatus: blocked
- Truth: .tasks/document-processing-platform/SUBTASKS.csv
- Completed: 4/5
- Current: 5 - all local work and the self-service image bundle are complete; actual target Linux verification is the only blocked gate
- Latest validation: task 5 local implementation and image bundle passed fresh PostgreSQL package normal/race; Worker 91 passed/2 skipped; security+contract+smoke 62 passed; Web build/lint; four digest-base image builds; resolved Compose JSON; fresh-volume four-service health; PostgreSQL 70:70 hardening; two-way delivery GID/mode checks; down/up database and file persistence; volume archive/restore ownership checks; custom pg_dump listing; Bash syntax; `diff --check`. Independent image-bundle re-review reports no Critical/Important. Local Docker Desktop remains correctly unaccepted because it is not the dedicated rootless target.
- Workspace: C:/sudy/github/GenericAgent/.worktrees/feature-document-job-pool
- Branch: feature/document-job-pool
- Completed task 1: versioned document pool settings; PostgreSQL CAS; deployment hard max; admin API/Web; runtime reconciler; full serial Go, race, Web build/lint, and independent review passed
- Completed task 2: document job/command/instance domain and migrations; PostgreSQL atomic queue/store with owner+generation+unexpired lease fencing; fixed-security Docker/Podman runtime; application DocumentManager lifecycle; independent `cmd/document-manager`; systemd boundary so only document-manager may hold runtime permissions; opt-in Docker smoke script
- Runtime environment note: local Docker Desktop is not rootless, so `GA_DOCUMENT_DOCKER_SMOKE=1` fails closed at host verification (`container runtime is not rootless`) before container create. Target rootless cgroup-v2 deployment must run `tenant_platform/tests/smoke/document_pool_docker.sh` with `GA_DOCUMENT_DOCKER_SMOKE=1`.
- Completed task 3: typed `export_docx` and loopback gateway; same-task multi-command job reuse; deterministic command idempotency; owner/generation/lease-fenced atomic artifact persistence; strict per-request task/membership/policy authorization; bounded verified download; Worker POSIX directory-FD atomic delivery and `[FILE:]` markers; all terminal paths and cross-task retries close pending jobs; exact process/schema/metadata allowlists; independent security review passed after all Important findings were resolved.
- Completed task 4: admin-only fixed-origin Sophub binding/search/import; encrypted write-only key; mutable candidates and append-only local Markdown versions; explicit global load/unload; atomic task snapshots preserving dedupe identity; typed scheduler/Worker transfer with digest and size validation; GA-native task-scoped `extra_sys_prompts` loading and cleanup; tenant read-only Web with no WeChat selection; tool policy remains the sole authorization boundary.
- Task 5 local scope complete: admin-only truthful PostgreSQL pool status and responsive Web panel; distinct-UID Linux rootless/cgroup-v2/digest/systemd preflight; exact NSS groups, proc/env/filesystem isolation and fixed effective config paths; exact-socket Document Manager access; real-image smoke executes two commands in one container; mandatory admin+tenant smoke; deployment/start/evidence/rollback runbook; hardened self-service Compose images with host-only Document Manager, forced preflight lifecycle, fixed delivery GID, fresh-volume PostgreSQL and complete backup/restore; independent final reviews report no Critical/Important.
- Blocked now: provide the actual target Linux host/SSH execution path with approved secret provisioning and execute `tasks/security-verification` item 6, including daemon reboot recovery. Only after target evidence exists may task 5 and the Epic become DONE.
